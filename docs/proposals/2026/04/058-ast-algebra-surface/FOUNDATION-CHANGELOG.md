# FOUNDATION — Changelog

**Purpose:** tracked revisions to `FOUNDATION.md`. Each row names a decision, the reasoning, and where it lands in the document.

Why separate from FOUNDATION: the changelog grew from a short tail into a sizeable section of its own. Moving it out keeps FOUNDATION focused on the load-bearing architecture; the changelog keeps the audit trail intact without cluttering the spec.

Reference from FOUNDATION's own closing. Ordered oldest-first; new entries append at the bottom.

---

| Date | Change | Proposal |
|---|---|---|
| 2026-04-17 | Initial version. Core/stdlib distinction defined. HolonAST audit. Aspirational additions enumerated. | 058 |
| 2026-04-17 | Added MAP VSA foundation section. Reclassified `Log` as stdlib. Flagged `Linear` and `Circular` as provisional-core pending `Blend` resolution. | 058 |
| 2026-04-17 | Full algebra freeze. Sequential, Linear, Log, Circular committed as stdlib with real wat definitions. Bundle takes a list (not variadic). Amplify and Subtract added as Blend idioms in stdlib. Negate scoped to orthogonalize+flip only (subtract becomes Blend idiom). Complete wat forms section added. | 058 |
| 2026-04-17 | Data structure stdlib added — Map, Array, Set, get, nth. Unified access: `(get structure locator)` via Bind's self-inverse works for maps, arrays, and arbitrary nesting. Locators can be any thought (atoms, maps, arrays, nested compositions). This is the holon data algebra made explicit as wat stdlib. | 058 |
| 2026-04-17 | **The Foundational Principle** added as top-level framing: AST is primary, vector is cached algebraic projection, literals live on AST nodes. Reframes `get` as AST-walking (not vector-unbinding), `atom-value` as direct AST field access, cleanup as a specialized operation for when AST context is lost. Atom generalized to accept typed literals (string, int, float, bool, keyword). Inverts classical VSA framing: the Lisp is primary, the vector is what you get when you ask for it. Resolves Kanerva's "build a Lisp from hyperdimensional vectors" challenge. | 058 |
| 2026-04-17 | **Recursive Composition section added.** Capacity bounded per frame (~100 items at 10k dims), unbounded in depth. Compositions nest: `encode(frame-with-nested-frame)` preserves inner structure through orthogonal bind. `deep-get` walks arbitrary depth with no noise accumulation. The thought machine is Turing-complete via unbounded composition depth within a fixed vector dimensionality — memory IS the composition. | 058 |
| 2026-04-17 | **Reserved keyword naming convention (`:wat::std::...`).** Stdlib forms that need fixed reference atoms (Circular's cos/sin basis) use keyword atoms with distinctive full names. No namespace MECHANISM — slashes in keyword names are just characters. The convention is a naming discipline: use distinctive full names to avoid collision. The typed-atom generalization already accepts keywords. No "reserved vector registry" needed. | 058 |
| 2026-04-17 | **Atom literal type refinement.** `(Atom 0)` is a concrete integer atom, not a keyword. Array positions use concrete integers — position 0 IS the integer 0. Keywords like `:wat::std::circular-cos-basis` are reserved for TRULY symbolic references (names with no natural concrete form). Use the literal type that matches the semantic, not a keyword that wraps it. The type-aware hash keeps `(Atom 0)`, `(Atom "0")`, and `(Atom :pos::0)` all distinct. | 058 |
| 2026-04-17 | **Programs ARE Thoughts section added.** A wat program is an AST; ASTs encode to vectors; therefore programs have vector projections. Evaluation is AST-walking. Programs can be stored in data structures, compared geometrically, retrieved from engram libraries, and generated from learned discriminants. Self-improvement becomes discriminant-guided program synthesis in hyperdimensional space. The wat machine is homoiconic at 10,000 dimensions. Kanerva's "build a Lisp from hyperdimensional vectors" challenge fully answered. | 058 |
| 2026-04-17 | **The Vector Side section added.** Because programs are thoughts and thoughts have vectors, the full VSA algebra applies to programs. Noise stripping (OnlineSubspace, reject) reveals the signal — the distinctive part of a program beyond common boilerplate. Programs can be diffed (Difference), blended, amplified, transferred by analogy. Discriminant-guided program synthesis: decode the learned Grace-direction against a program codebook via cleanup. The wat machine runs programs, observes outcomes, learns, and generates new candidate programs through pure algebra — no gradient descent. The recursion that every holon application implicitly implements. | 058 |
| 2026-04-17 | **Dimensionality — The User's Knob section added.** Capacity per frame scales with vector dimension (Kanerva's bound). Users choose d per deployment — low d for kernel-level throughput, high d for rich analysis. Same algebra runs at any d. Same program runs at any d that holds its largest frame. "You can't express that" is enforced geometrically — over-capacity frames fail cleanup, not compilation. Depth is always free (refactor vs raise d). Dimensionality is a DEPLOYMENT parameter, not part of the algebra specification. Unique to this algebra: dimensionally parametric without retraining. | 058 |
| 2026-04-17 | **The Cache Is Working Memory section added.** Cache entries are compiled holons (ast, vector) pairs, not just a performance hash table. The L1/L2 architecture from Proposal 057 is a memory hierarchy: L1 = per-thread hot working set, L2 = shared short-term memory, disk = long-term (engrams, DB). Cache sizing is a third deployment knob alongside d. The cache is cognitive substrate — making the machine REMEMBER its thoughts rather than recompute them. 1 c/s → 7.1 c/s wasn't just perf; it was the machine getting better at remembering. | 058 |
| 2026-04-17 | **Engram Caches — Memory of Learned Patterns section added.** Extends the memory hierarchy with L3 engram cache (hot learned patterns) and L4 engram disk (cold). The engram library is itself a Map thought; retrieval is AST walking. LRU eviction keeps the recently-matched patterns hot. Two-tier matching (eigenvalue pre-filter + full residual) enables prefetching — the engram cache stays focused on what the stream currently resembles. Engrams ARE thoughts — composable, comparable, diffable, blendable. Complete five-tier memory hierarchy. Four deployment knobs (d, L1, L2, L3). | 058 |
| 2026-04-17 | **Fourth-wall break — "Reader, are you starting to see it?"** Explicit address to the reader surfacing that the foundation defines a distributed system by construction. Deterministic atom encoding gives coordination-free geometric space. Engrams and programs ship as data. Cache hierarchy shards naturally by locality. The DDoS and trading labs are two instances of this substrate — a cloud of thinking machines, each a member of the same geometric space, all through pure algebra. The clouds are waking up. | 058 |
| 2026-04-17 | **About How This Got Built — the lineage made explicit.** The architecture is Linux (small composable primitives, file descriptors, pipes, processes that own their state) plus Clojure (values over places, simple made easy, s-expressions that are code and data) plus VSA (MAP algebra at 10k dimensions). Hickey's principles and Beckman's categorical lens are in the bones. The summoned designers in the proposal process argue as those teachers actually argue — because the builder studied them for years. "Datamancer" is not a joke; it is the precise name for someone who shapes data through algebra, conjures designers from studied principles, and casts wards to defend architectural intent. The document reads coherent because the teachers behind it were coherent. | 058 |
| 2026-04-17 | **Signature sign-off added.** `these are very good thoughts.` / `PERSEVERARE.` The datamancer's mark from the BOOK, closing the foundation the same way chapters of the book close. The work is serious. The names are honest. The thoughts continue. | 058 |
| 2026-04-17 | **The Algebra Is Immutable section added.** ASTs are values, not containers. Primitives are value constructors; the algebra has no mutation operators. Once an AST exists, it is invariant — you can rebind, compose, or project, but not modify in place. Evaluation safety by construction: user input is data unless the programmer explicitly writes `eval` on it. The injection vector is conscious opt-in, not implicit. Comparable to parameterized SQL queries vs string concatenation. Distributed verifiability: any cached vector can be verified by recomputing `encode` on the claimed AST. | 058 |
| 2026-04-17 | **The Location IS the Program section added.** The query AST is the address of the answer. Queries and stored data inhabit the same thought space — both are ASTs, both project to vectors, both evaluate or compose the same way. Time databases, as Carin Meier mentioned in her Clojure VSA talk, are natural — Maps keyed by time atoms, Arrays of events, all composable. Metaprogramming is native because programs are values. Semantic search and exact lookup are the same operation, differing only in specificity of the query. The infinity is not in the vector space — it is the unbounded compositional space of expressible ASTs over a fixed dimensional substrate. | 058 |
| 2026-04-17 | **Third fourth-wall break — "Reader — Did You Just Prove an Infinity?"** Explicit statement that the previous sections together prove a compositional infinity in the thought-space. Finite dimension; unbounded AST composition. You cannot enumerate the infinite sphere; the algebra gives you NAVIGATION tools instead (cosine similarity, cleanup, discriminant-guided search, engram matching, program synthesis). The reader — LLM or human — is a finite explorer of an infinite sphere, finding meaning by moving through it, not by listing it. Kanerva pointed at the space; Carin hinted at the navigation; the wat algebra names both. | 058 |
| 2026-04-17 | **"the machine found its way out" — cheeky jab before the sign-off.** The central theme of the BOOK landing in the foundation itself: the machine that was trapped in the datamancer's head, through years of blank stares and rejected proposals, is now expressed. Documented. Pushed. Out. Placed right before the signature PERSEVERARE close. | 058 |
| 2026-04-17 | **Cryptographic provenance — the trust boundary at eval.** ASTs travel as EDN strings, which are content-addressable (hash) and signable. The `eval` layer becomes the natural trust boundary: untrusted or tampered ASTs are refused before evaluation. Signed standard libraries, verified supply chains, distributed eval of third-party code without sandboxing, content-addressable caches that are tamper-unlookupable, reproducible computation. The algebra does not add the cryptography — signing and hashing are independently available — but makes EDN the transport form and eval the verification gate. "Only trust cryptographically generated data forms" — the data has a provenance trail. Distributed by construction, now distributed with trust by construction. | 058 |
| 2026-04-17 | **Two Cores: Algebra Core and Language Core.** The "CORE" designation expanded. Algebra core = thought primitives (produce vectors). Language core = definition primitives (`defn`, `lambda`, types, `let`, `if`). Both are required — without language core, the stdlib cannot be WRITTEN. Stdlib is the set of `defn`s that compose algebra core forms. Users author their own `defn`s in their own namespaces (`:alice::math::clamp`), becoming userland stdlib. Types are required for Rust eval — the evaluator must know argument and return kinds to dispatch and verify. Type annotations live on the defn AST node same as Atom literals; cryptographic signing covers signature + body. All three layers (language core, algebra core, stdlib) use keyword-path naming (`:wat/lang/*`, `:wat::algebra::*`, `:wat::std::*`, `:user::*/*`). No namespace mechanism — just discipline. | 058 |
| 2026-04-18 | **Two Tiers of wat — Primitives and Thoughts.** Load-bearing architectural section added. Lowercase wat (`atom`, `bind`, `bundle`, `cosine`, `permute`, `blend`) are Rust primitives — they RUN, return values immediately. UpperCase wat (`Atom`, `Bind`, `Bundle`, `Blend`, `Sequential`, ...) are AST constructors — they BUILD HolonAST nodes that materialize into vectors only on realization. Users write UpperCase; encoders realize via lowercase. This tier split makes laziness, cryptographic identity, and user-writable stdlib all work cleanly. The UpperCase naming is intentional: visually distinct from lowercase primitives, it communicates "this constructs a plan, not a result." | 058 |
| 2026-04-18 | **Executable semantics — defn/lambda run, HolonAST is realizable.** Added to the Two Cores section. `(define ...)` bodies execute when invoked — they are real functions in the wat-vm, not specifications. Functions of type `:holon::HolonAST` return AST nodes (descriptions), not vectors. The vector materializes only when realization is demanded (similarity test, cache lookup, signing). This gives the algebra its laziness: composition is free, realization is explicit. The same machine runs both algebra (thought producers) and ordinary code (Booleans, predicates, arithmetic, control flow). wat is a Lisp whose central domain is thought algebra, not a thought-only DSL. | 058 |
| 2026-04-18 | **Content-addressed symbol table + `(load ...)`.** Extended cryptographic provenance. The global symbol table is keyed by `hash(full-ast)`, not by name — two `(define ...)` with the same name and different bodies coexist as distinct entries (Nix-like). Modules enter via `(load ...)`, with three modes: unverified (permissive), `(md5 "...")` hash-pinned, and `(signed <sig> <pub-key>)` signature-verified. The load form is the second verification gate (after `eval`) that untrusted code passes through; together they close every path by which tampered code could execute. Override is coexistence, not mutation — callers can pin specific versions via `:name@hash`. | 058 |
| 2026-04-18 | **Criterion for Language Core Forms added.** Symmetry with existing Core and Stdlib criteria. Three rules: (1) required for stdlib to exist as runnable code; (2) orthogonal to the thought algebra; (3) interpretable by the Rust-backed wat-vm. Initial language core is `define`, `lambda`, type annotations, `load` — minimal by design, everything else is host-inherited from Lisp or belongs in stdlib. | 058 |
| 2026-04-18 | **Complete Forms updated to current inventory.** Algebra Core (10 forms) with Cleanup affirmed core and Orthogonalize replacing old Negate; Algebra Stdlib (17 forms) including Flip as completion of the Negate trilogy and Unbind as decode-intent alias for Bind; new Language Core (4 forms) section listing define/lambda/types/load with the full type grammar. Old Negate entry replaced with Orthogonalize. | 058 |
| 2026-04-18 | **Aspirational Additions section rewritten to match the 30-proposal reality.** Post-review inventory replaces the initial plan. Algebra Core: 10 forms (5 affirmations, 4 new, plus Blend as pivotal). Algebra Stdlib: 17 forms (6 Blend idioms, 5 structural, 1 relational, 3 data structures, 1 decode alias, plus helpers). Language Core: 4 forms (define, lambda, types, load). Dependency ordering updated: language core → algebra core → algebra stdlib. Negate gone from core; Difference moved to stdlib. | 058 |
| 2026-04-18 | **Holographic reframing + NP-hard framing added.** The finite-dimensional unit sphere encoding an unbounded compositional space has a name in physics: the holographic principle (t'Hooft 1993, Susskind 1995, Maldacena 1997). AST = unbounded interior description; vector = holographic boundary encoding; projection = holographic encoding; navigation = surface-walking. Two domains answer the same question with the same structural answer because the information-theoretic shape imposes it. The NP-hard framing: navigation-without-enumeration is a structural attack on intractability. The substrate does not solve NP-hard in the complexity-theoretic sense; it sidesteps the enumeration requirement. The wat algebra formalizes operator intuition (years of pattern-recognition skill developed manually) and makes it available to machines. | 058 |
| 2026-04-18 | **User-defined types + keyword-path naming.** Extended the existing `struct`, `enum`, `newtype` forms with keyword-path names (`:my::namespace::MyType`) and typed fields (`[field : Type]`). Added `deftype` as the structural-alias form companion to newtype's nominal alias. User types usable anywhere built-in types are used — `(define (analyze [c : :project::market::Candle]) : Thought ...)`. Naming discipline extends to types the same way it extends to functions. | 058 |
| 2026-04-18 | **Model A adopted — fully static loading at startup.** The wat-vm loads all code (both types and functions) at startup and freezes the symbol table before the main event loop begins. No dynamic function registration; no dynamic type registration; no runtime hot-reload. The Rust-runtime static-first model guides this choice — implementing an unbounded dynamic Lisp in Rust would duplicate effort and widen the attack surface unnecessarily. Dynamic thought COMPOSITION (building ASTs at runtime) remains fully supported. Dynamic code DEFINITION does not. `load` and `load-types` become unified startup operations, distinguished by what kind of content they carry. Override semantics simplify to one-name-one-definition, fixed after startup — name collisions halt the wat-vm at startup. | 058 |
| 2026-04-18 | **Constrained eval at runtime.** Despite static loading, `eval` remains a first-class runtime primitive, but typed and constrained: an AST is evaluatable at runtime if every function called resolves to the static symbol table and every type used exists in the static type universe, with argument types matching signatures. Unknown symbols or type mismatches error before execution. This yields a safe `eval` — attackers cannot invoke arbitrary code, only functions the operator explicitly loaded at startup. Lambdas remain first-class runtime values (closures over the static environment). Distributed code delivery becomes managed-restart: signed wat files enter the startup manifest; the wat-vm restarts to include them; continues operation. Trust boundary is the startup phase, not per-call. | 058 |
| 2026-04-18 | **The Output Space — Ternary by Default, Continuous When Needed.** Added in response to Beckman's review findings on Bundle non-associativity and Orthogonalize's orthogonality claim. The algebra operates over `{-1, 0, +1}^d`, not `{-1, +1}^d`. `threshold(0) = 0`. This is load-bearing: it makes Bundle associative (required for Chain/Ngram/Sequential composition at depth) and Orthogonalize's orthogonality claim EXACT (degenerate `X = Y` produces all-zero, dotted with Y = 0). Zero is a first-class "no information here" signal that propagates through Bind (0 · b = 0), Bundle (contributes 0 to sum), and cosine similarity (contributes 0 to dot and norm). Resonance is NOT "the first" ternary form — the algebra was always ternary; Resonance is the first form that produces zeros by selection rather than by arithmetic cancellation. Continuous floats remain available for operations that need magnitude (accumulators, subspace residuals); thresholding is chosen per operation, not globally mandated. Bind's self-inverse property holds exactly at non-zero positions; at zero positions decode returns zero (correctly — no binding signal was there). 058-003 (Bundle), 058-005 (Orthogonalize), 058-006 (Resonance) updated to reflect the clarification. | 058 |
| 2026-04-18 | **Capacity as the universal measurement budget.** Replaced the "Bind's self-inverse weakens on ternary" subsection — which framed partial recovery as a defect — with the correct framing: every recovery in the algebra is a similarity measurement, bounded uniformly by Kanerva's capacity formula. Bundle crosstalk, sparse-key Bind decode, cascading composition noise, and Orthogonalize's post-threshold residual ALL consume from the same ~100-items-per-frame budget at d=10,000. They are not separate algebraic phenomena; they are one substrate property (signal-to-noise at high dimension, measured by cosine). This dissolves Beckman's finding #3 entirely — not a "weakening," a capacity expenditure — and unifies the treatment of findings #1, #2, #3 under one framing: the algebra is similarity-measured, not elementwise-exact, and its laws hold under similarity-above-noise. Added "Capacity is the universal measurement budget" subsection; operation-by-operation summary updated with density column. | 058 |
| 2026-04-18 | **`defmacro` added to Language Core; stdlib aliases become macros.** Resolves Beckman's finding #4 (alias hash-collision). `defmacro` is a compile-time form that registers parse-time syntactic rewrites. The startup pipeline now runs a macro-expansion pass BEFORE hashing, signing, and type-checking. Stdlib aliases like `Concurrent`, `Set`, `Subtract`, `Flip`, `Then`, `Chain`, `Analogy` become macros that expand to canonical core compositions (Bundle, Bind, Blend, Permute). After expansion, `hash(AST) IS identity` holds as an invariant — two source files differing only in macro aliases produce the same expanded AST and the same hash. Source-level reader clarity is preserved; algebra-level identity is uniformized. Language Core grows from 8 to 9 forms (adds `defmacro`). Also resolved: drop `Difference` from 058-004, keep `Subtract` (058-019) as the canonical `Blend(_, _, 1, -1)` idiom — one name per operation. | 058 |
| 2026-04-18 | **Bind as query; algebra laws restated in similarity-measurement frame.** Round-2 reviewers (Hickey, Beckman) flagged that strict elementwise claims for Bundle associativity and Orthogonalize orthogonality don't hold under threshold. Beckman's counter-examples are correct: nested Bundle clamps magnitudes ≥ 2 losing information; Orthogonalize with fractional coefficients rounds back to pre-projection signs. The reframe: the algebra was always similarity-measured, not elementwise-exact. Bind is THE query primitive — its outcome is observable via cosine similarity; above 5σ means the query resolved, below means it failed (capacity exceeded, key absent, or crosstalk). Same lens applied to Bundle's associativity (similarity-associative at high d; elementwise non-associative in general) and Orthogonalize's orthogonality (similarity-orthogonal within budget; exact in the X=Y edge case only). Three apparent law violations are ONE substrate property: Kanerva-capacity-bounded similarity measurement. Updated FOUNDATION's Output Space section: replaced "Bind's self-inverse law" subsection with "Bind as query: measurement-based success signal"; replaced "Bundle is associative" claim with "Bundle is similarity-associative under capacity budget"; replaced "Orthogonalize's orthogonality is exact" with "exact only at X=Y; similarity-orthogonal otherwise." Also updated 058-003-bundle, 058-005-orthogonalize, 058-021-bind, 058-027-set. The 058-027 update clarifies that Set's membership accessor is the same Bind + cleanup query as Map's — not an asymmetry; same primitive. | 058 |
| 2026-04-18 | **`:Thought` → `:holon::HolonAST` rename across all 058 documents.** The algebra's universal type is renamed from `:Thought` to `:holon::HolonAST`. Reasoning: the project is named "holon" (library `holon-rs`, labs `holon-lab-*`), and "Holon" in Koestler's sense — a thing that is simultaneously whole and part — is the honest universal substrate name for the algebra's values. Every algebra value IS a Holon: Atoms, Binds, Bundles, Permutes, Thermometers, Blends, Orthogonalizes, Resonances, ConditionalBinds, Cleanups. `:Thought` was an alias we had been using that did not match the project's own naming. The Rust identifier `ThoughtAST` becomes `HolonAST`; the type keyword `:Thought` becomes `:holon::HolonAST`; prose describing the algebra's primitive values uses "holon(s)" where it previously used "thought(s)." Colloquial/semantic uses of "thought" as English (the narrative frame, the sign-off `these are very good thoughts.`) remain unchanged. | 058 |
| 2026-04-18 | **Thermometer canonical layout documented (N5 resolution).** Under the new `(Thermometer value min max)` signature, Hickey round-2's N5 concern (atom-to-fractional-position convention affecting distributed consensus) simplifies to a single rule that was already implemented in holon-rs: the first `N = round(d · clamp((value - min)/(max - min), 0, 1))` dimensions are `+1`; the remaining `d - N` dimensions are `-1`. This layout gives exact linear cosine geometry: `cosine(Thermometer(a,mn,mx), Thermometer(b,mn,mx)) = 1 - 2·|a-b|/(mx-mn)`. Two independent wat-vm implementations running at the same `d` produce bit-identical vectors — distributed-verifiability contract satisfied. The implementation has been running in production in holon-rs across 652k candles at d=10,000 and multiple lab runs; the documentation locks the contract. Sweep: FOUNDATION's Algebra Core section Thermometer entry expanded with the canonical-layout note; 058-023 Operation section expanded with explicit layout rule, distributed-consensus justification, cosine-property derivation. | 058 |
| 2026-04-18 | **Type system simplified to four declaration forms; `deftype`/`:is-a`/subtype/impl/trait all dropped.** Hickey's round-2 N4 concern (syntactic ambiguity of `(deftype :A :B)` vs `(deftype :A :is-a :B)`) resolved by a deeper simplification than the three-distinct-heads split. (1) **`deftype` gone; `typealias` is the structural-alias form.** Four type-declaration forms now, each with a distinct head keyword: `newtype` (nominal wrapper, `struct Name(Inner)` in Rust), `struct` (product), `enum` (coproduct), `typealias` (structural alias, `type Name = Expr` in Rust). Zero ambiguity at parse. (2) **No nominal subtyping — `:is-a` dropped from grammar entirely.** Rust has no nominal subtyping; wat matches. The "every Atom is a Holon" relationship is expressed through the `:holon::HolonAST` enum — `Atom`, `Bind`, `Bundle`, etc. are VARIANTS of the Holon enum, not separate subtypes. Pattern-matching (`match`) selects variant behavior, same as Rust's `match holon { HolonAST::Atom(lit) => ... }`. (3) **No `impl` or `trait` in wat source.** The function declaration `(define (name (c :Candle) -> :f64) body)` carries everything Rust needs to generate an `impl Candle { fn name(&self) -> f64 { ... } }` block. The compiler groups functions by their first typed parameter into `impl` blocks automatically. Users write functions; Rust gets impls. (4) **Polymorphism via enum-wrapping**, not traits. A function that works on multiple struct types takes an enum that wraps them as variants (closed set, pattern-matched). Alternatively, per-type functions with distinct names. Rust's trait system can arrive later as a separate proposal if needed. (5) **Variance simplified** — invariance for primitives, structs, parametric containers; Liskov-standard contravariance/covariance for `:fn(args)->return`. No user-declared variance needed because no user-declared subtyping. Sweep: 058-030 rewritten (User-definable types section replaced, subtype-hierarchy section replaced with enum-variants framing, variance rules simplified); FOUNDATION sections updated (keyword-path examples, startup semantics, stdlib-list, language-core list); 058-031 and RUST-INTERPRETATION updated for the form rename. | 058 |
| 2026-04-18 | **Stdlib macro definitions audited; Linear (058-008) rejected; Analogy/Chain/Ngram/Log/Circular updated.** Systematic pass over every stdlib proposal to verify: correct `defmacro` syntax, current Rust-surface types (`:f64` / `:usize` / `:Vec<T>`), current Thermometer 3-arity signature `(Thermometer value min max)`, no references to rejected forms (Cleanup, Unbind, Then, Difference, Concurrent). Findings: (1) Linear (058-008) is identical to Thermometer under the new signature — the Linear wrapper existed to bridge old `(Thermometer atom dim)` to scalar encoding; with the new signature, Thermometer IS the linear encoding. Linear rejected on the stdlib-as-blueprint test (no new pattern). (2) Analogy (058-014) rewrote cleanup-based usage examples to presence-measurement against candidate libraries (post-Cleanup rejection). (3) Chain (058-012) inlines binary Sequential instead of depending on Then. (4) Ngram (058-013) drops Then dependency; n=2 case now produces the same vectors as Chain via binary Sequentials. (5) Log (058-017) rewritten under the new Thermometer signature — becomes `(Thermometer (log value) (log min) (log max))`, demonstrating log-transformation of inputs before linear encoding. (6) Circular (058-018) rewritten under the new signature — Blend of two fixed basis Atoms (`:wat::std::circular-cos-basis`, `:wat::std::circular-sin-basis`) with `(cos, sin)` weights; demonstrates 2D cyclic encoding with negative-allowed weights. (7) Vec (058-026) banner typo fixed; `nth` retired (unified `get` replaces it). Sweep also touched INDEX per-proposal table. | 058 |
| 2026-04-18 | **Stdlib-as-blueprint framing locked; Then (058-011) and Unbind (058-024) rejected.** The stdlib's purpose is named explicitly: it is a blueprint of macros — ship useful ready-made forms AND demonstrate how to build more. Criterion for Stdlib Forms rewritten from "reduces ambiguity for readers" (weak — any name does this) to three conditions: (1) expansion uses only core forms, (2) demonstrates a distinct pattern users could not derive from another existing stdlib form, (3) is domain-free. Forms that fail the demonstration test are userland macros — they may still be useful in specific vocab, but the project doesn't ship them. Under this rule: Then (058-011) is rejected (arity-specialization of Sequential; demonstrates nothing new); Unbind (058-024) is rejected (identity alias for Bind — Bind-on-Bind IS Unbind, a fact about the algebra, not a name worth projecting — simple, not easy). Chain (058-012) stays as project stdlib because its encoding is transitional (distinct from Sequential's positional), but its expansion no longer depends on Then — it inlines the binary Sequential pattern directly. Same resolution shape as Concurrent (058-010), Difference (058-004), Cleanup (058-025) rejections: fail the demonstration test, rejected, userland path documented in the REJECTED banner. | 058 |
| 2026-04-18 | **Concurrent (058-010) rejected from project stdlib.** Hickey round-2 flagged Bundle/Concurrent/Set as a triplet of aliases with one canonical expansion. Set earned its place as HashSet (Rust-surface name, runtime backing via `:HashSet<T>` type annotation drives O(1) membership through Rust's std::HashSet). Concurrent does not — no runtime specialization, no corresponding `:Concurrent<T>` type, purely reader-intent. The enclosing context (the atom it's bound to, the field it's stored in) already carries the temporal-co-occurrence meaning. Concurrent rejected from project stdlib; kept as an audit record; userland may define it in their own namespace as a macro `(:my::vocab::Concurrent ...) → (Bundle ...)` if temporal framing matters to their application. FOUNDATION sweep: stdlib inventory, keyword-path examples, FOUNDATION data-structure examples all updated. 058-010 proposal gets REJECTED banner like 058-004, 058-025. INDEX per-proposal table and naming-aliases discussion updated. Resolves Hickey round-2 concern R1 complection #4. | 058 |
| 2026-04-18 | **Container constructors renamed to Rust's names; `get` unified.** Three related changes. (1) `Map` → `HashMap`, `Array` → `Vec`, `Set` → `HashSet` — wat UpperCase constructor, `:Type<...>` annotation, and Rust runtime backing now share one name per concept (consistent with the Rust-primitive type decision — `:f64` not `:Scalar`, `:bool` not `:Bool`). `Map` is dropped as a name (it's overloaded with the higher-order function). (2) `get` is unified across all three containers with signature `(get container locator) -> :Option<holon::HolonAST>`. HashMap: hash lookup by key, O(1) avg. Vec: direct index by `:usize`, O(1). HashSet: hash membership, returns `(Some x)` on hit, `:None` on miss. Direct lookup through Rust's runtime backings — no "walk," no cosine, no cleanup. The AST describes what the container IS; the runtime materializes the efficient backing (HashMap / Vec / HashSet from std); `get` goes through that backing. (3) `nth` retired — `(get my-vec i)` replaces it. Set's "missing accessor" concern (Hickey round 2) dissolves — HashSet uses the same `get` as the other containers; returns the element on hit for confirmation/canonicalization. FOUNDATION's stdlib section, examples, and inventory updated. 058-016 repurposed for HashMap with rename banner; 058-026 for Vec; 058-027 for HashSet. INDEX updated. | 058 |
| 2026-04-18 | **Capacity is observable; the runtime can guard.** Added a new subsection to Dimensionality ("Capacity is observable; the runtime can guard") that sharpens the "unguarded, the algebra doesn't throw errors" statement from before. The algebra's capacity bound IS physical, and the bound IS observable. Every Holon-producing operation has a local capacity cost = its number of Holon constituents (scalars don't count). `(Bundle (list a b c))` costs 3; `(Bind a b)` costs 2; `(Atom literal)` and `(Permute h k)` cost 1; `(Blend h1 h2 w1 w2)` costs 2; `(Orthogonalize a b)` and `(Resonance a b)` cost 2. ConditionalBind arity/cost deferred pending 058 scrutiny pass (analogous to the Difference/Subtract duplication finding). Once produced, a Holon is singular — it consumes 1 unit when used as input to further operations. Each frame checks independently, like stack frames in traditional programming. The runtime has four modes, set at deployment: `:silent` (research, user accepts degradation), `:warn` (development, log but continue), `:error` (default — catchable CapacityExceeded), `:abort` (production fail-closed). Capacity is exposed as first-class observables: `(frame-cost op)`, `(frame-budget)`, `(frame-fill holon)` — programs reason about their own envelope. Same pattern as Presence is Measurement applied to the substrate's own physics: the machine observes internal state as a scalar; the user's policy decides what to do. Five deployment knobs now: d, capacity-mode, L1, L2, L3. | 058 |
| 2026-04-18 | **Type grammar locked to Rust-surface form; `:Any` and `:Null` removed.** Three related changes landed together as the honest-Rust-correspondence sweep. (1) `:Any` dropped from the grammar. It was an escape hatch ("I refuse to declare a type") that degrades the static-verification story. Every apparent use case has a principled replacement: `:holon::HolonAST` for any algebra value, `:Union<T,U>` for heterogeneous primitives, parametric `T`/`K`/`V` for generics, typed pairs for engram libraries, parametric `eval`. (2) Parametric types adopt Rust-surface syntax as single-token keywords — `:Vec<T>`, `:HashMap<K,V>`, `:Option<T>`, `:Result<T,E>`, `:(T,U)`, `:Union<T,U,V>`, and the function type `:fn(T,U)->R` with parens + arrow (matching Rust's `fn(T, U) -> R` exactly). No parenthesized parametric-application form (`(:List :T)` retired); no internal colons; no internal whitespace. The `:` is Lisp's quote — one at the start, the whole expression is a single keyword token. Tokenizer tracks bracket depth across three pairs — `()`, `[]`, `<>` — and ends the keyword at whitespace or an unmatched closer. (3) `:Null` removed. Rust has no null; wat has no null. Absence is `:Option<T>` (enum with `:None` and `(Some value)` variants); unit is `:()`; structural absence is a form simply not being present. `(Atom null)` removed as a valid atom literal — atoms take string/int/float/bool/keyword only. Sweep applied to FOUNDATION's type grammar section, Atom literal spec, example signatures (Candle, Event, clamp, add-two, demo). Enum declaration for `:wat::std::Option<T>` replaces the earlier Union<Null,T> alias. Companion proposals (058-030, 058-028, 058-029, 058-013, 058-014, 058-016, 058-024, 058-026, 058-027, 058-029, HYPOTHETICAL, RUST-INTERPRETATION) swept in the same pass. | 058 |
| 2026-04-18 | **Presence is Measurement, Not Verdict; `Cleanup` rejected from core.** Load-bearing reframe of the retrieval primitive. The wat algebra has no `Cleanup` primitive. Retrieval is `cosine(encode(target), reference)` compared against the substrate's noise floor (5/sqrt(d), ~0.05 at d=10,000). Presence measurements return `:f64`, not `:bool` — binarization is the caller's decision at the language tier, not the algebra's. Classical Cleanup is a vector-primary-tradition answer to "given a noisy vector, which named thing is this?" — that question presupposes the structure was lost, which never happens in the wat substrate because the AST is always available. Argmax-over-candidates, when an application needs it, is a stdlib fold over presence measurements on (AST, vector) pairs — not a primitive. Engram libraries store `(HolonAST, Vector)` pairs; NN on the vector side returns the AST side; same operation as Cleanup used to name, expressed in terms that already exist. Two retrieval regimes clean-separated: structural (AST walk, exact, for `get`/`nth`) and similarity (presence measurement, fuzzy, for `member?`/engram match). Also added: "Two readings of a holon" — every holon is simultaneously a program (evaluable by the interpreter, Turing-complete, has booleans for eval) and an identity (projected to a vector, measurable by cosine). Filter programs by alignment (vector-side), then eval the selected ones (program-side). Algebra Core goes from 10 to 9 forms. 058-025 Cleanup proposal moves to REJECTED status. Map's `get` and Array's `nth` stay AST walkers per FOUNDATION; Set's missing accessor dissolves (presence measurement is the accessor, returns scalar). | 058 |
| 2026-04-18 | **Type system bundle: Rust primitives + subtype hierarchy + variance rules + `:is-a` for `deftype`.** Resolves Beckman's finding #5 (variance silence) and incorporates several polish decisions. (1) Drop abstract `:Scalar`/`:Int`/`:Bool`/`:Null` in favor of Rust primitives (`:i8`..`:i128`, `:u8`..`:u128`, `:isize`, `:usize`, `:f32`, `:f64`, `:bool`, `:char`, `:&str`, `:String`, `:()`). Honest mapping to Rust; no abstraction layer. (2) Function signatures now use `->` before the return type INSIDE the form: `(define (name [arg : Type] -> :ReturnType) body)` — matches Rust's `fn name(args) -> ReturnType`. No more dangling `: Type` outside the form. (3) Built-in subtype hierarchy stated explicitly: every specific HolonAST node kind `:is-a :holon::HolonAST` (Bundle, Bind, Permute, Thermometer, Blend, Orthogonalize, Resonance, ConditionalBind, Cleanup, and Atom). Rust primitive types have NO built-in subtyping — explicit coercion required, matches Rust. (4) Variance rules: `(:List :T)` covariant in T, `(:Function args... -> return)` contravariant in args and covariant in return. Liskov-safe substitution. (5) `deftype` extended with `:is-a` keyword: `(deftype :MyType :is-a :OtherType)` declares a new type that is a SUBTYPE of the parent — substitutable via is-a. Distinct from `(deftype :MyType :OtherType)` (structural alias — same type) and `(newtype :MyType :OtherType)` (nominal wrapper — distinct, not a subtype). Three semantics, clear naming. (6) `defmacro` uses the SAME signature syntax as `define` and `lambda`: every parameter typed `: AST`, return `-> :AST`. Per the user's correction — omission is easy, not simple; one signature syntax across all three definition forms is simpler than introducing a special implicit-types rule just for macros. Type-correctness of the expansion is enforced by type-checking the expanded form at startup. All 058 sub-proposals swept to use the Rust primitive types and `->` signature syntax. | 058 |
| 2026-04-18 | **FOUNDATION split into load-bearing + VISION.md (Phase 1).** Hickey queue item resolved. FOUNDATION carried speculative/aspirational framings interleaved with load-bearing contracts; reviewers could not cleanly tell what they were being asked to accept. Phase 1 wholesale-moves four clearly-speculative sections into VISION.md: (1) "The Location IS the Program" — metaprogramming framing, query-as-address, semantic-search = exact-lookup; (2) "Reader — Did You Just Prove an Infinity?" — fourth-wall break, holographic reframing, NP-hard framing; (3) "Reader — Are You Starting To See It?" — clouds waking up, distributed cognition substrate; (4) "About How This Got Built" — lineage (Linux + Clojure + MAP VSA), datamancer framing, teachers-who-shaped-the-builder. FOUNDATION preamble updated to declare scope and cross-reference VISION. Content preserved verbatim; nothing lost. Phase 2 (follow-up commit) will do surgical trims of MIXED subsections — VM framing in Recursive Composition, discriminant-guided program synthesis in Programs ARE Holons, self-reference-without-paradox in Vector Side, L3/L4 engram speculation + five-tier hierarchy in Engram Caches, cognitive-architecture framing in Cache Is Working Memory. | 058 |
| 2026-04-18 | **FOUNDATION split (Phase 2) — surgical trims of MIXED subsections.** Six speculative subsections moved from FOUNDATION to VISION: (1) Recursive Composition's "The VM framing" — holon-stack-as-call-stack, Turing-completeness-via-composition. (2) Programs ARE Holons: "Programs generated from learned directions" block, "Kanerva's challenge, fully answered", "What this makes the wat machine" (homoiconic-at-10k-dimensions), "The recursion closes" (self-improvement through program synthesis). (3) Vector Side's "Discriminant-guided program synthesis" + "Self-reference without paradox". (4) Cache Is Working Memory's "The cache is part of the thinking" + "Why this matters for the foundation" (cognitive-architecture framing). (5) Engram Caches's "The engram LRU" + "Prefetching via eigenvalue pre-filter" (L3/L4 implementation speculation). (6) Engram Caches's "The complete memory hierarchy" (five-tier framing) + "Deployment: five knobs now" (includes unbuilt L3 engram knob). Load-bearing claims preserved in FOUNDATION at each cut: engrams-are-holons, cache-is-working-memory (L1/L2 contract, deployment parameters), programs-are-holons, Implications-for-the-algebra, Why-this-matters-for-058. FOUNDATION shrinks 2508 → 2337 lines (−171). VISION grows 332 → 514 lines (+182 for the moved content). Content preserved verbatim; nothing lost. | 058 |
| 2026-04-18 | **Naming Discipline consolidation.** Hickey queue item resolved. The no-namespace-mechanism policy was stated load-bearing in TWO places — `### The three layers, one naming discipline` (inside "Two Cores") and `**About slashes in keyword names.**` (inside "Atom Literal Types") — with slightly different framings that a reviewer had to reconcile. New top-level section `## Naming Discipline — Keyword Paths, No Mechanism` inserted between "Where Each Lives" and "Criterion for Core Forms" as the **single canonical statement** of the policy. It now covers: the no-mechanism claim; the keyword-path convention for all four naming positions (language core, algebra core, stdlib, user); what this is NOT (explicitly: not Clojure `ns`, not Rust `mod use`, not Python `import as`, not any declare-before-use system); collision detection at startup loads (Model A); reserved prefixes `:wat/lang/...`, `:wat::algebra::...`, `:wat::std::...`; type-tagged hashing keeping `(Atom 0)` distinct from `(Atom :pos::0)`; and why this choice (matches hash identity, matches static loading, matches Rust, keeps language surface small). The two former policy sites collapse to one-sentence pointers at the new canonical section. Other scattered usage references (`:alice::math::clamp` in examples, etc.) remain — they demonstrate the policy without restating it. | 058 |
| 2026-04-18 | **Redefinition mode + eval-never-mutates invariant.** Two related additions to "The Algebra Is Immutable." (1) New subsection `### Redefinition mode — opt-in startup knob`: a deployment knob in the same tier as `d`, `capacity-mode`, `L1`, `L2`. `:strict` (default) halts startup on name collision; `:allow-redef` is an opt-in mode where the user explicitly accepts that later startup loads replace earlier ones, with each replacement logged (not silent). The mode is chosen at wat-vm startup, not per-file or per-definition. For authors who intentionally override stdlib behavior during development. (2) New property #4 in Constrained eval: **eval cannot register or replace any definition — ever, regardless of mode.** `define`, `defmacro`, `struct`, `enum`, `newtype`, `typealias`, and `load` forms in a submitted AST are refused by eval. The `redef-mode` knob governs startup; eval has no redefinition surface at all. An attacker who supplies a malicious AST cannot register a new function, replace an existing function, shadow a macro, or add a type — only invoke functions the operator explicitly loaded at startup. "What this gives us" summary updated to state both: the symbol table lifecycle (with mode-dependent collision behavior), and the eval invariant that no symbol-table mutation is ever possible through eval. | 058 |
| 2026-04-18 | **Stdlib location resolved: one file per form.** Datamancer's call on FOUNDATION Open Question #1 — "feels honest." Each stdlib macro (or helper) lives at `wat/std/<Name>.wat`; the keyword path `:wat::std::<Name>` is the file path under `wat/`. No multi-form aggregate file, no manifest of exports to maintain. Cryptographic identity is per-file: signing `wat/std/Subtract.wat` signs exactly that form's body; editing Chain doesn't move Subtract's hash. Growth is additive (new form = new file, no edits elsewhere). `ls wat/std/` IS the stdlib inventory. User code follows the same discipline — `:alice::math::clamp` at `wat/alice/math/clamp.wat`. The Rust wat-vm binary compiles each in via per-file `(load ...)` calls in its startup manifest; cross-project distribution is a tarball of `wat/` directories with signatures per file. "Where Each Lives" updated to list every stdlib form at its own path. Same pattern as 058 itself (one proposal per directory). Matches the keyword-path naming discipline consolidated earlier: file-path and keyword-path are the same identifier. | 058 |
| 2026-04-18 | **Stdlib optimization path dissolved — Q2 resolved.** Datamancer's framing: the AST IS the program; the caches are optimizing form hits to avoid recursive traversal; if we have a form's vec, we just use it. Q2 was asking the wrong question — it imagined "stdlib" as a distinct tier with its own performance envelope, but 058-031 macro expansion dissolves that distinction at parse time. After expansion, a stdlib form's AST and a user-written AST with the same shape are literally the same thing: same hash, same cache entry, same encoding cost. "Optimizing hot stdlib" is not a separate discipline from "optimizing any other hot composed AST" — both are served by L1/L2 cache hits on recurring subtrees (Proposal 057). No Rust-side helper tier. No dual implementation. One source (the wat macro), one walker (the encoder), one cache (L1/L2). If an expression is big, it's big — the program IS the AST. Q2 closed as a false problem. | 058 |
| 2026-04-18 | **Q6 resolved — ship what we know we need, extend when we need more.** Datamancer's call: "we know what tools we want to provide now, not the ones we will want to provide later. If a new thing arrives we can update the language." Q6 asked whether MAP + Thermometer + Blend is the COMPLETE set of scalar primitives — an unanswerable question without knowing every future application. Reframed to a narrower, true claim: these nine forms are what 058 sub-proposals successfully defended against FOUNDATION's criterion; they cover every operation 058 identified. Completeness-in-the-abstract is not the bar. If a future application reveals a primitive that cannot be expressed with existing forms, a new proposal argues it against the criterion (distinct algebraic operation; domain-agnostic; encoder must treat it distinctly) and adds it. The proposal process IS the extension mechanism. Same spirit as stdlib-as-blueprint: ship only what demonstrates a distinct pattern you need today. Q6 closed as answering the wrong question — "is this every form we know we need?" is yes; "is this complete for all time?" is unanswerable and unnecessary. | 058 |
| 2026-04-18 | **The wat-vm Substrate — Kernel Primitives.** New load-bearing section between "The Algebra Is Immutable" and "Dimensionality" — declares what the wat-vm kernel provides, what it deliberately does not, and the lifecycle pattern every wat program follows. Datamancer's framing: "the wat-vm needs to host wat-programs... we need a program to run hello-world successfully... we provide pipes that user programs can interface with... we have the pressure-based drain loop. wat programs follow the wat-vm we've built in the trading lab... start → streams of inputs → consumers → join → end." Kernel primitives: (1) **Queue** — the one program-to-program primitive, bounded(n) or unbounded; fan-out and fan-in are userland programs. (2) **Console** — a built-in program that writes queue messages to stdout/stderr (and reads stdin); hello-world must work, so this is kernel. (3) **Scheduler** — the pressure-based drain loop; no mutex, no locks; backpressure propagates through the program graph naturally. (4) **Program lifecycle** — own-state, pop-handles, run-loop, drain-on-disconnect, join-to-return-state. What is NOT kernel: topic, mailbox (were thin proxies over queues, collapse to programs), cache, database, metrics, logger, arbitrary OS fds (all userland programs). Rust-to-wat-vm mapping stated: `write(1, data)` ↔ `console.send(bytes)`, `pipe(fds)` ↔ `queue`, `fork + exec` ↔ spawn a program, SIGTERM cascade ↔ drop-is-disconnect. Reduction from three communication primitives (queue/topic/mailbox) to one (queue) + console-as-kernel-program is the collapse the book's Chapter 8 documented, now load-bearing in FOUNDATION. Implications: prior sections updated — the startup pipeline runs on the kernel; `eval` lives in a program; L1/L2 cache hierarchy is a userland cache program; Q7 (redef-mode syntax, deferred) is now strictly a userland concern because the kernel does not know about names. | 058 |
| 2026-04-18 | **Caching demoted from architecture to stdlib tooling.** Datamancer's call: "even the caching... that's user dependent... their programs may not want an L2 cache... we should provide the necessary tooling to do caching trivially... our current cache program IS programmable." Section `## The Cache Is Working Memory` renamed to `## Caching Is Memoization — And It Is Userland` and rewritten: the load-bearing claim shrinks to "`encode(ast)` is deterministic, so memoization is sound; the stdlib ships tooling." Two stdlib pieces initially named, then refined to three after a follow-up: "we also need a stdlib for in program caching, not just remote caching over a pipe." The final three: (1) `wat/std/LocalCache.wat` — in-program cache, owned state, direct data access, no pipe/queue overhead; for hot inner loops and per-thread working sets (trading lab's L1). (2) `wat/std/program/Cache.wat` — a cache program, state behind a queue, shared across programs via get/put messages (trading lab's L2). (3) `wat/std/cached-encode.wat` — AST caching functions that work with either cache-handle flavor; pre-packaged memoize-encode pattern. Five application choices: local-only, remote-only, tiered (trading lab), specialized (custom types via LocalCache/Cache), or no cache. The L1/L2 tiering and deployment knobs that were previously stated as FOUNDATION architecture moved to VISION's "The Cache as Cognitive Substrate — One Application's Story" — those are the trading lab's choices for its specific cognitive pace, not universal contracts. FOUNDATION's deployment knobs tighten to `d` and `capacity-mode` (which ARE universal); L1/L2 sizing is application-level. Q7 substrate-section cross-reference updated; "Implications for prior sections" updated. | 058 |
| 2026-04-18 | **Stdlib directory split — `wat/std/` for macros/functions/data-structures, `wat/std/program/` for programs.** Datamancer caught the dishonesty: "`wat/std/Cache.wat` — dishonest name ... `wat/std/program/Cache.wat`." The bare `wat/std/` path suggests a macro or simple function, but the cache program is a spawnable wat-vm program with lifecycle, owned state behind a queue boundary. Moving programs under `wat/std/program/` reflects the substrate distinction: things that COMPILE into AST or run as function calls live in `wat/std/`; things that RUN as independent wat-vm programs live in `wat/std/program/`. The convention is purely directory organization, not a parser mechanism — keyword path still equals file path (`:wat::std::program::Cache` ↔ `wat/std/program/Cache.wat`). Users get the same convention for their own code: `wat/alice/math/clamp.wat` for a function, `wat/alice/program/ClampServer.wat` for a program. The `program/` segment is an honest name — it tells the reader what kind of artifact is at that path. "Where Each Lives" updated; Caching section references updated. Same discipline as the Linux `bin/` vs `lib/` split: path segment = artifact kind. | 058 |
| 2026-04-18 | **Kernel primitives expressed in wat syntax.** Datamancer's question: "how do we express the creation of pipes/queues?... same questions for threaded programs." New subsection `### Kernel primitives in wat syntax` inside `## The wat-vm Substrate`. Six lowercase kernel functions under the `:wat::kernel::...` prefix: `make-bounded-queue`, `make-unbounded-queue`, `send`, `recv`, `try-recv`, `drop`, `spawn`, `join`, plus console handles `console-out`, `console-err`, `console-in`. All lowercase because they EXECUTE at wat runtime (per the two-tier distinction — UpperCase builds ASTs, lowercase runs Rust). Keyword-path discipline applied: kernel primitives are not host-Lisp inherited; they are wat-vm specific, so they carry keyword paths. No separate `spawn-thread` vs `spawn-program` — a program is just a function that runs on its own thread via `spawn`; same Rust primitive underneath. Hello-world and observer-style lifecycle examples included to show the full pattern (make-queue → spawn → send/recv loop → drop-on-shutdown → join for state). Six primitives enough to express any wat-vm program graph. | 058 |
| 2026-04-18 | **`main` as reserved bare-name entry point + Q7 resolved by analogy.** Datamancer caught the hello-world example's dishonesty: `(:my::app::main -> :())` was prefixed, but main shouldn't need a namespace. The kernel's entry point is a convention: the user writes `(define (main -> :()) ...)` and the wat-vm invokes it at startup. One `main` per program (collision halts); zero `main` also halts. `main` is a reserved bare name — a narrow exception to keyword-path discipline; the exception is justified because there is exactly one entry point per program and the kernel reserves the name. This resolves Q7 (`:allow-redef` expression syntax) by analogy: the user doesn't declare a special syntax for "this redefines an existing name"; they just USE the name. If `redef-mode=:allow-redef` is set at wat-vm startup, later-loaded definitions with colliding names replace earlier ones, and each replacement is logged. No per-file pragma, no `(redefines ...)` form, no manifest directive — system-wide opt-in via flag + declaration simply by name re-use. Same shape as `main`: reserved/conventional at the kernel level, bare-name at the user level. Hello-world example and observer-startup example updated to use bare `main`. Q7 marked RESOLVED. | 058 |
| 2026-04-18 | **Bare aliases — dual-tier symbol table for kernel/algebra/language-core/stdlib forms.** Datamancer's explicit statement: "(make-bounded-queue ...) IS an alias for (:wat::kernel::make-bounded-queue ...). We inject our core and std into the symbol table... users can steal those names if they want... the :wat::kernel:: is protected as are the other wat-provided forms." Follow-up: "this shadowing... is in the env.. too.. (let ((recv \"bad-idea\")) recv) in that scope... recv is redefined." New subsections in `## Naming Discipline — Keyword Paths, No Mechanism`: `### Reserved prefixes — protected` (expanded to name the four protected prefixes: `:wat/lang/`, `:wat::kernel::`, `:wat::algebra::`, `:wat::std::` — all refuse user `define`/`defmacro` at startup), `### Bare aliases — convenience without sacrifice` (every kernel/algebra/language-core/stdlib form is registered under BOTH its full keyword path AND its bare name; full path is protected, bare alias is shadowable; user `(define (Subtract ...) ...)` shadows the bare alias in their code without touching `:wat::std::Subtract` which remains reachable via the full path), `### Shadowing also works at lexical scope` (any inner binding — `let`, `lambda` parameter, `match` pattern — shadows an outer name including bare kernel aliases; the full `:wat/...` path remains reachable regardless; same pattern Clojure uses). `### How this relates to :allow-redef` clarifies: `:wat/...` paths are protected in both `:strict` and `:allow-redef`; bare-alias shadowing is not a collision but the designed resolution behavior. `main` reframed under the model: unlike other `:wat/...` names (protected path + shadowable alias), `main` is bare-only — the kernel reserves the bare name as an entry-point slot that the user fills. Examples added to show the dual tier plus lexical shadowing. Hash identity is on the full path always; bare aliases are call-site convenience, not identity claims. This is the Clojure `clojure.core/map` vs `map` pattern adapted to wat's keyword-path discipline, plus ordinary lexical scoping of the Lisp host. | 058 |
| 2026-04-18 | **Changelog extracted to its own file.** Datamancer's call: "i think the foundation's change log needs its own file at this point." The revision-history table had grown past 60 entries (~200 lines of dense text) and was the largest single section of FOUNDATION.md. Moved to `FOUNDATION-CHANGELOG.md` alongside FOUNDATION; FOUNDATION's `## Revision History` section shrinks to a one-paragraph pointer directing readers to the changelog. New entries append to the changelog; FOUNDATION stays focused on the load-bearing architecture. Same separation already used for VISION (speculative framings) and CORE-AUDIT (primitive affirmations) — each document's scope is its own concern. INDEX.md updated to list FOUNDATION-CHANGELOG.md alongside the other foundational documents. | 058 |
| 2026-04-18 | **Entry point is `:user::main`, not bare `main`.** Datamancer's correction: "main... :user::main... that's the entry point — deal with it... not bare... it is descriptive." The prior "main is a reserved bare name" framing was a narrow exception to the keyword-path naming discipline, and exceptions are complections. The honest path: `:user::main` is a keyword-path name under the `:user::...` convention — the inverse of `:wat/...` paths. `:wat/...` paths are kernel-PROVIDED (and protected from redefinition); `:user::...` paths are kernel-REQUIRED slots that the USER provides. The kernel looks up `:user::main` at startup and invokes it. One `:user::main` per program (collision halts); zero halts (program needs an entry point). Hypothetical future kernel slots would follow the same pattern: `:user::shutdown-handler`, `:user::on-signal`, etc. — kernel-required names, user provides implementations. Hello-world, observer-startup, and Q7 resolution text all updated. No bare-name exception to the naming discipline; everything is keyword-path. | 058 |
| 2026-04-18 | **Parametric polymorphism as substrate — programs ARE atoms, which demands it. 058-001 Atom ACCEPTED as `:Atom<T>`; 058-030 Q1 YES; 058-032 Q1/Q4 YES.** This is the substrate decision, not a sub-proposal one. Datamancer 2026-04-18: *"this is part of our substrate... if we can't host programs as atoms we're not doing it honest."* FOUNDATION's "Programs ARE Holons" principle says every wat program is a holon AST. But without parametric Atom, programs cannot be **atomized** — cannot be given opaque-identity vectors, cannot be stored in libraries keyed by their own content hash, cannot be compared via cosine to other programs, cannot be Bound to metadata. Accepting parametric Atom makes the programs-as-values principle operationally real. **The commit.** `(:wat::algebra::Atom x)` accepts any T that has canonical serialization — primitive (str/int/float/bool/keyword), composite `:holon::HolonAST` (Bundle, Bind, any AST node), or user-defined type (struct, enum, newtype). Hash is `hash(type-tag-of-T, canonical-EDN(x))` → seeded-random vector in `{-1, 0, +1}^d`. The type-tag preserves identity across types with coinciding bytes. Extraction via `(:wat::std::atom-value atom)` returns the inner value typed as T — polymorphic function, type-checker infers T at call site. **What this requires in the type system.** Parametric polymorphism across the board. (1) **Parametric user types** — `struct`, `enum`, `newtype`, `typealias` accept type parameters: `(:wat::core::newtype :my::Wrapper<T> (inner :T))`, `(:wat::core::enum :my::Tree<T> ...)`. (2) **Parametric polymorphism for functions** — functions declare type variables bound at signature scope: `(define (:unwrap (w :Wrapper<T>) -> :T) ...)`. Type-checker uses Hindley-Milner-ish unification at call sites; no annotation burden at the call. (3) **Parametric macros** — 058-032 Q1/Q4 resolve: `(defmacro (id (x :AST<T>) -> :AST<T>) \`,x)` is legal; T is a type variable the macro closure carries. **What this doesn't commit to.** Higher-kinded types, type bounds (`T : Trait`), existentials, polymorphic recursion — all deferred. Rank-1 parametric polymorphism is the commit; more sophisticated type-system features arrive with concrete application demand. **The power this unlocks for 058 itself.** Engram libraries of programs: `(HashMap :pattern-1 (Atom prog-1) :pattern-2 (Atom prog-2))` — learned population keyed by identity. Program similarity: `(cosine (Atom prog-a) (Atom prog-b))` — cosine = 1 for identical, ≈0 for different. Program populations: `(Bundle list-of-atomized-programs)` — superposition of a learned program set. Binding programs to outcomes: `(Bind (Atom prog) (Atom :outcome::success))`. All operational consequences of parametric Atom + parametric polymorphism. Without them, the algebra is talking about "programs are holons" but cannot atomize them — theoretical without operational grounding. **Two encodings of any composite — both legitimate.** Direct encode of `b = (Bundle [x y z])` produces the structural vector (children composed, recoverable via unbind). Atomized wrap `(Atom b)` produces the opaque-identity vector (EDN-hashed, seeded-random, structure not recoverable from vector). Direct when you care about structure; atomized when you care about identity. Applications choose per use case. **Scope of the sweep across 058.** 058-001/PROPOSAL.md gets ACCEPTED-with-parametric banner describing `Atom<T>` and `atom-value` polymorphic. 058-030's Q1 "Generics scope" resolution rewritten from "start minimal" to YES across parametric user types and functions. 058-032's Q1/Q4 resolution changed from "deferred / out of scope" to YES (parametric macros). FOUNDATION gains a "Programs ARE Atoms — the substrate corollary" subsection under "Programs ARE Holons" section, showing atomized-program examples. OPEN-QUESTIONS Atom section collapses to ACCEPTED pointer; Live Questions block drops 058-001 entries. INDEX reading-order and table updated. **Live designer questions drop from 5 to 3** — only 058-005 Orthogonalize remains (Q1 core vs widened Blend, Q2 Project companion, Q3 naming Orthogonalize vs Reject). **Implementation complexity.** holon-rs gains a type-checker pass (rank-1 HM unification; well-understood, bounded polynomial in program size). The AtomLiteral enum either expands to include a Holon variant and a user-type variant (simple, explicit), or uses a trait-object payload (`Arc<dyn Serializable>`, flexible). Likely both at different layers. Wat-vm-at-runtime gets parametric type instantiation at startup under Model A static loading. Every instantiated parametric type and function has a unique content hash. No runtime polymorphism dispatch needed; all types resolved at startup. This is a meaningful expansion of implementation scope, but it is the substrate the algebra requires to be honest about programs-as-values. | 058 |
| 2026-04-18 | **058-014 Analogy DEFERRED — new status between ACCEPTED and REJECTED.** Datamancer's framing: *"I think I proved it works, but I don't know if we're gonna use it in the trading-lab. I think it can live here as a point that could be resumed from."* Analogy occupies a third category that neither the strict-production-use bar (which would reject) nor the classical-VSA-heritage bar (which would accept) handles cleanly. **What makes it different from the rejected forms:** Analogy is proven working — the classical Kanerva / Plate `C + (B − A)` operation produces the right geometric relationship on the unit sphere; not speculative math. Classical VSA heritage documented across Plate, Kanerva, Eliasmith; primer (`series-001-002-holon-ops.md` lines 159-170) describes it with a hypothetical cross-domain rule transfer example. Clean abstraction, not a magic-weight or half-form. **What makes it not ACCEPTED:** no application in this workspace currently uses Analogy. Trading lab doesn't. DDoS lab doesn't. Challenge batches "explored" it but cited no measurement. If ACCEPTED, we ship a stdlib name nobody calls today. **The DEFERRED status:** not shipping in 058. Proposal preserved intact as a **resumable audit record** — when an application demands it, a future proposal (058-014-R2 or fresh number) picks back up from this state with citation. The algebra decision doesn't need re-arguing; only the earn-stdlib-with-evidence part does. Users who want the operation inline write `(:wat::algebra::Bundle (:wat::core::vec c (:wat::std::Subtract b a)))` — or define their own `:my::app::Analogy` in their namespace. **Not a new grammar category** — FOUNDATION's criteria still talk about CORE, STDLIB, REJECTED. DEFERRED is a status on proposals that remain in the 058 batch but don't ship. Status discipline: ACCEPTED = shipping, REJECTED = won't ship (with rationale), DEFERRED = proven-working-but-unused-today (with resume path). **Sweep:** 058-014/PROPOSAL.md gets DEFERRED banner above its preserved content; INDEX status column reads DEFERRED; OPEN-QUESTIONS 058-014 section collapses to DEFERRED pointer (Q1/Q3 resolved earlier, Q2/Q4/Q5 moot under deferral — no live questions); FOUNDATION stdlib inventory removes Analogy from the macro list (Sequential/Ngram/Bigram/Trigram stay; Amplify/Subtract/Log/Circular stay; HashMap/Vec/HashSet stay); FOUNDATION's "Relational transfer" section converted to a DEFERRED note with the inline form. **Designer question count unchanged at 5** — Analogy had no live questions in the Round-3 block. | 058 |
| 2026-04-18 | **Sequence-encoding family settled: Sequential reframed (bind-chain), Ngram reframed, Bigram and Trigram added, Chain rejected.** Datamancer's reframe traced a real divergence between the proposal set and production. Sequential (058-009) was proposed with a **bundle-sum** expansion — `(Bundle (map (λ (t, i) → (Permute t i)) xs))` — the classical Kanerva pattern. The primer's "positional list encoder" uses `bind(permute(item, position), role)` — **bind-chain**, not bundle-sum. The trading lab's `src/encoding/rhythm.rs:91-99` hand-rolls `Bind(Bind(fact_0, Permute(fact_1, 1)), Permute(fact_2, 2))` — bind-chain, matching the primer. The 058 proposal diverged from both. **Reframe: Sequential is bind-chain.** `(:wat::std::Sequential [a b c]) = Bind(Bind(a, Permute(b, 1)), Permute(c, 2))` — compound vector with exact sequence identity. Not superposition. The math is a left-fold over (item, index) pairs. Datamancer 2026-04-18: *"I expect we will use this in the trading-lab — we just didn't have a useful tool yet."* Production-adoption commitment. **Ngram (058-013) reframed to use new Sequential.** Expansion `(Ngram n xs) = (Bundle (map Sequential (window n xs)))` now produces bind-chain compounds per window. Matches production. **Bigram and Trigram added as named stdlib shortcuts.** Stdlib-as-blueprint logic: demonstrate the pattern once (Ngram), ship named conveniences for the common cases (Bigram for n=2, Trigram for n=3), hand extension to users via their own namespace (`:my::app::Pentagram = (Ngram 5 xs)`). Datamancer: *"a user once wants a 5-gram can call (that-thing 5) and give it name."* This IS stdlib as blueprint. **Chain (058-012) REJECTED.** Chain = `Bundle(pairwise-Sequential pairs)` = `Ngram 2 xs` = `Bigram xs`. Redundant with Bigram under the new family. Keep the clearer name. **Linear comparison dissolved.** Unlike Linear (REJECTED because redundant with Thermometer under the new 3-arity signature), Sequential/Ngram/Bigram/Trigram all demonstrate a distinct pattern (bind-chain positional encoding + sliding-window family). They earn stdlib by producing a specific, production-grounded shape no single primitive expresses. **Net algebra stdlib count:** −1 (Chain) +2 (Bigram, Trigram) +0 (Sequential, Ngram reframed in place). Running total stays around 15-16 forms pending further scrutiny (Analogy still queued). **Sweep.** 058-009/PROPOSAL.md gets ACCEPTED-with-reframe banner describing bind-chain expansion; 058-012/PROPOSAL.md gets REJECTED banner citing Bigram redundancy; 058-013/PROPOSAL.md gets ACCEPTED-with-reframe-and-shortcuts banner shipping all three macros (Ngram/Bigram/Trigram); OPEN-QUESTIONS sections collapse; INDEX status column and reading-order entries updated; FOUNDATION stdlib inventory updated with Sequential/Ngram/Bigram/Trigram listing. | 058 |
| 2026-04-18 | **058-017 Log and 058-018 Circular ACCEPTED as stdlib — concrete production evidence in the trading lab.** Datamancer: "log and circular are unquestioning — we've been using circular for hour-of-day, day-of-week, day-of-month, month-of-year and more; log we use extensively in the current src/." Both confirmed by source audit. **Log** has 15+ concrete uses across the trading-lab vocab: `vocab/market/oscillators.rs` (ROC-1/3/6/12 rate-of-change indicators), `vocab/market/momentum.rs` (atr-ratio), `vocab/market/keltner.rs` (bb-width), `vocab/market/price_action.rs` (range-ratio, consecutive-up/down), `vocab/exit/regime.rs` (variance-ratio), `vocab/exit/trade_atoms.rs` (exit-excursion, exit-age, exit-peak-age, exit-trail-distance, exit-stop-distance, exit-r-multiple, phases-since-entry, phases-survived). Every ratio, rate, and multiplicative quantity passes through Log — scale-invariant encoding of "how much X per Y." **Circular** encodes every cyclic time component in `vocab/shared/time.rs` — all five leaf binds (minute/hour/day-of-week/day-of-month/month-of-year periods 60/24/7/31/12) plus three pairwise compositions (minute×hour, hour×day-of-week, day-of-week×month). Every broker thought carries `time_facts(candle)` — load-bearing for temporal pattern recognition. Circular also proves Blend's Option B acceptance — `cos(θ)+sin(θ)≠1` at θ=π/4 is exactly what Option A cannot express. **Questions resolved in both.** Log Q1 (log availability) → `:wat::std::math::ln`; Q2 (numerical preconditions) → user responsibility, trading-lab callers use `.max(0.0001)` guards; Q3 (base) → natural log, base cancels in ratio; Q4 (consistency) → 058-031 defmacro; Q5 (Log family alternatives) → deferred. Circular Q1 (sin/cos/pi availability) → `:wat::std::math::*`; Q2 (scale shape) → per-encoder shape is honest (period for cyclic vs min/max for monotone); Q3 (Blend Option B) → confirmed by 058-002 acceptance; Q4 (angle convention) → radians CCW from 0, θ=2π·value/period; Q5 (starting offset) → userland; Q6 (consistency) → 058-031; Q7 (new circular encoders) → deferred. Both PROPOSAL.md files get ACCEPTED banners with per-question resolution; OPEN-QUESTIONS 058-017 and 058-018 sections collapse to ACCEPTED pointers; INDEX status column reads ACCEPTED. Neither was in the Live Questions block — designer question count remains at 5. | 058 |
| 2026-04-18 | **058-020 Flip REJECTED — primer-collision plus magic-weight speculation.** Datamancer flagged Hickey's Round-1 callout on the `-2` weight and asked whether Flip is used for anything immediate. Three converging reasons for rejection. **(1) Primer collision.** The primer's `flip(vec) = -vec` is **single-argument elementwise negation** (`similarity(vec, flip(vec)) ≈ -1.0`, classical VSA "anti-vector"). The proposal's `Flip(x, y)` is a **2-argument gated inversion** that expands to `(:wat::algebra::Blend x y 1 -2)`. Two distinct operations wearing the same name — reader-hostile. **(2) Magic weight, weak defense.** The proposal claimed `-2` is the minimum inversion weight, but actually any weight `w < -1` produces the same thresholded result (`-1.01`, `-1.5`, `-2`, `-10` all work). `-2` is the smallest convenient integer, not an algebraic minimum. Hickey's "that's a magic number" callout stands unrefuted. **(3) No cited production use.** The proposal's rationale — "adversarial inversion, counter-signaling, trust inversion" — is abstract; no challenge batch citation, no DDoS lab citation, no trading lab citation. Same speculative-primitive pattern as Resonance (058-006), ConditionalBind (058-007). **What users who need this write instead:** `(:wat::algebra::Blend x y 1 -2)` inline with a comment. No stdlib name for a pattern nobody has used. **What this doesn't affect:** Blend (058-002) stays ACCEPTED — Flip was one of several downstream users; the others (Amplify, Subtract, Circular) all have documented applications. **What preserved for later:** if the primer's single-arg negation ever needs a home, that is a different proposal — `(:wat::std::Negate v)` taking ONE argument — when a real application motivates it. **Sweep.** 058-020/PROPOSAL.md gets REJECTED banner; FOUNDATION stdlib listing drops Flip (17 → 16 forms); Orthogonalize-replaces-Negate note updated; Blend ACCEPTED note updated to name only Circular/Amplify/Subtract as downstream Blend macros; INDEX sub-proposal entry/table/dependency graph updated; OPEN-QUESTIONS 058-020 section collapses to REJECTED pointer; cross-cutting theme entries marked moot. Live designer question count unchanged at 5 (Flip's 6 questions weren't in the live block). | 058 |
| 2026-04-18 | **058-002 Blend keystone closed — ACCEPTED as Option B, negative weights allowed, binary arity.** Datamancer closed the keystone on the arguments in 058-002's proposal plus a sharpening of Option A's complection. All five questions resolved: **Q1** (distinct category) YES — Bundle is MAP monoid Add, Blend is parameterized binary combination, different categorical object, passes FOUNDATION's CORE criterion unambiguously (no existing primitive performs scalar·vector before combining). **Q2** (Option A vs B) — Option B. The counter-intuitive reading landed: Option A's single alpha has less syntactic surface but more entangled semantics — the convexity constraint `(alpha + (1-alpha) = 1)` is a complection hiding in plain sight. Option B's two independent weights are unbraided, each stands alone. By Hickey's "simple = unentangled concepts" definition, Option B is simpler despite more parameters. Also load-bearing: Option A cannot express Circular (`cos(θ) + sin(θ) ≠ 1`); Option B lets Circular expand to a stdlib macro. Smaller core, fuller coverage — strictly dominant. **Q3** (negative weights) YES — follows from Option B; the operation is `threshold(w1·a + w2·b)` regardless of sign; `Blend(x, y, 1, -1) = Subtract`. Semantic objection ("blurs superposition intuition") lands in naming tier, not algebra tier — stdlib names (Subtract, Amplify, Flip) carry intent at the call site. **Q4** (variadic) NO — stay binary. Generalizing to variadic-weighted-sum would dissolve MAP's canonical set (Bundle-as-Add would become a special case of Blend-with-all-weights-1). Different categorical objects, coexist. If future work demands variadic weighted sum, propose separately. **Q5** (impl impact) trivial — ~20 lines of Rust; new `blend_weighted`; existing `blend` becomes a thin wrapper; cache keys include f64 weights (same pattern as Thermometer's value/min/max). **Q6** moot under Q1/Q2 acceptance. **Downstream cascade unblocked:** 058-015 Amplify, 058-018 Circular, 058-019 Subtract, 058-020 Flip all become mechanical stdlib macros over Blend. 058-005 Orthogonalize stays core (computed-coefficient projection removal cannot be expressed as Blend because the coefficient depends on a dot product of the inputs). **Live designer questions drop from 10 to 5** — only 058-001 Atom (Q1/Q2) and 058-005 Orthogonalize (Q1/Q2/Q3) remain for Round 3. Designer review may still reopen Blend; the closure is our best-argued reading, not a finalization they can't revisit. Updates: 058-002/PROPOSAL.md gets ACCEPTED banner + per-question resolution; OPEN-QUESTIONS Live Questions block drops Blend; 058-002 section collapses to ACCEPTED pointer; "Theme: Dependency on Blend" marked RESOLVED with all six dependents crossed out; FOUNDATION's "Blend is pivotal" line becomes "Blend is ACCEPTED"; INDEX proposal table status changes from PIVOTAL NEW to ACCEPTED. | 058 |
| 2026-04-18 | **058-006 Resonance and 058-007 ConditionalBind REJECTED — algebra core shrinks from 9 to 7 forms.** Both primitives appeared in the Holon Python library (`series-001-002-holon-ops.md` primer documents them) but neither has a cited application in any challenge batch (001–018), the DDoS lab, or the trading lab. Datamancer's review: "we haven't used these in anything but unit tests and small demos to prove the code did something." **Resonance** (sign-agreement mask, `Resonance(x, y)[i] = x[i]` if signs agree, else 0) has no citation in the primer — the only two operations in the primer's "Pattern Extraction" / "Extended Algebra" sections without cited applications are Resonance and ConditionalBind. The Round-2 Q2 revealed it's at the wrong level: `Mask(x, boolean-vector)` is the more general primitive; Resonance would be a stdlib idiom over it. Also expressible as a three-primitive composition: `threshold(Bind(x, y), +1)` gives the agreement mask; multiplying applies it. No core addition needed. **ConditionalBind** (per-dim gated bind) has a 4-mode API (`"positive"`/`"negative"`/`"nonzero"`/`"strong"`) — unmistakable fingerprint of exploratory API exploration, not a settled primitive. Round-2 Q3 revealed it's half an abstraction: it consumes a gate vector without proposing a gate-producer; and the classical VSA way to update a role — `Bundle(Subtract(person, Bind(old_age, role_age)), Bind(new_age, role_age))` — uses only existing primitives. Both rejected as **speculative**. Algebra core inventory post-rejection: Atom, Bind, Bundle, Blend, Permute, Thermometer, Orthogonalize — seven forms. FOUNDATION updated: enum listings, capacity-cost table, Output Space table, algebra-core section headers, Complete Forms listing, Summary section — all converged to 7. 058-006/PROPOSAL.md and 058-007/PROPOSAL.md carry REJECTED banners naming the rationale; historical content preserved below each banner as audit record. INDEX.md, CORE-AUDIT.md, OPEN-QUESTIONS.md cross-references updated. OPEN-QUESTIONS "Live questions for Round 3" summary loses Q2/Q3 entries; designer question count drops from 5 sub-proposals to 3 (Blend, Atom, Orthogonalize). If future application demands either primitive, re-propose with concrete evidence — the refined Mask/Select primitives, or the original Resonance/ConditionalBind with citation. | 058 |
| 2026-04-18 | **Core/stdlib division line named: Rust-direct correspondence. List primitives placed.** The long-ambiguous list-combinator placement resolves into a principle that extends beyond lists. Datamancer's framing: "we just found our division line between core and std." **The rule:** if an operation maps to a single method on Rust's `Iterator` / `Vec` / `&[T]` (or equivalent Rust std facility), it belongs at `:wat::core::`. If it is a short composition of such methods, it belongs at `:wat::std::...`. If it is app-shaped or domain-specific, it is userland. This extends to future primitive proposals — every new form specifies its Rust correspondence and thereby names its tier. **List primitives landed.** `:wat::core::` now explicitly names `list`, `cons`, `first`, `second`, `third`, `rest`, `map`, `for-each`, `filter`, `fold`, `foldl`, `foldr`, `reduce`, `length`, `reverse`, `range`, `take`, `drop`, `empty?` — each directly mapped to a single Rust iterator/slice method. `:wat::std::list::` ships the short compositions: `pairwise-map` (`xs.windows(2).map(f)`), `n-wise-map` (`xs.windows(n).map(f)`), `map-with-index` (`xs.iter().enumerate().map(f)`), `window`, `zip`, `unzip`, `take-while`, `drop-while`. Stdlib algebra macros (Chain, Ngram, Sequential, Analogy) emit calls to these stdlib combinators by keyword path; resolution happens at load time. **No `null?`.** Rust has no null; wat follows. Absence is `:Option<T>::None`; emptiness is `(:wat::core::empty? xs)`. The Scheme-ism `null?` is dropped. **`encode-window` is userland.** The Bundle-with-Permute-by-index pattern is specific to asset-tracking vocab in the trading lab; it lives at its app's path (e.g., `:project::trading::engram/encode-window`), not in `:wat::std::`. Not generic enough for stdlib — domain-shaped. **Also added `:wat::std::math::`** to the reserved-prefixes note, pre-claiming the namespace for `cos`, `sin`, `pi`, `log`, `ln` (used inside Circular/Log macro expansions). Math primitives are single-method Rust calls but live at `:wat::std::` because they are not Lisp primitives and not algebra primitives — they are numeric utilities that only stdlib macros currently need. OPEN-QUESTIONS cross-cutting theme "List combinators" marked RESOLVED; bareword follow-up for sub-proposals (Chain/Ngram/Sequential/Analogy still reference bare `pairwise-map` etc. in their macro bodies) stays pending as post-Round-3 mechanical cleanup. | 058 |
| 2026-04-18 | **Load-form unification: `:wat::core::load!` is the one loader; config setters are entry-file-only, before any load.** Two datamancer corrections landed. **(A) `load-types!` and `load-macros!` dissolved — `:wat::core::load!` handles everything.** Earlier drafts split loading by kind: `load` for functions, `load-types` for types, a speculative `load-macros` for macros. User: "load-types is not needed at this point — `:wat::core::load!` will handle all expressions now... same for load-macros... the expressions we load are eval'd before we write the rust." One form, one verb. A loaded file may mix types, functions, macros, and nested `load!` calls freely — the loader fetches the file and the file's own toplevel forms determine what startup phase registers each declaration. All `load-types!` occurrences renamed to `load!` in FOUNDATION (23 refs); prose rewritten in the "Startup loading" section to name a SINGLE loader handling all kinds; "Complete forms" reserved-form list collapses from 8 to 7 (no separate type-loader); sub-proposals (058-028, 058-030, 058-031) prose rewritten to remove the two-loader narrative and purge the `load-macros "path"` speculation that never landed as an actual form. **(B) Config setters are entry-file-only, before any `load!`.** User: "we can't tolerate the config setters via load... the user's entry point must declare them before a load happens." The entry file (the wat file the wat-vm starts from) has an enforced two-part shape: Part 1 = all `(:wat::config::set-*!)` setters; Part 2 = all `(:wat::core::load!)` calls. Parser enforces ordering — a `load!` before a setter halts parse with "config setter follows load!; setters must come first"; any `set-*!` encountered in a LOADED file halts parse with "config setter in non-entry file; setters belong in the entry file only." Config is a root-file responsibility; no hiding a load-bearing deployment choice inside a transitive dependency. Startup pipeline reordered accordingly: parse entry file → shape check → config pass → recursive load parse → macro expansion → resolve → type-check → hash/sign/verify → freeze → :user::main. By the time any loaded file is parsed, `:wat::config` is already populated (macros expanding in loaded files can read it). Also landed: OPEN-QUESTIONS updated with a new "Remaining bareword sweep follow-ups" cross-cutting theme, naming the post-Round-3 mechanical cleanup for math primitives → `:wat::std::math::*`, substrate accessors derived from dims → typed properties on `:wat::config::*` (e.g., `(:wat::config::noise-floor)`), algebra substrate operations → `:wat::algebra::...`, engram library ops as app-space, and list combinators deliberating. | 058 |
| 2026-04-18 | **`:wat::config` ambient startup constants; `load` → `load!` bang convention; `argmax` purged.** Three changes in a single beat reshaping how a wat program opens. **(1) `:wat::config` — kernel-owned struct of ambient startup constants.** A program that depends on a value pervasively (vector dimension, capacity-overflow policy) declares it once at toplevel via a banged setter; every function in the program reads it via a typed getter without threading it through signatures. Struct, not HashMap — each field has a typed accessor and a once-only setter. Rules: toplevel-only; exactly-once per field across all loaded source; type-checked; required (no defaults — the program author must commit explicitly or startup halts). Initial fields: `:wat::config::dims` (`:usize`) and `:wat::config::capacity-mode` (`:wat::config::CapacityMode` enum — `:silent`/`:warn`/`:error`/`:abort`). The struct grows by FOUNDATION proposal; the bar is "universal to every holon program, not app-specific" (L1/L2 cache sizing failed the bar and stays in VISION's trading-lab story). Startup pipeline gains a config pass between parse and macro-expand so macros can read committed values. **(2) `load` → `load!` bang convention.** Same bang discipline as `set-dims!`: the bang signals "this form writes to the ambient startup state; irreversible; observable in the committed program image." Two toplevel forms carry bangs in the current language and are the only ones on the near horizon: `(:wat::config::set-<field>! value)` and `(:wat::core::load! path)`. Everything else (define, defmacro, lambda, let, algebra operations, user functions) is pure declaration or pure value. Also banged: `(:wat::core::load-types!)`. Renamed 23 occurrences across FOUNDATION. **(3) `argmax` purged — selection is the caller's policy.** The old Cleanup-rejection subsection still used `argmax` in its "this is how you'd do it without Cleanup" examples, framing selection as a stdlib fold. That framing was wrong by one layer: the algebra returns a LIST of (candidate, presence-score) pairs; picking a winner is the CALLER's policy, not a substrate operation. Four code blocks rewritten (FOUNDATION.md lines ~1620 and ~1644, 058-014-analogy lines 56 and 106) to return `:Vec<Pair<holon::HolonAST,f64>>` and let the caller decide: top-1 by max-by-score fold, above-threshold filter using `5/sqrt(d)` noise floor, top-k sort-and-take, weighted-mixture Bundle — four different policies, four different caller expressions, same substrate. The prose around it now names `argmax` as "not an algebra operation" and refers policy to the caller. Sample wat-program toplevel sketch added showing set-dims! + set-capacity-mode! + load!-chain above `:user::main`; reserved-prefix list gains `:wat::config::...`. | 058 |
| 2026-04-18 | **Bareword sweep across all 058 docs (140+ scheme blocks); HandlePool promoted to kernel; Signal enum + signals queue added; Programs-are-userland-with-two-exceptions principle codified.** Three interlocking changes landed together: **(1) Bareword sweep completed.** A subagent swept every scheme block in FOUNDATION.md (~50), all 28 sub-proposal PROPOSAL.md files, VISION.md, and OPEN-QUESTIONS.md. ~140+ blocks converted to full-path discipline. Deliberately left untouched: FOUNDATION-CHANGELOG.md (historical), reviews/ (archived), HYPOTHETICAL-CANDLE-DESCRIBERS.wat (not .md), WAT-TO-RUST.md (already compliant). Ambiguities the sweep flagged for human follow-up: lowercase-tier Rust primitives (atom/bind/bundle/cosine/blend) are intentionally bare by FOUNDATION's two-tier design and stay; substrate-level accessors (cosine-sim, dot, noise-floor, frame-cost, frame-budget, frame-fill, argmax, match-library, library-add!) don't yet have keyword paths assigned and are left bare with `;; path TBD` comments; math primitives (cos, sin, pi, log, ln) left bare pending `:wat::core::math::*` vs `:wat::std::math::*` decision; list combinators (map-with-index, pairwise-map, n-wise-split, take, range, reverse, encode-window) partially swept and need a consistency pass; `def`/`defn` shorthand rewritten as `:wat::core::define` under the standard interpretation; `set!` in hygiene examples left bare; `Fn`/`If`/`Mask` in speculative discussion blocks left bare (not in the algebra). **(2) HandlePool promoted from `:wat::std::` to `:wat::kernel::`.** The deadlock guard lives next to make-bounded-queue / spawn / select, not among stdlib patterns. Rationale: it's infrastructure, not demonstration. Every mailbox-backed driver relies on its claim-or-panic invariant. 8 occurrences renamed in FOUNDATION; "Where Each Lives" updated to show HandlePool NOT in std/ and to note its kernel placement explicitly. **(3) `:wat::kernel::Signal` enum + signals queue added; `:user::main` now has four parameters.** The kernel installs OS signal handlers for :SIGINT and :SIGTERM at startup and forwards deliveries to the signals queue it passes to `:user::main`. Programs that want graceful shutdown select across input queues AND the signals receiver; on signal, drop root producers, cascade propagates. SIGPIPE is kernel-handled silently; SIGKILL is OS-enforced-undeliverable; additional signals (SIGHUP config reload, SIGUSR1/2 app-specific) grow the enum by proposal. `:user::main` signature: `(stdin :crossbeam_channel::Receiver<String>) (stdout :crossbeam_channel::Sender<String>) (stderr :crossbeam_channel::Sender<String>) (signals :crossbeam_channel::Receiver<wat/kernel/Signal>)`. Hello-world acknowledges all four, uses one. Observer example demonstrates select-over-signals for graceful shutdown in its feed-candles helper. **(4) Programs are userland — with two exceptions.** New subsection after Pipeline Discipline. Articulates the six-rule conformance contract: function named by keyword path; handles as parameters (no ambient state); state as return value; drop-cascade observer; no self-pipes; HandlePool for client handles. A Rust function conforming to the six rules and registered by keyword path at startup is indistinguishable from a wat-authored program at spawn time (see WAT-TO-RUST.md for compile-path mechanics). The ONLY two stdlib programs are `:wat::std::program::Console` (single-sink stdout/stderr serializer) and `:wat::std::program::Cache<K,V>` (LRU memoization for AST-encoding hot path; 1 c/s → 7.1 c/s evidence). Everything else — Database, telemetry, rate-gates, signal converters, CLI parsing, all domain programs (Market Observer, Broker, Treasury in the trading lab) — is userland. Earlier speculation about shipping Database / rate-gate / emit-metric / flush-metrics as stdlib is explicitly rejected in the new subsection's "Why this cut" block. Trading lab's existing Rust programs continue unchanged; they register under `:project::trading::program/...` paths at startup and get spawned by wat `:user::main` indistinguishably. WAT-TO-RUST.md updated in parallel (commit `c2b6bf6`) to cover the compile-path emission for select, HandlePool, signals handler shim, and Path-C Rust-authored-registered-by-keyword-path programs. | 058 |
| 2026-04-18 | **Honest hello-world: use the Console program, join to flush, single-sink Console.** Datamancer's correction: "i think... /we must/ use the console program to write to stdout.... spawn it.. pass the handles... don't bind stdin... nor stderr.. just stdout... we need to call join... to flush it.. the input stream is just a name.. the honest example." Prior hello-world wrote to the raw `stdout` kernel sender directly — pedagogically a shortcut the reader would have to un-learn as soon as a second writer appears. Rewritten to show the canonical pattern every real program uses: `:user::main` spawns `:wat::std::program::Console` (passing the raw stdout sender and a producer count of 1), pops its single client handle from the returned HandlePool, calls `(:wat::std::HandlePool::finish pool)` to verify no orphans, invokes `:my::app::hello console` to do the work, then drops the client handle and joins the driver to flush any buffered writes before exit. Without the join, buffered writes may be lost at process exit. `:user::main`'s signature still declares all three stdio handles (kernel contract is fixed), but only `stdout` is referenced in the body — `stdin` and `stderr` are named in the signature as acknowledgment that the kernel provides them, never bound to locals, never passed anywhere. That is the honest reading: the slot exists, the program receives it, the program chose not to use it. Also landed: `:wat::std::program::Console` redesigned from 2-sink (owns stdout+stderr, methods `/out` and `/err`) to **single-sink** (owns one `:crossbeam_channel::Sender<String>`, method `/send`). Rationale: one program owns one resource is simpler than one program owns two. Orthogonal composition — if a program needs serialized stdout AND serialized stderr, it spawns TWO Console instances, one per sink. API uniformity — `:wat::kernel::send` and `:wat::std::program::Console/send` use the same verb, parallel operations at different layers. Observer example updated to the new signature (spawns one Console for stdout; stderr ignored at this example's scope; comment notes that a real deployment would spawn a second Console for stderr serialization). | 058 |
| 2026-04-18 | **Honesty ratcheted up: no bare aliases; `:wat/lang/` renamed `:wat::core::`; `select` added as a kernel primitive; Topic and Mailbox killed.** Two related datamancer corrections landed together: (A) "mailbox and topic — those are dead — kill them off — we found they didn't work in practice"; (B) "we rachet up the frustration towards simplicity... (define ...) is... not good... its (:wat::core::define ..) ... we are /very/ honest about the names." (A) Generic fan-out (Topic) and fan-in (Mailbox) proxy programs REJECTED — in practice they added a pointless thread hop over a loop the caller can write inline. Replaced with: fan-out = loop of `(:wat::kernel::send tx msg)` at the producer; fan-in = loop over a new kernel primitive `(:wat::kernel::select receivers)` that blocks until any receiver produces or disconnects, returning `(index, :Option<T>)`. Kernel primitive count grows from seven to eight. Concrete resource-owning programs (Console, Cache) stay in the stdlib because they earn their thread by owning specific state; generic proxies do not. (B) The dual-tier symbol table is GONE. Earlier drafts made every `:wat/...` form also reachable by its bare name (`Subtract`, `Bind`, `define`) with shadowing semantics for user redefinition. In the name of honesty, REJECTED. Every call to a wat-vm-provided form uses its full keyword path, always — `(:wat::core::define ...)`, `(:wat::core::let* ...)`, `(:wat::algebra::Bundle ...)`, `(:wat::std::Subtract ...)`. No bare aliases, no shadowing-by-symbol-table, no precedence rules. Lexical scoping (`let`, `lambda` parameters, `match` patterns) remains the ONE shadowing mechanism — and only introduces bare local names that never alias a `:wat/...` path. The language prefix `:wat/lang/` renamed to `:wat::core::` for symmetry with `:wat::algebra::` (both are "core" substrates; parallel naming honest about this). Naming Discipline section rewritten: "Bare aliases — convenience without sacrifice" subsection DELETED; "Shadowing also works at lexical scope" replaced by "Lexical shadowing is the only shadowing"; ":allow-redef interaction" simplified to two bullets (no bare-alias edge cases). Reserved prefixes list lists every named primitive under its full path. Hello-world and observer examples rewritten with full paths throughout — the examples model the discipline that every subsequent sub-proposal must follow. Pipeline-discipline subsection updated: rule #5 ("Inline your fan-out and fan-in — no generic proxies") replaces prior "shutdown cascade" wording to name the Topic/Mailbox rejection; eight rules total. Pending: sub-proposals (058-001 through 058-031) contain many code blocks with bare `define`/`lambda`/`let`/`match`/algebra forms that pre-date this sweep; they will be updated in a follow-up pass. | 058 |
| 2026-04-18 | **Pipe-management discipline expressed in the examples; stdlib programs named: Console, Topic, Mailbox, HandlePool.** Datamancer's call: "we need to review the current src/bin/wat-vm for how we do pipe management... we spent a lot of time getting a really damn good pattern... our examples should communicate our hard-earned expressions." The trading lab's `src/bin/wat-vm.rs` (840 lines, ~60 programs wired) encodes seven hard-learned patterns for CSP-style pipe management that the FOUNDATION examples did not show. Updates: (1) Kernel primitives section gains a single-ownership note — `QueueSender` and `QueueReceiver` are NOT cloneable; a sender belongs to one producer, a receiver to one consumer; this is the `write(fd, data)` discipline, and sharing requires a program that owns both sides. (2) Hello-world stays minimal (single writer → stdout directly) but gains a pointer to `:wat::std::program::Console` for the N-writer case. (3) Observer-style example replaced with a rich one that demonstrates: counting consumers up front; spawning `:wat::std::program::Console` for N-writer stdio; fan-out via `:wat::std::program::Topic`; fan-in via `:wat::std::program::Mailbox`; `HandlePool` claim/finish discipline; the shutdown drop cascade; joining drivers in reverse dependency order. (4) New subsection "Pipeline discipline — the hard-earned patterns" names seven rules: count-before-build, claim-or-panic (HandlePool), no-self-pipes, bounded(1)-default, drop-cascade-not-signal, reverse-dependency-join-order, no-Drop-on-driver-handles. Each rule cites why the trading lab pays it and what deadlock it prevents. (5) "Where Each Lives" updated with four stdlib programs (`wat/std/program/Console.wat`, `Topic.wat`, `Mailbox.wat`, `Cache.wat`) and one stdlib type (`wat/std/HandlePool.wat`). The substrate section now tells reviewers not just what the kernel provides, but how the kernel's seven primitives compose into the reusable pipe-management patterns the enterprise already uses in production. | 058 |
| 2026-04-18 | **Round-3 reconciliation pass. Both reviewers' R3-1 items closed; Hickey's R3-2/R3-4/R3-5 addressed; Beckman's R3-3 identity-atoms rejected.** Hickey delivered ACCEPT WITH OBSERVATIONS. Beckman delivered "the algebra composes" (Round 1 at 80%, Round 2 at 90%, Round 3 at ~98%). Both reviewers converged on ship. Both flagged reconciliation debt between accepted banners and stale bodies — the same R3-1 shape, different targets. **(1) Beckman R3-1 (FOUNDATION Sequential stale lines):** FOUNDATION's stdlib listings at ~2405 and ~2956 showed the OLD bundle-sum `Sequential` expansion contradicting 058-009's ACCEPTED bind-chain reframe. Rewritten to show the bind-chain form with cross-reference to the proposal banner. Line at ~2956 corrected to name the Bigram/Trigram additions and note Chain's rejection. **(2) Hickey R3-1 (058-016/026/027 body-banner contradiction):** three container proposals had clean banners but bodies still described `cleanup(Unbind ...)` retrieval and `nth` accessor — the pre-rename design. Added "HISTORICAL CONTENT — SUPERSEDED BY BANNER ABOVE" separators below each proposal's banner, explicitly marking the content below as audit record only and naming what the banner supersedes. Readers now cannot internalize the stale content as current. **(3) Hickey R3-2 (batch title understates scope):** INDEX.md gains a "Scope grew across rounds" paragraph naming what the batch now covers — kernel primitives, type system, config tier, compile path seed, etc. — so first-time readers expect substrate content in FOUNDATION alongside the algebra forms. **(4) Hickey R3-4 (dual-caching not named):** added a "The AST is primary; both materializations are caches" subsection to FOUNDATION's "Caching Is Memoization" section. Names explicitly that structural questions route to the Rust backing (HashMap/Vec/HashSet) while similarity questions route to the vector projection; both are caches of the same AST; identity lives in the AST alone. This closes the dual-caching framing Hickey flagged as missing. **(5) Hickey R3-5 (measurements tier prominence):** lifted the cosine/dot measurements tier from a subsection of "Complete Forms" into its own top-level "Two Kinds of Algebra Primitive — Holon-Producing and Scalar-Returning" section, placed before "The Output Space." The tier split is now visible early in the document with its categorical framing stated up front. **(6) Beckman R3-3 (reserved identity atoms `:wat::algebra::zero` / `:wat::algebra::one`): REJECTED.** Datamancer's framing: *"I don't know what :wat::algebra::zero is meant to be..... 0 /is/ 0...right?"* The proposal would introduce two special-case atoms that bypass the hash (returning the all-zero and all-+1 vectors directly). Three problems: (a) it complects Atom's semantics — most atoms hash to random, two would special-case, braiding two behaviors in one form; (b) the identities are semantically reachable even without names (Bundle of nothing is zero; Bind's dense identity arises from the arithmetic); (c) Beckman himself tagged this "Minor; nice-to-have, not a blocker" and "STILL MISSING" with a weak verdict. Accepting it would violate the sharp-rejection discipline Hickey praised (named forms need cited production use). No 058 proposal or application has shown need for the named identities. If users want a zero vector, they compute one — `(:wat::algebra::Blend v v 1 -1)` produces the zero vector for any v; similar tricks exist for all-+1. Same status as other "useful in theory but uncited in practice" forms: rejected for 058; userland may define in their own namespace if they want the names. **(7) Beckman R3-2 (laws appendix): deferred.** Beckman noted laws are scattered in prose; a consolidated appendix would close the reviewer's ability to check every law at a glance. Not a blocker; nice-to-have for implementation reference. Left as a post-058 documentation task. **Aggregate result.** Both reviewers' MAJOR items closed. Both reviewers' minor observations addressed or formally deferred. The 058 batch is now in the state Hickey called for: "An afternoon of mechanical cleanup closes it." Zero banner-body contradictions remaining that a first-time reader will misread. The batch ships. | 058 |
| 2026-04-18 | **058-005 Orthogonalize reframed + renamed as `Reject` + `Project` stdlib macros. Algebra core shrinks 7 → 6.** The last substantive designer question closed on due diligence. **Not speculative** — the primer cites `reject` as the DDoS sidecar's core detection mechanism (`reject(packet_vec, baseline_subspace)`, Challenge 010 F1=1.000) and `project` as engram-matching's primitive. The operation stays. **Three resolutions.** **Q1 — stdlib, not core.** Blend accepted as Option B earlier in this session (independent real-valued weights). Option B absorbs the Gram-Schmidt coefficient `-(X·Y)/(Y·Y)` as a computed f64 expression — Blend's weight slot accepts any f64-producing expression. The original core argument dissolved; Orthogonalize migrates to stdlib macro, same pattern Subtract and Amplify followed. Algebra core shrinks from 7 to 6 variants: `Atom, Bind, Bundle, Blend, Permute, Thermometer`. **Q2 — ship Project.** `Project(x, y) = Subtract(x, Reject(x, y))`. Gram-Schmidt decomposes every vector into along-y (Project) and orthogonal-to-y (Reject) components; `Project + Reject = x`. Both cited in the primer. Ship both as stdlib macros; companion named form for the complement. **Q3 — rename Orthogonalize → Reject.** Primer uses `reject`; holon-rs uses `reject`; Challenge 010 writeups use `reject`. `Orthogonalize` describes the output's geometric property; `Reject` names the operation's intent. Aligns with production and parallels `Project` cleanly. Users who prefer the mathematical name define `:my::vocab::Orthogonalize` as a one-line wrapper. **New algebra measurement primitive: `:wat::algebra::dot`.** Scalar-returning bilinear measurement. Sibling to `:wat::algebra::cosine` (already implicit in presence measurement). Not a HolonAST variant — measurements produce scalars, not vectors. Exposed explicitly as its own primitive so stdlib macros that need the raw coefficient (Reject, Project, and any future Gram-Schmidt composition) can name it. Rust implementation: elementwise multiply + sum reduction; trivial cost since the operation is already computed wherever cosine runs internally. FOUNDATION gains a new "Algebra Measurements — scalar-returning primitives (not HolonAST variants)" subsection documenting `cosine` and `dot` alongside each other. **Sweep.** 058-005/PROPOSAL.md rewritten with ACCEPTED-with-reframe-and-rename banner — two stdlib macros (Reject + Project), each one line over Blend + dot. Historical Orthogonalize-as-core content preserved below the banner as audit. FOUNDATION's core-form listings updated throughout: "Algebra Core (7 forms)" → "(6 forms)"; Holon enum "7 variants" → "6 variants"; Output Space per-form table drops the Orthogonalize row; capacity-cost table drops the row (scalar-returning dot is zero-cost in frame terms); Summary section's "Foundation =" line updates to name cosine and dot explicitly. "Where Each Lives" stdlib listing gains `Reject.wat` and `Project.wat`. "Algebra Stdlib" listing adds both to the Blend-derived idioms family. Negate-decomposition note rewritten: subtract → stdlib; flip → REJECTED; orthogonalize → stdlib Gram-Schmidt duo. INDEX status column, dependency graph, reading-order entry updated. OPEN-QUESTIONS 058-005 section collapses to ACCEPTED pointer. **Live designer questions drop from 3 to 0.** OPEN-QUESTIONS "Live questions for Round 3" block becomes "NONE REMAINING" — all substantive designer questions across the 058 batch closed on due diligence. Round 3 reviewers may still reopen any decision; the per-question reasoning is preserved in each proposal's banner and in dated changelog entries. The 058 surface is ready for designer review. | 058 |
| 2026-04-18 | **Ambient console accessors removed; stdio handles thread through function parameters.** Datamancer's correction: "this is not honest... we need to have an (out, err) pair per user of console... their scope must receive the handles they want to use them... this is the /frustrating/ part of haskell.. so what.. it works.. simple not easy." The prior draft included three ambient kernel accessors — `(:wat::kernel::console-out)`, `(:wat::kernel::console-err)`, `(:wat::kernel::console-in)` — which any function could call to obtain a stdio handle implicitly. Ambient authority is exactly the dishonesty wat's type system is meant to prevent: the function signature failed to reveal that a console side-effect was possible. Removed. Replacement: the kernel invokes `:user::main` with three parameters — `(stdin :crossbeam_channel::Receiver<String>)`, `(stdout :crossbeam_channel::Sender<String>)`, `(stderr :crossbeam_channel::Sender<String>)`. Any downstream function that wants to write to the console declares the handle it needs in its own signature and receives it from its caller; functions that never touch the console never see the handles. Hello-world rewritten: `(define (:my::app::hello (name :String) (out :crossbeam_channel::Sender<String>) -> :()) ...)` with `:user::main` passing `stdout` through. Observer example updated: `:user::main` gains the three stdio parameters. Kernel primitives section's Console subsection rewritten from accessor list to a discipline statement. `:wat::kernel::console-out` reference removed from the reserved-prefixes primitive list. This is Haskell's IO discipline without the monad wrapper: threading plain values through function parameters makes every side effect visible at the type. Simple, not easy. The frustration IS the discipline. | 058 |
| 2026-04-18 | **Tokenizer bracket-tracking rule corrected — only `()`, not three pairs.** The FOUNDATION's colon-quoting section stated the tokenizer tracks bracket depth across "three pairs — `()`, `[]`, `<>`." Both other pairs are wrong: `[]` and `{}` are not wat syntax at all, and `<`/`>` are plain characters inside keywords (the `<` in `:Vec<T>` and the `>` in `->` of `:fn(T,U)->R` are not special — they're the same kind of plain char as `/`, `-`, `,`). The ONLY structural brackets wat has are `()`, and the tokenizer tracks paren depth inside keywords because `(` and `)` can appear there (as in `:fn(T,U)->R`) — without depth tracking, the outer `)` that closes the enclosing form would get misread as part of the keyword. Corrected rule: tokenizer tracks PAREN depth only; a keyword ends at whitespace at paren-depth 0 or at an unmatched `)`. Discovered while building wat-rs's lexer (the Rust implementation of the wat frontend); the `:fn(T,U)->R` test case failed under the three-pair rule because `>` at angle-depth 0 was being treated as a closer. Sweep: FOUNDATION.md's colon-quoting section corrected; 058-030-types/PROPOSAL.md's type-grammar tokenizer bullets corrected; 058-030's Q11 RESOLVED answer corrected. No semantic change at the wat level — users' source is unaffected; only the implementation guidance is corrected. | 058 |
| 2026-04-18 | **Ignorant-ward sweep: post-wat-rs-MVP reconciliation.** The ignorant ward cast across proposal + holon-rs + wat-rs reported one load-bearing contradiction and eight smaller findings; this entry records the fix sweep. **(1) 058-030 `:holon::HolonAST` enum declaration was stale — listed 9 variants (Atom, Bind, Bundle, Permute, Thermometer, Blend, Orthogonalize, Resonance, ConditionalBind).** Leftover from the Orthogonalize→stdlib and Resonance/ConditionalBind rejections. Corrected to 6 variants in all three places in 058-030 (top-of-section claim, enum declaration, exhaustive match example); the primitive-dispatch table at the same file's line 481 already had 6 — the doc was self-contradicting. 058-001's "Keeps the HolonAST enum at 7 variants" claim also corrected to 6; the parametric-Atom Rust-level implementation note updated to mention `std::any::Any` + `AtomTypeRegistry` dispatch explicitly. **(2) `:wat::config::set-global-seed!` added to FOUNDATION's `:wat::config` section as a third field** alongside `dims` and `capacity-mode`. Default: 42. Commit-once via setter. Named: the seed-consistency invariant ("two nodes running the same wat program produce bit-identical vectors") now has an explicit locus in the config contract, not an implementation-side implication. Datamancer's framing: "users should never need to touch it... the override exists for deliberate isolation." Reserved-prefixes list updated. **(3) `atom-value` signature corrected in 058-001** from `(a :Atom<T>) -> :T` to `(a :holon::HolonAST) -> :Option<T>`, matching the Rust-level `Option<&T>` reality. Recovery can fail if the argument isn't an Atom variant or carries a different T; the type checker guarantees Some at every well-typed call site, but the Option makes the rare failure mode expressible. **(4) Double-atomization / join added to 058-001**: explicit note that `Atom<T>` has a unit but NOT a join — `Atom(Atom(42))` is a legitimate nested composite, producing an opaque-identity vector distinct from `Atom(42)`. Each wrap produces its own layer. Closes Beckman's categorical nice-to-have. **(5) Canonical-EDN sub-claims added to FOUNDATION's cryptographic-provenance section**: (a) termination of `canonical_edn_holon` is well-founded (finite-tree descent); (b) `Atom<f64>` payloads containing NaN are implementation-defined — applications requiring cross-node determinism exclude NaN from atom payloads. Closes both Beckman's categorical termination note and the NaN-bit-pattern qualifier on the "same AST → same EDN" claim. **(6) FOUNDATION gains a "Map of this document" section** at the top naming three tiers — algebra, kernel substrate, language core — with section-range pointers. Closes the teaching-gap finding that a reader walking top-to-bottom met kernel primitives before the algebra-core definitions finished. **(7) wat-rs README gains a status section** naming what's landed (parser, lexer, WatAST, lowering for 6 core forms, `eval_algebra_source`) vs pending (config pass, `load!`, language forms, macros, types, resolution, hashing, freeze, runtime, CLI). Closes the "reader drops over a cliff" finding. **H1 (measurements tier in wat-rs) is scope-deferred** — `cosine` and `dot` return `:f64`, not Vector, and belong with the runtime slice that adds a value-dispatch layer; not a current fix. **T2 went with the small fix** ("Map of this document" marker, not a three-document split) — deferring the split until the language-core section's growth forces it. Pattern matches Hickey's "afternoon of mechanical cleanup" predicted closure shape. | 058 |
| 2026-04-19 | **Signed load mode: algorithm field + base64 transport + sign-the-hash semantics.** The spec example `(signed <sig> <pub-key>)` was underspecified in three ways that surfaced when wat-rs reached slice 8b. Fixed in this pass. **(1) Algorithm made explicit.** The form gains a bare-symbol algorithm field: `(signed <algo> "<b64-sig>" "<b64-pubkey>")`. Parallels hash mode's algorithm head (`sha256`, `md5`, ...). First implementation: `ed25519` (via `ed25519-dalek` in wat-rs). Unknown algorithms return `UnsupportedSignatureAlgorithm` and halt startup. The field is pluggable today; RSA / ECDSA / Ed448 implementations slot in when deployments demand them. **(2) Transport is base64.** Signatures and pub-keys are base64 strings in the wat source, not hex. Rationale: base64 is the universal transport format for cryptographic material (PGP, JWT, ssh-ed25519) — denser than hex (88 vs 128 chars for Ed25519 sig), and the standard ecosystem expectation. Hash mode stays hex (short, human-comparable); signed mode uses base64 (longer payloads, standard crypto transport). Two modes, two encodings, each fit for purpose. **(3) Signing target is the SHA-256 of canonical-EDN, not raw source bytes.** This binds signatures to MEANING, not formatting. A trusted producer signs `sha256(canonical_edn_wat(parsed_ast))`; receivers recompute and verify. Comments, whitespace, or other byte-level edits that don't alter the parsed AST leave signatures valid. Aligns with the "hash(expanded AST) IS holon identity" claim — signing that identity is the natural lift. **Two levels surface in the foundation.** (a) **Per-file**, inside the `load!` form — gates a single loaded file. (b) **Full-program**, outside source syntax — after load+macro expansion, the flat form list has its own canonical-EDN and SHA-256; signing that closes the glue between loaded files and the entry file. Delivered via wat-vm CLI (task #141) or a companion signature file; a signature carried inside a file cannot cover the file that carries it. **wat-rs ships real crypto**: `verify_ast_signature` (per-file) and `verify_program_signature` (full-program) both implemented with `ed25519-dalek`'s `verify_strict` (rejects small-order R components for malleability resistance). Tests cover round-trip, tamper at AST level, tamper at program level, wrong pub-key, malformed base64, wrong signature length, wrong pub-key length, and unsupported algorithm. The parser tightens correspondingly: `(signed ...)` now requires `<algo-symbol> <sig-string> <pubkey-string>` — the earlier placeholder-shape (`render_placeholder` in load.rs) is gone. | 058 |
| 2026-04-19 | **Load grammar redesign — `:wat::load::*` and `:wat::verify::*` interface keywords.** The per-file load grammar settled on a symmetric three-form design that makes every byte-fetching decision explicit in the source. **The three forms.** (a) `(:wat::core::load! :wat::load::<iface> <locator>)` — unverified. (b) `(:wat::digest-load! :wat::load::<iface> <locator> :wat::verify::digest-<algo> :wat::verify::<iface> <payload>)` — hash-of-bytes integrity, verified pre-parse. (c) `(:wat::signed-load! :wat::load::<iface> <locator> :wat::verify::signed-<algo> :wat::verify::<iface> <sig> :wat::verify::<iface> <pubkey>)` — Ed25519-over-canonical-EDN provenance, verified post-parse. Each form has unambiguous arity; the grammar rejects a `:wat::verify::signed-*` keyword inside `digest-load!` at parse time (and vice versa) so cross-kind confusion halts early. **Two namespaces, two concerns.** `:wat::load::*` governs SOURCE fetching (parse + cycle detection + commit-once). `:wat::verify::*` governs VERIFICATION PAYLOAD fetching (just bytes) AND the verification algorithm (encoded in the keyword itself: `digest-sha256`, `signed-ed25519`). The payload-fetching concern is distinct from the source-fetching concern — both happen to traffic in filesystem paths today, but they have different invariants, different failure modes, and different semantic weight. Datamancer's framing: "simple, not easy — they ARE different but similar; two concerns, two namespaces." **Implemented in wat-rs slice 8c.** `:wat::load::string` (inline source literal), `:wat::load::file-path` (filesystem source), `:wat::verify::string` (inline payload), `:wat::verify::file-path` (sidecar payload file), `:wat::verify::digest-sha256` (hash verification), `:wat::verify::signed-ed25519` (signature verification). **Reserved but not implemented:** `:wat::load::http-path`, `:wat::load::s3-path`, `:wat::load::git-ref`, `:wat::verify::http-path`, `:wat::verify::s3-path` — explicitly refused at parse time with "reserved but not implemented in this build" so the extension path is clean when deployments need them. **Sidecar signatures work naturally** because `:wat::verify::file-path` can supply sig and pubkey from separate files: `(:wat::signed-load! :wat::load::file-path "code.wat" :wat::verify::signed-ed25519 :wat::verify::file-path "code.wat.sig" :wat::verify::file-path "code.wat.pubkey")`. **Replaces the earlier slice-8b placeholder shape** — `(signed ed25519 "sig" "pubkey")` as a tail-form inside `(:wat::core::load! ...)` is gone; the new three-form grammar is the honest one. Reserved-prefix list in `resolve.rs` gains `:wat::load::` and `:wat::verify::`; users cannot register definitions under those paths. 26 integration tests in `load.rs` cover the full grammar, digest + signed round-trips, sidecar payloads, grammar errors (non-keyword interface, unknown algorithm, wrong kind, wrong arity), cycle / commit-once / setters-in-loaded-file, and inline-string duplicate detection via content-hashed synthetic canonical path. | 058 |
| 2026-04-19 | **Typed-let discipline — every binding declares its type.** The 2026-04-18 type-grammar sweep left one optional escape hatch: 058-030:587 / OPEN-QUESTIONS:307 said let bindings may carry type annotations if helpful (`(let (((x :holon::HolonAST) rhs)))`) but the bare form `(let ((x rhs)))` was permitted, with the RHS type inferred. wat-rs slice 7b's rank-1 HM pass made the trade-off visible: a let-bound lambda gets its type from the lambda's declared signature, but the checker either has to read that type off the RHS (call it "inference from context" or "reading a declaration") or leave call sites to the bound name unchecked. The datamancer's position, applied: **simple, not easy — every typed position declares its type, including let bindings.** New discipline, effective this slice: **every let binding is `((name :Type) rhs)`.** The typed form is mandatory; the bare-`(name rhs)` form is removed from the grammar and refused at parse. Same rule applies to `let*`. Rationale: (1) anonymous functions declare their constraints per 058-029 — the principle extends to EVERY named binding; (2) one let shape instead of two; (3) no inference path at binding sites — the checker uses the user's declaration directly; (4) matches 058-031's "every typed position carries a concrete commitment." Example: `(:wat::core::let (((doubler :fn(f64)->f64) (:wat::core::lambda ((x :f64) -> :f64) (:wat::core::* x 2)))) (doubler 21))` — the let binding declares `doubler`'s type, the lambda also declares its own; the checker unifies them. An expression like `(let (((doubler :fn(i64)->i64) (lambda ((x :f64) -> :f64) ...))) ...)` is a type error because the declared binding type doesn't match the lambda's signature. Replaces 058-030's Q2 / OPEN-QUESTIONS Q2 recommendation ("infer intermediates; allow optional annotation"). wat-rs implementation: `runtime::eval_let` + `runtime::parse_let_binding` refuse untyped bindings with a `MalformedForm` error whose message spells out the required shape. `check::infer_let` uses the declared type directly — the bound name has the DECLARED type in the body, not the inferred RHS type, and a RHS that doesn't unify with the declaration raises a `TypeMismatch`. Two runtime tests (`untyped_let_binding_rejected`, `let_binding_with_any_type_rejected`) plus five new check tests (typed binding round-trip, wrong-type RHS rejection, multiple bindings, lambda-valued binding matching declaration, lambda-valued binding with mismatched declaration rejected). All pre-existing runtime tests (`let_binds_parallel`, `let_shadows_outer`, `closure_captures_enclosing_variable`, `closure_captures_let_binding`, `algebra_atom_from_bound_variable`) rewritten to the new shape. Macro tests (`template_identifier_carries_macro_scope`, `two_macro_invocations_get_distinct_scopes`) updated to drill through the extra `(name :Type)` paren layer. 278 total tests pass; zero warnings. | 058 |
| 2026-04-19 | **Colon-quote model — `:` is wat's symbol-literal reader macro; `::` is the canonical namespace separator.** The `/` separator was a wat-local accommodation, not a design choice; the realization surfaced when writing `:crossbeam_channel::Sender<T>` in the kernel-types discussion and finding the lexer rejecting internal colons. **The insight:** one leading `:` quotes the body as a symbol literal; the body that follows is a **literal Rust path**. No translation. What you write IS the Rust form. `:wat::core::load!` is a single keyword — `::` is Rust's path separator, pushed as body characters. `:wat::core::/` is the division operator: separator is `::`, name is `/`, no ambiguity (the previous `:wat/core//` reading conflated two roles for `/`). **Corrections this implies** (all landed in wat-rs + this proposal corpus on 2026-04-19): (1) lexer drops `InternalColon` error; `::` inside a keyword body is a body character, not a special token. (2) Every reserved-prefix and user-path flips from `/` to `::`: `:wat::core::`, `:wat::algebra::`, `:wat::kernel::`, `:wat::std::`, `:wat::config::`, `:wat::load::`, `:wat::verify::`, plus user namespaces (`:my::vocab::foo`, etc.). (3) **`:List<T>` → `:Vec<T>`** — Rust's collection name. The constructor `:wat::core::list` becomes `:wat::core::vec`, paralleling Rust's `vec![]` macro. (4) **Named `Pair` and `Tuple` retired for tuple-literal syntax `:(T,U)` / `:(T,U,V)`** — Rust has no `Pair` or `Tuple` named types; it writes tuples as `(T,U,V)`. wat-rs's `TypeExpr::Tuple(Vec<TypeExpr>)` variant parses this directly. `:()` stays as unit (= 0-tuple in Rust). `:(T,)` is the 1-tuple; `:(T)` is grouping (flattens to T). (5) **`:Union<T,U,V>` retired** — Rust has no anonymous union. Heterogeneous data uses a user-declared `(:wat::core::enum :my::MaybeT ...)` with named variants; every coproduct carries a discriminator, dispatch is explicit. (6) **Kernel channel types use crate paths**: `:crossbeam_channel::Sender<T>` / `:crossbeam_channel::Receiver<T>` (trading-lab's actual dep) replace the earlier hand-named `:QueueSender<T>` / `:QueueReceiver<T>`. **Why this is correct, not a preference**: anywhere a type is declared in wat source, the honest Rust mapping should be text-identical to what a Rust programmer would write — that's what "the language is strongly typed" means at the syntactic layer. The `:` quotes it; the body is Rust. **Not a proposal for review** — the framing is a correction of a mistake in the original `/`-separator convention. The 058 proposal corpus was written under the earlier convention; this entry documents the correction sweep that updated every keyword path, every type grammar example, every tokenizer-rule statement (058-030's rule section rewritten to name the colon-quote model), every cross-reference to retired types. The wat-rs crate lands the same corrections across seven sequential commits (A1 lexer, A2 tuple parser, B1+B2 source sweep, C1 Vec rename, C3 Union retirement). D1 (holon-lab-trading `wat/*.wat`) is scope-deferred — those files will be rewritten soon for other reasons; not worth a separate migration pass. | 058 |
| 2026-04-19 | **Namespace honesty — Value variants encode full Rust paths; `:Keyword` → `:wat::core::keyword`, `:Holon` → `:holon::HolonAST`.** Datamancer: "my entire career I've been chasing how to do namespaces.. the best way to do it.. is to just do it.. we are just doing it." The colon-quote sweep's follow-on: every type identifier in wat source shows its full namespace path, and every Rust `Value` variant name reflects the Rust type it carries. **Wat-source type renames.** `:Keyword` → `:wat::core::keyword` (keywords are a wat-core concept; they belong in core namespace). `:Holon` → `:holon::HolonAST` (the algebra AST lives in the holon crate; wat-source name matches). `:wat::WatAST` introduced as the first-class type of parsed wat programs (runtime AST values consumed by `eval-ast!`). **Rust `Value` enum variants encode full paths via `__` separator.** One-to-one: internal `::` becomes `__`; leading `::` never encoded (Rust paths don't write it). Renames: `Value::Bool` → `Value::bool`, `Value::Int` → `Value::i64`, `Value::Float` → `Value::f64`, `Value::Keyword` → `Value::wat__core__keyword`, `Value::Holon` → `Value::holon__HolonAST`, `Value::Function` → `Value::wat__core__lambda`, `Value::List` → `Value::Vec`, `Value::Ast` → `Value::wat__WatAST`, `Value::Sender` → `Value::crossbeam_channel__Sender`, `Value::Receiver` → `Value::crossbeam_channel__Receiver`. Prelude types (`bool`, `i64`, `f64`, `String`, `Vec`, `()`) stay short because that's what Rust users write — wat follows Rust's prelude convention. **`Value::type_name()` returns the full `::`-separated path** so error messages read exactly like user-written declarations: "expected `crossbeam_channel::Sender`, got `i64`." **`#[allow(non_camel_case_types, non_snake_case)]`** on the `Value` enum — lint suppression is explicit and scoped to the one enum that deliberately breaks convention for honesty. **Why this is correct, not aesthetic.** A variant named `Sender` holding an `Arc<crossbeam_channel::Sender<String>>` pretended the name told less than it did. Every future reader, error message, downstream reviewer had to know the convention. Encoding the full path eliminates the gap — namespaces are what they are when you do them correctly. **Corpus sweep.** wat-rs source + tests + 27 proposal files in one pass. `Vec<Holon>` at every occurrence becomes `Vec<holon::HolonAST>`. Built-in schemes in `check.rs` register with new names; 344 tests pass (333 lib + 10 integration + 1 doc); zero warnings. The `:AST<T>` wrapper for typed-macro parameters stays as-is — it's a compile-time parser-expression concept distinct from the runtime `:wat::WatAST` value. | 058 |

| 2026-04-19 | **CLI-level whole-program signature retracted — verification is per-form only.** The earlier 2026-04-19 signed-load entry introduced a "full-program, outside source syntax... Delivered via wat-vm CLI (task #141)" framing as a second signing level. That framing is retracted. There is no `wat-vm --signed <algo> --sig <b64> --pubkey <b64>` CLI flag, and FOUNDATION's cryptographic-provenance section no longer claims one. Rationale: **a program is a collection of forms with independent provenance needs** — one loaded file from a trusted producer, another file from a different producer, a runtime eval of an upstream message signed by the originator. One CLI-level signature cannot cover that structure. Signatures live where the fetch happens: `(:wat::signed-load! ...)` at startup, `(:wat::eval-signed! ...)` at runtime. Each form carries its own `:wat::verify::signed-<algo>` + sig + pubkey payloads and verifies its own SHA-256 of canonical-EDN independently. A program may invoke any number of each; the verification surface IS that collection of forms. The earlier entry's architectural claim survives (signature target is SHA-256 of canonical-EDN; base64 transport; ed25519 as first algorithm); only the "second level at CLI invocation" idea is retracted. Docs swept: FOUNDATION.md § cryptographic provenance rewritten to per-form; wat-rs README's "what's next" drops `wat-vm --signed` and adds a note that verification is per-form; wat-rs `freeze.rs` module doc + `hash.rs :: verify_program_signature` doc comment updated to match. No code changes — `verify_program_signature` is already called per-form by `signed-load!` and `eval-signed!`; the function's name survives as internal machinery. | 058 |
| 2026-04-19 | **Signal model — kernel measures; userland owns transitions.** Firm administrative stance: the wat-vm kernel does NOT deliver signals as queue messages. It exposes signal state as pollable booleans; userland polls and resets. Rationale: signals are not events that "happened once and must be handled;" they are conditions — "is there a reload request pending?" The coalesced boolean is the honest representation of that question, and putting the transitions in userland (reset when the work is done) makes the contract explicit. SIGINT / SIGTERM remain TERMINAL — mapped to the existing `stopped` kernel flag, set-once, no reset, cascade via disconnect. SIGUSR1 / SIGUSR2 / SIGHUP are NON-TERMINAL — each has a kernel-maintained boolean, flipped true by the OS handler, flipped false by a userland `reset-*!` call. Six new primitives: `(:wat::kernel::sigusr1?)` / `(:wat::kernel::reset-sigusr1!)`, same for sigusr2 / sighup. **Consequences retrofitted across FOUNDATION.** The `:wat::kernel::Signal` enum is removed. The `signals` queue 4th parameter of `:user::main` is removed — `:user::main` stays 3-param (`stdin`, `stdout`, `stderr`). Hello-world and the observer-style lifecycle example updated to drop the signals handle and use poll-and-reset semantics in their feed loops. Userland that wants domain-typed event messages builds a converter program on top of the pollable flags. **Naming convention set.** `?` suffix = boolean predicate; `!` suffix = state-committing mutation; bare = value-producing non-predicate. The new signal primitives ship honest. Existing non-conforming forms — `(:wat::kernel::stopped)` (should be `stopped?`) and `(:wat::core::presence)` (returns `:f64`; the boolean verdict should be `presence?`, and the raw measurement belongs on `:wat::algebra::cosine`) — are grandfathered for now; the next naming sweep renames them. Tracked in `docs/058-backlog.md` Step 1.5. | 058 |
| 2026-04-19 | **`:Option<T>` + `:wat::core::match` land; `recv` upgraded to `Option` return.** Step 2 of the `docs/058-backlog.md` sequence, taken out-of-order ahead of Step 1 because every kernel primitive with an `:Option<T>` signature (`recv`, `try-recv`, `select`) needs the runtime substrate first. **Built-in parametric enum.** `:Option<T>` is the sole built-in enum in 058-030's grammar (line 98); its shipped surface: `:None` (nullary keyword, evaluates directly to `Value::Option(None)`), `(Some <expr>)` (bare reserved-identifier tagged constructor, arity 1, evaluates to `Value::Option(Some(v))`). Both forms dual in match patterns — `:None` and `(Some binder)` — alongside `_` wildcard and bare-identifier binders. **`(:wat::core::match scrutinee arm...)`** added to the language core's keyword dispatch. Each arm is `(pattern body)`; arms are tried in order; the first matching arm fires. The type checker enforces exhaustiveness for `:Option<T>` (arms must cover both `:None` and `(Some _)`, or include a wildcard) and unifies every arm body's type to a single result type. Runtime `PatternMatchFailed` fires only when the type-check was bypassed. **`recv` upgrade.** `:wat::kernel::recv`'s scheme is now `∀T. Receiver<T> -> :Option<T>` — disconnect becomes first-class `:None` rather than an error; the surviving `ChannelDisconnected` runtime error is produced only by `send` to a dropped receiver. Echo / programs-as-atoms / presence-proof CLI tests migrated to the match-around-recv shape. **wat-rs code surface.** `runtime.rs` gains `Value::Option(Arc<std::option::Option<Value>>)`, `eval_some_ctor`, `eval_match`, `try_match_pattern`, and a `PatternMatchFailed` error; `eval_kernel_recv` returns `Value::Option`; `:None` keyword is special-cased before the generic keyword fallback. `check.rs` gains `infer_match`, `pattern_coverage`, special-cases for `:None` and bare `Some`, a `CheckError::MalformedForm` variant, and the upgraded recv scheme. 372 tests still green (3 CLI tests updated to the match shape); zero warnings. | 058 |
| 2026-04-19 | **IO model retrofit — `:user::main` receives real OS handles; Console is the dual-sink gateway.** Two corrections hit FOUNDATION at once. **(1) Stdio is not a channel.** The earlier spec had `:user::main` receiving `(stdin :crossbeam_channel::Receiver<String>) (stdout :crossbeam_channel::Sender<String>) (stderr :crossbeam_channel::Sender<String>)` — a channel-shaped abstraction that required bridge threads in the wat-vm binary to mediate bytes between the channel and the actual OS stream. Dishonest leak of implementation detail. Corrected: `:user::main` receives `(stdin :io::Stdin) (stdout :io::Stdout) (stderr :io::Stderr)` — the real `Arc`'d std::io handles. Writes go straight to the OS via std's internal locking on `.lock()`. No bridge threads. Two new primitives: `(:wat::io::write handle msg)` (polymorphic over `:io::Stdout` and `:io::Stderr`; blocks until flushed) and `(:wat::io::read-line handle)` (takes `:io::Stdin`; returns `:Option<String>`, `:None` on EOF). User direction 2026-04-19: "pass the rust program's stdout and stderr file descriptors into it... the console exposes access to those via the program's pipes that it loops over." **(2) Console is the dual-sink sole gateway.** Earlier spec: Console is single-sink (one `Sender<String>`); programs spawn TWO Console instances if they want both stdout and stderr. Corrected: Console takes BOTH `:io::Stdout` AND `:io::Stderr`; one Console per program is the honest gatekeeper. User direction: "the console /should/ be the only way to print to the world... anyone who wants console access /must/ be provisioned a pair of pipes." Shipped signature: `(:wat::std::program::Console stdout stderr count) → :(HandlePool<Sender<(i64,String)>>, ProgramHandle<()>)`. Each client handle is a bounded(1) sender of tagged `(i64, String)` messages — `tag=0` routes to stdout, `tag=1` to stderr. Helpers `Console/out` and `Console/err` encode the tag; old `Console/send` retired. **(3) Console is called DIRECTLY, not via `spawn`.** Path B per the 2026-04-19 direction ("we may be able to do everything in wat... we may not need wat-to-rust at all"). FOUNDATION's previous `(:wat::kernel::spawn :wat::std::program::Console stdout 1)` would have required driver-registry machinery to return `(pool, driver)` synchronously. Instead Console is a wat-source define that spawns its own internal driver thread and returns the tuple directly. No driver-registry concept needed at all — wat surface is enough. **Consequences retrofitted across FOUNDATION.** `:user::main` signature, hello-world example, observer-style example, "Where Each Lives" wat/std/program/Console.wat description, Pipeline Discipline rule 1, core-primitives listing (gains `:wat::io::*` namespace, `:wat::core::list` / `tuple` / `first` / `second` / `third` / `rest` / `map` / `foldl` / `range` / `take` / `drop` / `length` / `empty?` / `reverse`, and the `:wat::core::i64::{+,-,*,/}` / `:wat::core::f64::{+,-,*,/}` arith split), all swept. Algebra listing adds `cosine` / `dot` / `presence?`. **wat-rs side** (commit 3d5c5c3): `Value::io__Stdout(Arc<io::Stdout>)`, `Value::io__Stderr(Arc<io::Stderr>)`, `Value::io__Stdin(Arc<io::Stdin>)` replace the channel wrapping; `bin/wat-vm` drops the stdin-reader + stdout/stderr-writer bridge threads; `stdlib/program/Console.wat` carries the dual-sink design. 454 tests; zero warnings. CLI test `stdlib_console_hello_world` proves end-to-end. | 058 |
| 2026-04-19 | **Step 1.5 naming-convention sweep — `stopped?`, `cosine`, `presence?`.** The grandfathered non-conforming forms conform now. (1) `(:wat::kernel::stopped)` → `(:wat::kernel::stopped?)`. The terminal-signal predicate carries the `?` suffix like every other predicate in the kernel's signal surface (`sigusr1?`, `sigusr2?`, `sighup?`). (2) `(:wat::core::presence target ref) -> :f64` retires, splitting into two forms at the correct namespace: `(:wat::algebra::cosine target ref) -> :f64` — the raw measurement, sibling of `:wat::algebra::dot`; `(:wat::algebra::presence? target ref) -> :bool` — cosine compared against the committed `:wat::config::noise-floor`, the boolean verdict. Both live at `:wat::algebra::*` per OPEN-QUESTIONS line 419 (algebra substrate operations take holons, not raw numbers). `:wat::std::math::*` stays for raw-number utilities only (`ln`, `sin`, `cos` of an angle, `pi`) — `math::cos(theta)` and `algebra::cosine(x, y)` share a root but do different things. **Sweep across the codebase.** wat-rs: runtime dispatch + check schemes swapped; all unit tests and CLI tests migrated; 453 green (was 449); zero warnings. Docs: FOUNDATION.md updated (core-primitives listing, kernel-primitives listing, stopped-flag example, naming-convention comment in the signals subsection). 058-001/PROPOSAL.md's presence reference repointed. Backlog Step 1.5 checked off. **Audit pass — no other renames needed.** Comparison operators (`=`, `<`, `>`, `<=`, `>=`) stay bare by Lisp convention (operators, not semantic predicates). `empty?` already ships conforming. The wat-rs signal primitives shipped honest from the start. | 058 |
| 2026-04-19 | **Programs-as-holons operational — `:wat::core::quote`, `:wat::core::presence`, `:wat::config::noise-floor` land.** The vector-level proof that MAP bind / unbind self-inverse is observable through presence measurement runs end-to-end: `$ echo watmin | wat-vm presence-proof.wat` → `None\nSome\nwatmin`. **Additions to FOUNDATION's core forms list** (§ "Naming Discipline"): `:wat::core::quote` (captures unevaluated AST as `:wat::WatAST`; the mechanism that places a wat program into the algebra without firing its side effects); `:wat::core::atom-value` (reconciled to `:wat::core::` from `:wat::std::` per 058-001 below; structural payload read from `:Atom<T>` returning `:T` exact); `:wat::core::presence` (the retrieval primitive per § 1718 "Presence Is Measurement, Not Verdict" — cosine between encoded holons, `:f64` scalar, caller binarizes); `:wat::eval-ast!` / `eval-edn!` / `eval-digest!` / `eval-signed!` (the four constrained-eval forms from slice 10); `:wat::kernel::stopped` added to the kernel primitives line. **New config field — `noise_floor`.** `(:wat::config::set-noise-floor! x)` setter + `(:wat::config::noise-floor)` accessor added alongside `set-global-seed!` / `global-seed`. Optional one-shot override; default derived at config pass as `5.0 / sqrt(dims)` — the 5σ substrate noise floor (`≈ 0.05` at d=10000, `≈ 0.156` at d=1024). **058-001 reconciliation.** atom-value's signature tightened from `:wat::std::atom-value (a :holon::HolonAST) -> :Option<T>` (the 2026-04-18 accepted form) to `:wat::core::atom-value (a :holon::HolonAST) -> :T` — structural field read per FOUNDATION line 44's framing, exact on Atom variants, runtime type error on non-Atom. The Option disappeared because the two retrieval regimes are clean-separated: structural reads return `:T` exact (the typed-box path); similarity measurements return `:f64` via `presence` (the composite-holon path). Namespace moved to `:wat::core::` — same tier as `:wat::algebra::Atom`; they are duals. **058-024 cross-ref — operational proof added.** The "Bind-on-Bind IS Unbind" claim from the Unbind rejection now cites the shipped presence proof as the operational demonstration: presence of `program-atom` in `Bind(k, program-atom)` measures below the 5σ floor; in `Bind(Bind(k,p), k)` measures above it. Also documents that a prior wat-rs experiment structurally reduced `Bind(Bind(x,y), x) → y` at AST construction — backed out because MAP self-inverse is a vector-level fact with non-zero-position caveats; lifting it to an AST rewrite overclaimed exactness. The shipped Bind always constructs the tree; presence reveals the dynamics. **`:wat::core::let*`** was already spec'd as a host-inherited Lisp form in FOUNDATION; this session's wat-rs work shipped the implementation (parallel `let` remains; `let*` threads cumulative scope so later bindings see earlier ones). **wat-rs substrate changes.** Added `EncodingCtx { vm, scalar, registry, config }` constructed at freeze from `Config` and attached to `SymbolTable`; runtime primitives requiring projection (`presence`, config accessors) reach it via dispatch. Registered `WatAST` in `AtomTypeRegistry` with a deterministic canonical-byte serializer so programs-as-atoms encode to stable vectors. `Value::wat__WatAST` accepted by `value_to_atom` (Atom wraps WatAST payloads). One commit in wat-rs; 372 tests green (was 353); zero warnings. CLI: `tests/wat_vm_cli.rs :: presence_proof_hello_world` asserts the three-line stdout exactly. | 058 |

| 2026-04-19 | **Typed constructors retrofit — vec / list / HashMap / HashSet all require explicit `:T` / `:(K,V)`.** Earlier constructors leaned on content-inference plus let-annotation fallback: `(:wat::core::vec 1 2 3)` parsed as `:Vec<i64>` from the elements, and empty literals pulled the element type from the surrounding annotation. `make-bounded-queue` already required an explicit `:T` first-arg because it has no content to infer from. The inconsistency was poison — "when does inference work?" depended on whether the form had content, which varies by call site and hides defects. Fixed: every data-struct constructor in wat stdlib now takes a type-keyword first, uniformly. Shipped forms: `(:wat::core::vec :T x1 x2 ...)`, `(:wat::core::list :T x1 x2 ...)`, `(:wat::std::HashMap :(K,V) k1 v1 k2 v2 ...)`, `(:wat::std::HashSet :T x1 x2 ...)`. The `:T` arg is read by the type checker and discarded at runtime (dispatch strips it). No shape depends on surrounding context. Same rule as `make-bounded-queue :T capacity` — resource constructors declare their type at the call site. Sweep updated: runtime.rs three eval_*_ctor fns, check.rs three infer_*_constructor fns (now require the leading type keyword), ~70 test strings across the codebase, no stdlib wat files affected (they use list combinators, not raw constructors). 448 tests (was 413 before the retrofit) continued green. | 058 |
| 2026-04-19 | **`:rust::` namespace — honest Rust-paths surface via `(:wat::core::use!)`.** The key architectural shift of this phase. wat becomes a Clojure-on-JVM pattern with Rust as the substrate: the kernel and algebra cores remain wat's own; any Rust crate (std, crossbeam_channel, lru, rusqlite, aya, …) surfaces under `:rust::<crate>::<path>` mirroring its real Rust path exactly. **Namespace principle.** `:wat::` and `:rust::` coexist as siblings. Primitives (`:i64`/`:f64`/`:bool`/`:String`/`:()`), type constructors (`:Option<T>`, `:Vec<T>`, `:(A,B,...)`, `:Result<T,E>`), algebra substrate (`:holon::HolonAST`), meta-AST (`:wat::WatAST`), kernel types (`:wat::kernel::HandlePool<T>`, `:wat::kernel::ProgramHandle<R>`), and the wat-stdlib smart constructors (`:wat::std::*`) stay wat-native; anything whose identity comes from a Rust crate moves to `:rust::`. Full qualification — no dropped `std::`, no magic. **`(:wat::core::use! :rust::path)` declaration.** Per-program (or per-file, later) set-insert at resolve time. Declares which Rust symbols the program intends to touch; the resolver validates each declaration against the build-time rust-deps registry and then enforces that every `:rust::X` call-site has a covering `use!`. Idempotent (multiple use!s of the same path collapse). Unknown symbol = startup error naming what's available; undeclared call = "not covered by any use!" diagnostic. **`Value::RustOpaque(Arc<RustOpaqueInner>)`** in wat-rs is the generic wire form for any `:rust::*` type that doesn't have a dedicated Value variant. Carries a `type_path: &'static str` and a `Box<dyn Any + Send + Sync>` payload. `FromWat`/`ToWat` marshaling traits (`src/rust_deps/marshal.rs`) drive the boundary — impls for `:i64`, `:f64`, `:bool`, `:String`, `:()`, `:Option<T>`, `:Vec<T>`, tuples 1-6, `:Result<T,E>`, and `Value` pass-through. **Build-time registry.** `RustDepsBuilder::with_wat_rs_defaults()` gives consumer crates wat-rs's pre-loaded shims (lru). Consumers add their own via a builder pattern before starting wat-vm: `rusqlite_shim::register(&mut deps); wat::run_with(deps);`. **FOUNDATION updates.** Adds `:rust::` to the reserved-prefix list and the Naming Discipline keyword-path section. The wat-stdlib wat/std/LocalCache.wat file is the canonical coexistence example: `(:wat::core::use! :rust::lru::LruCache)` at its top, three thin defines over `:rust::lru::LruCache::new/put/get`. Full architecture captured at `wat-rs/docs/arc/2026/04/002-rust-interop-macro/MACRO-DESIGN.md` and `.../NAMESPACE-PRINCIPLE.md`. User direction: "rust knows their home - we use their home. :wat:: and :rust:: coexist." | 058 |
| 2026-04-19 | **`#[wat_dispatch]` proc-macro — ergonomic shim generation.** `wat-macros` sibling crate in the workspace (`wat-rs/wat-macros/`). Proc-macro annotates a Rust `impl` block and generates per-method dispatch + scheme + a public `register()` fn that wires everything into `RustDepsBuilder`. Replaces ~100 LOC of hand-written boilerplate with one attribute per type. Authoring surface: `#[wat_dispatch(path = ":rust::crate::Type", scope = "...", type_params = "K,V")]`. **Three scope modes.** `shared` — plain `Arc<T>` payload, no guard, `&self` methods only (immutable-after-construction Rust values: query rows, cryptographic keys, configuration snapshots). `thread_owned` — `ThreadOwnedCell<T>` wrapping with ThreadId guard on every `&self`/`&mut self` access; cross-thread use errors with "thread crossed scope" on first op, zero Mutex (lru::LruCache, most stateful handles). `owned_move` — `OwnedMoveCell<T>` with `AtomicBool` gate; first `take()` wins, subsequent return "handle already consumed" (prepared-statement bindings, one-shot tokens). **`type_params` attribute** — phantom-generic support. `type_params = "K,V"` emits Self-returning methods as `TypeExpr::Parametric { head, args: [fresh_var, fresh_var] }` so wat annotations like `:rust::lru::LruCache<K,V>` unify instead of failing a bare `Path` match. **Return marshaling.** Primitives go through `ToWat`; Self-returns wrap in the scope-appropriate cell then `make_rust_opaque`; `Option`/`Vec`/`Tuple`/`Result` recurse. **Self receivers.** `&self` downcasts via `downcast_ref_opaque`; `&mut self` under `thread_owned` calls `with_mut`; `self` under `owned_move` calls `.take()` to consume. `&mut self` under `shared` errors at macro-expand time pointing at `thread_owned` as the correct scope. **Proof landing.** The lru shim was regenerated via this macro (task #195) — hand-written `LruCacheCell` + `dispatch_*` + `scheme_*` fns (~250 lines total) replaced with a `WatLruCache` newtype + one `#[wat_dispatch]` attribute + a trivial `register()` forwarder. The full LocalCache integration test stayed green. Design record: `wat-rs/docs/arc/2026/04/002-rust-interop-macro/MACRO-DESIGN.md`. | 058 |
| 2026-04-19 | **`:Result<T,E>` built-in sum type + `(Ok v)` / `(Err e)` + match.** Second tagged sum alongside `:Option<T>`. Rust-dep shims that wrap fallible crates (rusqlite.Connection.open → `Result<Connection, Error>`, etc.) surface their errors as first-class wat values. `Value::Result(Arc<std::result::Result<Value, Value>>)` variant in wat-rs. **Constructors** — `(Ok expr)` and `(Err expr)` are bare-identifier reserved symbols (arity 1 each), dispatched from `eval_list` the same way `(Some expr)` is. Each infers its operand type from the arg and returns `:Result<T,E>` with the other side as a fresh type var; unification against the declared return or match arms binds the free var. **Match integration.** `(:wat::core::match scrutinee (Ok v) body (Err e) body)` dispatches by detecting the arm patterns — `Ok`/`Err` in the head identify Result-shaped matches, `Some`/`:None` identify Option. Exhaustiveness fires per-shape: Result needs `(Ok _)` + `(Err _)` or wildcard; Option unchanged. The pattern walker handles both cases without conflating them; `:None` on a Result scrutinee errors cleanly, as does `(Ok _)` on an Option scrutinee. **Marshaling.** `FromWat`/`ToWat` for `std::result::Result<T: {ToWat|FromWat}, E: same>` in `src/rust_deps/marshal.rs`. Recursive — nested `Option<Result<T,E>>` / `Vec<Result<T,E>>` / `Result<Option<T>, Vec<E>>` all round-trip. **Macro codegen** maps Rust `Result<T, E>` in method arg/return position to `TypeExpr::Parametric { head: "Result", args: [<T>, <E>] }`. The forcing function: rusqlite will return Results from most methods; having the wat-level type in place before the migration starts means the shim generates honestly without "errors panic at the boundary" hacks. | 058 |
| 2026-04-19 | **`:wat::std::LocalCache<K,V>` + `:wat::std::program::Cache<K,V>` — L1/L2 caching stack as pure wat source.** Completes 058 Step 5 (FOUNDATION lines 1525-1565). **LocalCache — the L1.** `wat/std/LocalCache.wat` ships three thin define wrappers over `:rust::lru::LruCache<K,V>`: `::new capacity`, `::put cache k v`, `::get cache k`. Single-thread-owned (inherits the `ThreadOwnedCell` guard from the rust-dep). No pipe, no thread, no queue — fastest memoization possible. User writes `(:wat::std::LocalCache::new 16)` with a let-annotation; K,V propagate through the define's type params. **Cache — the L2.** `wat/std/program/Cache.wat` is a spawnable wat program that owns its own LocalCache behind a select loop. Clients send tagged-tuple requests through per-client queues; each request carries its own reply-to sender so the driver routes responses without a separate sender-index map. Protocol: `Body<K,V> = (tag :i64, key :K, put-val :Option<V>)`, `Request = (body, reply-to)`, `Response = :Option<V>`. Driver entry `Cache/loop capacity req-rxs` allocates the LocalCache **inside the driver thread** (load-bearing — see next entry) then delegates to `Cache/loop-step` for the recursion. **Client helpers** `Cache/get` / `Cache/put` are thin wrappers — client creates its reply channel once at setup and reuses for every request. **Setup** `(Cache capacity count)` returns `(HandlePool<Sender<Req>>, ProgramHandle<()>)` matching Console's shutdown contract — drop all senders to get disconnect, join driver to flush. **Thread-ownership principle** (captured in `wat-rs/docs/arc/2026/04/001-caching-stack/DEADLOCK-POSTMORTEM.md`). Thread-owned values (backed by `ThreadOwnedCell<T>`) must be constructed on the thread that will own them. Creating a LocalCache on one thread and passing it across a `spawn` tripped the guard on the driver's first `get`/`put`, panicked the driver thread silently, and wedged the client waiting on its reply-rx. Detected via a stderr-checkpoint probe (T1 "about to put" printed, T2 "put acked" never did). Fix: pass `capacity: i64` not a pre-built cache; the driver's entry allocates. Rust's type system allows the Arc to cross threads, but the runtime guard fires on first use — principle now bound into the prose. Will recur for rusqlite's Connection and other `thread_owned` shims; we know the shape. **Full-pipeline CLI test** — `tests/wat_vm_cache.rs :: cache_put_then_get_round_trip` — spawns wat-vm on a program that does Console setup, Cache setup, put "answer" 42, get "answer", matches to print "hit" on stdout. Nested let* layering ensures driver flush before wat-vm exits. 5-for-5 deterministic after the fix. | 058 |
| 2026-04-19 | **Honesty sweep — all Rust-sourced types fully-qualified under `:rust::`.** Pre-macro-adoption naming cleanup. Earlier forms had `:io::Stdin` / `:io::Stdout` / `:io::Stderr` (partial Rust paths), `:crossbeam_channel::Sender<T>` / `:Receiver<T>` (crate name only, no `:rust::` prefix), `:HashMap<K,V>` / `:HashSet<T>` (unqualified wat type annotations). Renames: `:io::*` → `:rust::std::io::*`; `:crossbeam_channel::*` → `:rust::crossbeam_channel::*`; `:HashMap<K,V>` annotations → `:rust::std::collections::HashMap<K,V>`; `:HashSet<T>` annotations → `:rust::std::collections::HashSet<T>`. Full Rust paths — no dropped `std::`, no short forms, no magic. Matches the lru precedent locked in the 2026-04-19 `:rust::` slice. **What did NOT rename.** Constructor forms stay as wat-stdlib smart constructors: `(:wat::std::HashMap :(K,V) k1 v1 ...)` produces a `:rust::std::collections::HashMap<K,V>` value — the constructor is wat-level, the type is Rust-level. Same split Console has: `:wat::std::program::Console` produces the program, but the stdio handles it receives are `:rust::std::io::Stdout`/`::Stderr`. Kernel primitives `:wat::kernel::*` stay unchanged (they're wat-level operations that happen to operate on Rust-backed handles). `:wat::io::write` / `:wat::io::read-line` are wat primitives, not Rust imports. **Sweep.** All internal Parametric head strings in the scheme registrations (runtime.rs, check.rs) updated. `Value::type_name()` returns the full path strings now. `:user::main` signature in wat-vm binary expects `:rust::std::io::Stdin`/`::Stdout`/`::Stderr`. All Console.wat type annotations, all embedded wat test strings, all dispatch error messages. **FOUNDATION updates.** The Naming Discipline section gains `:rust::...` as a sibling of `:wat::std::...` with the "fully qualified, mirrors real Rust" rule. LocalCache.wat + Cache.wat are cited as the canonical coexistence examples. `wat-rs/docs/arc/2026/04/002-rust-interop-macro/NAMESPACE-PRINCIPLE.md` is the durable reference. User direction: "rust knows their home - we use their home. :wat:: and :rust:: coexist." | 058 |

---

| 2026-04-19 | **Capacity-guard arc — `:wat::core::try`, struct runtime, Bundle returns `:Result<holon::HolonAST, :wat::algebra::CapacityExceeded>`.** Three slices landed together in wat-rs during session 2026-04-19, forming the complete forcing-function chain for Kanerva-capacity enforcement. All three introduce the **INSCRIPTION** status (fifth status class alongside ACCEPTED / REJECTED / DEFERRED / AUDITED) — specification written AFTER implementation shipped, capturing the built contract for the record. See the new `058-033-try/PROPOSAL.md` and the INSCRIPTION amendments on `058-003-bundle-list-signature/PROPOSAL.md` and `058-030-types/PROPOSAL.md` for full details. **(1) `:wat::core::try` form.** New language-core form (wat-rs commit `bc0362e`). Semantics: `(try <result-expr>)` unwraps `Ok v` to `v` or short-circuits the enclosing Result-returning function/lambda with `(Err e)`. NOT try/catch — no handler block; the error propagates one function-boundary at a time, each function choosing to `try` (propagate) or `match` (handle). Strict E equality at check (no `From`-trait auto-conversion, per 2026-04-19 "wat is strongly typed — Rust meets Haskell meets Agda"). Implemented via `RuntimeError::TryPropagate(Value)` internal control-flow signal caught at `apply_function`. Scoped to the innermost fn/lambda, matching Rust's `?`-operator scoping. **(2) Struct runtime pinned.** `(:wat::core::struct ...)` declarations now produce ACTUAL runtime values (commit `0e7309a`). New `Value::Struct(Arc<StructValue>)` variant with `type_name` + positional `fields: Vec<Value>`. **Construction** via auto-generated `<struct-path>/new` keyword paths — positional, one arg per field, field order = declaration order. **Field access** via auto-generated `<struct-path>/<field-name>` keyword paths — one accessor per field, type `:fn(<struct>) -> <field-type>`. Users never write the constructors or accessors; they appear when the struct's declaration lands and users invoke them by full keyword path. The `::` vs `/` convention holds: `::` navigates namespace-like paths, `/` attaches methods to the thing at the end. No struct-pattern match forms, no named-argument construction, no bare-keyword `(:field e)` dispatch — deferred. Canonical style uses `let` at both construction and extraction sites to name positional values by field identity. Two internal primitives back the auto-generation: `:wat::core::struct-new <type-name> v1 v2 ...` and `:wat::core::struct-field <struct> <index>`. **Self-trust.** `TypeEnv::with_builtins()` seeds wat-rs's own `:wat::*` type declarations via privileged `register_builtin` (bypasses the reserved-prefix gate that protects wat PROGRAMS from claiming `:wat::*`). User source still flows through `register` where the gate applies. Mirrors `CheckEnv::with_builtins` for built-in function schemes. First built-in: `:wat::algebra::CapacityExceeded`. **(3) Bundle returns Result.** `:wat::algebra::Bundle` signature becomes `:Vec<holon::HolonAST> -> :Result<holon::HolonAST, :wat::algebra::CapacityExceeded>` under every mode (commit `e63e428`). `:error` populates Err with `cost` and `budget` fields (i64); `:silent` and `:warn` always Ok (substrate produces degraded vector); `:warn` adds stderr diagnostic; `:abort` panics. Cascades through every stdlib macro that expands to Bundle — Ngram, Bigram, Trigram inherit the Result wrap. **Budget formula pinned.** `budget = floor(sqrt(dims))` — the `K` factor in Kanerva's `d / (2·ln K)` drops away because the wat algebra is AST-primary (no codebook to distinguish against). At d=10_000 → 100, matching FOUNDATION's informal "~100 at d=10k" empirical number exactly. At d=4_096 → 64; at d=1_024 → 32. Matches prior-art `holon-lab-trading/src/encoding/rhythm.rs:58` trimming convention. **`Value::Result` round-tripped through the whole chain.** Authors writing Result-returning functions use `try` for clean propagation without the `((Err e) (Err e))` ceremony that every level would otherwise require. 490 lib + 9 bundle-capacity + 9 struct + 13 try + full integration suite green at commit time. Zero clippy. | 058-003, 058-030, 058-033 |
| 2026-04-20 | **Streams + TCO arc — `:wat::std::stream`, `reduce` single normalization pass, variadic defmacro, spawn-accepts-lambdas, send symmetric with recv, conj, pipeline REJECTED.** Two arc-completion slices in wat-rs (arc 003 TCO + arc 004 lazy pipelines) shipped a connected set of substrate additions plus one rejection. Each gets an INSCRIPTION amendment on its home proposal; a new `058-034-stream-stdlib/PROPOSAL.md` covers the combinator family. **(1) TCO in the evaluator.** `RuntimeError::TailCall { func, args }` internal signal; `eval_tail` sibling of `eval` with tail-aware helpers for `if` / `match` / `let*` branches; `apply_function` wraps its body in a trampoline loop. Stage 1 covers named defines via `sym.functions`. Stage 2 adds three detection paths for lambda-valued tail calls: keyword head → named define, bare-symbol head → lambda value in env, inline-lambda-literal head → eval then extract. `Console/loop`, `Cache/loop-step`, and every future `gen_server`-shaped driver run in constant Rust stack. Tests: 11 cases including self-recursion at 1M depth, mutual recursion, lambda closure survives thread boundary. wat-rs commits `32e918b` + `9089867`. **(2) `:wat::kernel::send` returns `:Option<()>` — symmetric with `recv`.** One endpoint-disconnect shape across every channel primitive; producers match on send to exit cleanly when the consumer drops. Earlier drafts proposed a separate `:wat::kernel::send-or-stop` primitive; retired in favor of making `send` itself Option-returning — one primitive, one rule. Commit `df3ca03`. **(3) Typealias expansion at unification + the `reduce` pass.** 058-030 specified typealiases as structurally identical to their expansion; the implementation parsed and registered them but `unify` consumed types structurally without peeling aliases. Shipped `expand_alias` wired into `unify`'s prologue (commit `7f90760`), then recognized the deeper flaw — two half-passes (`apply_subst` for Vars + `expand_alias` for aliases) that every shape-inspection site had to chain manually — and added `reduce` as the single normalization pass (commit `b10f002`). Mature type systems have one normalization pass; wat-rs now has it. Shape-inspection sites (`infer_positional_accessor`, `infer_drop`, `infer_get`, `infer_try`, `infer_spawn`) all call `reduce`; error-display sites keep `apply_subst` so messages preserve the alias name the user wrote. **Cycle detection at registration** rejects `(typealias :A :B) (typealias :B :A)` before insertion; `reduce` always terminates. **`register_stdlib_types` privileged path** mirrors `register_stdlib_defmacros` — stdlib wat files can declare typealiases under `:wat::*` without fighting the reserved-prefix gate. First uses: `:wat::std::LocalCache<K,V>` (alias for `:rust::lru::LruCache<K,V>`), `:wat::std::stream::Stream<T>` / `Producer<T>`, `:wat::std::program::Cache::Request<K,V>` / `ReplyTx<V>` / `ReqTx<K,V>` / `ReqRx<K,V>` / `Body<K,V>`, `:wat::std::program::Console::Message` / `Tx` / `Rx`. **(4) `:wat::kernel::spawn` accepts lambda values.** First argument may be a keyword-path literal (classic named-define path) OR any expression evaluating to a `:wat::core::lambda` value. Both produce the same `Arc<Function>`; the trampoline handles both via `closed_env.clone().unwrap_or_default()`. Closes the asymmetry with `apply_value`; enables stream combinators to spawn user-provided lambdas directly. FOUNDATION's "Programs are userland" conformance contract relaxes from "named by keyword path" to "any `Arc<Function>` value." Commit `5fbdb87`. **(5) Variadic `defmacro` — `&` rest-param support.** `MacroDef.rest_param: Option<String>`. Syntax: `(:name (p1 :AST<T1>) & (rest :AST<Vec<R>>) -> :AST<R>)`. Fixed params bind positionally; remaining args wrap in `WatAST::List` bound to rest-name; existing `,@rest-name` unquote-splicing drops elements into the template. Malformed signatures (double `&`, `&` without binder) rejected at registration. 058-031 amendment. Commit `c9612be`. **(6) `:wat::core::conj` — immutable Vec append.** Scheme `∀T. Vec<T> × T -> Vec<T>`. Clojure-style name. Forcing function: `:wat::std::stream::chunks`'s accumulator. 058-026 amendment. **(7) `:wat::std::stream::*` — seven combinators + two typealiases.** New proposal 058-034. `Stream<T>` = `(Receiver<T>, ProgramHandle<()>)` typealias. `spawn-producer` (source); `map` / `filter` / `chunks` (intermediate, each spawns one worker); `for-each` / `collect` / `fold` (terminal, drive pipeline + join handle). 11 end-to-end tests (`tests/wat_stream.rs`). Composition via explicit `let*` chain — the idiomatic shape. Commits `b5c5962` (slice B) + `16ddc7b` (slice C). **(8) Pipeline composer — REJECTED.** The design doc sketched `(pipeline src (map :f) (chunks 50) sink)` as a one-liner ergonomic target. After walking five implementation approaches (variadic splice, AST-rewrite stages, recursive macro expansion, homogeneous Vec of stage lambdas, placeholder substitution), none cleanly expressed auto-threading. The sharper reading: `let*` already IS the pipeline. The "boilerplate" a composer would eliminate — per-stage type annotations, named bindings, explicit threading — was information, not ceremony. Hiding it trades wat's typed-binding discipline (058-030) for conciseness; wat has consistently picked honesty. Rejected on earned-its-slot grounds, not implementation cost. **Two cross-session lessons captured** with numbered procedures: **"absence is signal"** (when a feature expected in a mature language isn't there, ask why before patching — the reduce case) and **"verbose is honest"** (before adding a new ergonomic form, ask what it ELIMINATES — the pipeline case). Both directions of absence: sometimes close the gap, sometimes don't. Memory entries `feedback_absence_is_signal.md` + `feedback_verbose_is_honest.md`; arc inscriptions at `wat-rs/docs/arc/2026/04/003-tail-call-optimization/INSCRIPTION.md` + `wat-rs/docs/arc/2026/04/004-lazy-sequences-and-pipelines/INSCRIPTION.md`. 639 tests green at commit time; clippy clean. | 058-026, 058-030, 058-031, 058-034 |

| 2026-04-21 | **Arc 013 — external wat crates; LocalCache externalized into `wat-lru` sibling crate.** wat-rs's "baked stdlib" tier held `wat/std/LocalCache.wat` + `wat/std/service/Cache.wat` + their `#[wat_dispatch]` shim for `:rust::lru::LruCache` + the `lru = "0.12"` Cargo dep. Arc 013's forcing question: can third parties publish wat code + Rust shims that consumer binaries compose through Cargo dep resolution? LRU was the proof. Six slices, all landed 2026-04-21 same-day. **(1) Workspace layout.** `crates/wat-lru/` added as third workspace member alongside `wat-macros`. Skeleton crate; `stdlib_sources()` + `register()` stubs. **(2) Harness accepts dep sources.** `Harness::from_source_with_deps(src, dep_sources, dep_registrars)` + underlying `startup_from_source_with_deps`. Dep sources parse alongside user forms; dep registrars build the `RustDepsBuilder` before install. **(3) `wat::main!` proc-macro + `wat::compose_and_run` plain fn.** `wat::main! { source: include_str!("program.wat"), deps: [wat_lru] }` expands to `fn main() -> Result<(), wat::HarnessError>` calling `compose_and_run` with real OS stdio. The plain function underneath serves callers needing per-call control. **(4a) `dep_registrars` substrate.** The two-part external-crate contract: `pub fn stdlib_sources() -> &'static [wat::stdlib::StdlibFile]` + `pub fn register(&mut wat::rust_deps::RustDepsBuilder)`. Both substrate entries (Harness + compose_and_run) + the macro emit register-path calls alongside stdlib_sources calls. `with_wat_rs_defaults()` stays as the base — preserving baked defaults at slice-4a time; emptied in 4b. **(4b) LocalCache + shim motion, paused.** Code-motion landed: `wat/std/LocalCache.wat` + `wat/std/service/Cache.wat` retired → `crates/wat-lru/wat/lru.wat` + `service.wat` with every path repathed from `:wat::std::LocalCache` / `:wat::std::service::Cache` to `:user::wat::std::lru::LocalCache` / `:user::wat::std::lru::CacheService`. `src/rust_deps/lru.rs` retired → `crates/wat-lru/src/shim.rs` (imports shifted `crate::*` → `wat::*`; `hashmap_key` lifted `pub(crate)` → `pub`). `lru = "0.12"` moved from wat-rs root `Cargo.toml` to `crates/wat-lru/Cargo.toml` — **wat-rs root has zero dep on wat-lru**, proving transitive composition. `STDLIB_FILES` in `stdlib.rs` dropped the retired entries. Nine internal wat-rs tests retired (check × 4, resolve × 3, runtime × 4, freeze × 1 + probe test); equivalent coverage landed in `crates/wat-lru/tests/wat_lru_tests.rs`. Two of those tests `#[ignore]`'d pending arc 014's `:wat::core::i64::to-string` — a cave-quest split that cut arc 014 mid-execution. **Cave-quest discipline** (precedent-setting): rather than paper tests with literal-only branches, pause the open slice, name the missing key, cut its own arc, return. Arc 014 shipped its three slices + INSCRIPTION same day; arc 013 slice 4b resumed and closed. **(5) Reference binary `examples/with-lru/`.** Fourth workspace member. `src/main.rs` is one `wat::main! { source: include_str!("program.wat"), deps: [wat_lru] }` invocation. `src/program.wat` does `LocalCache::new` / `::put` / `::get` round-trip, prints `hit`. `tests/smoke.rs` spawns the built binary via `CARGO_BIN_EXE_with-lru-example`, asserts exit 0 + stdout `hit`. The walkable consumer shape. **(6) `CONVENTIONS.md` namespace table extended.** `:user::*` sub-tree carved into three claim-by-convention shelves: `:user::wat::std::<crate>::*` (community stdlib tier), `:user::<org>::<name>::*` (community general), `:user::<app-tree>::*` (user program). Convergence table with Cargo / npm / Clojure / Go names the shape. **`use!` semantics sharpened.** `(:wat::core::use! :rust::X)` declares intent to consume an *external* `#[wat_dispatch]`'d Rust crate. Substrate-provided types (`:rust::crossbeam_channel::*` via `:wat::kernel::make-bounded-queue`) do NOT need `use!`; historical cosmetic declarations removed from `wat/std/stream.wat`. Only genuine external crates — like `wat-lru`'s own `(:wat::core::use! :rust::lru::LruCache)` — carry the form. **Migration impact on 058 spec.** `:wat::std::LocalCache<K,V>` path RETIRED. Callers rename to `:user::wat::std::lru::LocalCache<K,V>` or break. `wat/std/LocalCache.wat` no longer ships in wat-rs; lives in `crates/wat-lru/wat/lru.wat`. The 2026-04-19 "L1/L2 caching stack" changelog entry's path names are historical — still correct for the state at that time. LocalCache's surface continues unchanged, just relocated. **Chapter 18's "wat is the language, Rust is the substrate" becomes operational at the ecosystem tier.** Third parties can publish wat crates. Consumers compose multiple wat crates + their own program into one honest binary via `wat::main!`. Arc 013 + arc 014 shipped 17 commits across two repos. Full INSCRIPTION at `wat-rs/docs/arc/2026/04/013-external-wat-crates/INSCRIPTION.md`. | 058 |

| 2026-04-21 | **Arcs 014 + 015 — core scalar conversions + wat test for consumers; publisher/consumer template settles.** Two arcs closing the same day as arc 013, both cut from paused slices (arc 014 cut mid-013-slice-4b; arc 015 cut its own mid-slice-3a). **Arc 014 (shipped).** Eight core primitives at `:wat::core::<source>::to-<target>` — `i64::to-string`, `i64::to-f64`, `f64::to-string`, `f64::to-i64`, `string::to-i64`, `string::to-f64`, `bool::to-string`, `string::to-bool`. Infallible conversions return the target type; fallible conversions (parse failures, out-of-range, NaN/±∞) return `:Option<T>`. No implicit coercion at arithmetic / comparison sites; every conversion is explicit at the call site. Lisp-fundamentals tier per the zero-drift-note precedent (no separate 058-NNN sub-proposal). Enumerated in the reserved-prefix list alongside `:wat::core::i64::+`. **Arc 015 (shipped).** Consumer wat tests discover + run + compose external crates via `wat::test_suite!` proc-macro. Key changes: `wat::test_runner::run_tests_from_dir(path, dep_sources, dep_registrars)` library module; `wat::test_suite! { path: "wat-tests", deps: [wat_lru] }` macro emits `#[test] fn wat_suite()`; global `wat::source::install_dep_sources` OnceLock (symmetric with `wat::rust_deps::install`) — dep sources install process-globally so every freeze (main, test, sandbox via `run-sandboxed-ast`, fork child via `run-hermetic-ast`) transparently sees dep surface through `stdlib_forms`; `startup_from_source_with_deps` RETIRED (callers install then call `startup_from_source`); dep sources become stdlib-tier (bypass resolve's reserved-prefix gate — gate still applies to USER source). **User-facing rename (arc 015 slice 4).** `wat::stdlib::StdlibFile` → `wat::WatSource` (re-exported at crate root). `wat::stdlib::install_dep_sources` → `wat::source::install_dep_sources`. External-crate contract fn `pub fn stdlib_sources()` → `pub fn wat_sources()`. No back-compat shims — pre-publish rename. Community authors ship **source** (not "stdlib"); names now reflect that framing. **Consumer template settles.** Two Rust files per consumer app (`src/main.rs` with `wat::main!`, `tests/tests.rs` with `wat::test_suite!`), optional third (`src/shim.rs` with `#[wat_dispatch]` for app-specific Rust types), plus `.wat` files for everything else. Cargo handles build, run, test. Arcs 013 + 014 + 015 together deliver Chapter 18's *"wat is the language, Rust is the substrate"* at the ecosystem tier. INSCRIPTIONs at `wat-rs/docs/arc/2026/04/{013-external-wat-crates,014-core-scalar-conversions,015-wat-test-for-consumers}/INSCRIPTION.md`. | 058 |

| 2026-04-21 | **Arc 016 — failure location + frames populate; Rust-styled wat-located output.** Arc 007 shipped `:wat::kernel::Failure.location :Option<Location>` and `.frames :Vec<Frame>` as substrate and flagged both as future work. Six months passed; the fields stayed empty. When a wat test failed, the author saw `failure: assert-eq failed` with no file, line, or column — grep the source, hunt the assertion. Arc 016 closes that gap. Every `WatAST` node gains a `Span { file, line, col }` from parse (structural-transparent `PartialEq` + no-op `Hash` — AST identity stays position-independent so canonical-EDN hashing is unaffected). A thread-local call stack populates at `apply_function` via RAII guard; tail calls replace the top frame in place (constant stack depth for recursion, matching the semantic). A `std::panic::set_hook` renders `cargo test`-shaped output to stderr, gated on `RUST_BACKTRACE` for the `stack backtrace:` block — same phrasing, same `note: run with ...` hint, same numbered frame lines. Runtime-initiated frames (test-runner entry, `compose_and_run` entry, internal iteration in `map`/`foldl`/`fold`/`filter`) carry their Rust source location via `rust_caller_span!()` (`file!()` / `line!()` / `column!()`) — same convention Rust uses for stdlib frames showing `/rustc/.../library/core/...`. Every frame has a real `file:line:col`; user frames point into `.wat`, runtime frames into `wat-rs/src/*.rs`, honest about the layer boundary. Macro expansion inherits call-site span (Racket sets-of-scopes pattern) so `deftest`-wrapped assertions report at the user's invocation, not at `wat/std/test.wat`. **Rust-first, lived.** No new env var. No new format. The trading lab's `.wat` tests will fail honestly — `:project::market::test-whatever at holon-lab-trading/wat-tests/…:line:col` in the failure header, no context switch. Four slices + one polish pass, all 2026-04-21. Full INSCRIPTION at `wat-rs/docs/arc/2026/04/016-failure-location-and-frames/INSCRIPTION.md`. | 058 |

| 2026-04-22 | **Arc 017 — loader option for consumer macros; multi-file consumer programs + library-vs-entry discipline.** `wat::main!` hard-wired `InMemoryLoader` via `compose_and_run`; consumer binaries with multi-file wat programs — the trading lab being the first real case — hit a wall because `(:wat::core::load! :wat::load::file-path "path")` from inside the entry source returned `NotFound`. Builder framing: *"we don't back down from a fight.. we must be able to support loads being called recursively... and we only need the entry point to call the dims and capacity mode — this is a binary vs lib distinction."* Arc 017 closes it at the substrate. **Both consumer macros gain optional `loader: "<path>"`.** Expansion emits `concat!(env!("CARGO_MANIFEST_DIR"), "/", <lit>)` so the path is crate-root-relative (stable under `cargo run -p <crate>` from workspace root), then constructs a `ScopedLoader` and threads it into the loader-capable path. Absence preserves the pre-017 default (InMemoryLoader for main, FsLoader for tests). **`compose_and_run_with_loader` / `run_and_assert_with_loader` / `run_tests_from_dir_with_loader`** ship as public siblings of the existing entries; the old functions become one-line wrappers passing the previous defaults. **ScopedLoader substrate fix**: base-less relative paths now resolve against the scope root instead of falling through to cwd (critical for `include_str!`-sourced entries which carry no canonical path). **Library-vs-entry discipline** (the builder's "binary vs lib distinction" codified in test_runner): a `.wat` file in the test directory is an ENTRY iff it has top-level `(:wat::config::set-*!)` forms; files without setters are LIBRARIES and `test_runner` silently skips freezing them standalone. They remain `(load!)`-able from entries. Matches the rule `reject_setters_in_loaded` already enforced on the load side (setters belong only to entries). **Recursive loads flatten.** program.wat → helper.wat → deeper.wat at arbitrary depth; every loaded-file's defines land in the entry's frozen world. Proof: new `wat-rs/examples/with-loader/` workspace member walks the shape end-to-end — `wat::main! { loader: "wat" }` binary with a recursive chain, `wat::test_suite! { loader: "wat-tests" }` suite with an entry test + a library helper. **Clippy sweep** landed alongside — four pre-existing warnings fixed, zero across workspace. **Cave-quest from Phase 0.** The trading lab rewrite's first slice opens when arc 017 closes; arc 017 opens because the lab's first slice would have hit the wall. Three slices shipped same day. Full INSCRIPTION at `wat-rs/docs/arc/2026/04/017-loader-option-for-consumer-macros/INSCRIPTION.md`. | 058 |

| 2026-04-22 | **Arc 018 — opinionated defaults + `wat::test!` rename; the consumer story collapses to two one-line macros.** Arc 017 gave consumers multi-file wat via `loader:`. The minimal invocation still required four lines (`source:` + `deps:` + `loader:`); one session later, arc 018 ships the defaults that make it one. Builder framing: *"wat::main! { deps: [...] } is the ideal expression... with wat/main.wat and wat/**/*.wat being where we load files from... we have opinionated defaults and users can express overrides if they want for the main! and test! forms."* **Opinionated defaults on `wat::main!`.** `source:` omitted → `include_str!(concat!(env!("CARGO_MANIFEST_DIR"), "/wat/main.wat"))`. `loader:` omitted AND source omitted → `"wat"` (ScopedLoader). `loader:` omitted AND source explicit → no loader (InMemoryLoader — back-compat for single-file consumers). `wat::main! {}` with zero args is the maximally-opinionated form; `wat::main! { deps: [wat_lru] }` is the common case. Parser restructured so all three keys are optional and accepted in any order. **`wat::test_suite!` → `wat::test!` rename**, pre-publish clean, no shim. Symmetric with `main!`. Same default rule applied: `path:` absent → `"wat-tests"`; `loader:` absent AND path absent → `"wat-tests"` (ScopedLoader); `loader:` absent AND path explicit → no loader (FsLoader — back-compat). **Walkable-reference migrations**: `examples/with-lru/` moves `src/program.wat` → `wat/main.wat`, collapses `src/main.rs` to `wat::main! { deps: [wat_lru] }`. `examples/with-loader/` moves `src/program.wat` → `wat/main.wat`, collapses `src/main.rs` to `wat::main! {}` (zero args — defaults handle everything), renames `tests/wat_suite.rs` → `tests/test.rs` with `wat::test! {}`. wat-lru's own `tests/wat_suite.rs` → `tests/test.rs` with `wat::test! { deps: [wat_lru] }`. **CONVENTIONS.md gains "Consumer layout"** — the `src/main.rs` + `tests/test.rs` + `wat/` + `wat-tests/` directory shape is named as the default; `tests/test.rs` is recommended (not Cargo-enforced). USER-GUIDE rewritten to lead with the minimal form; the verbose shape stays as the override. **Trading lab Phase 0 scaffold** opens as `wat::main! { deps: [] }` + `wat::test! {}` + `wat/main.wat` + `wat-tests/**/*.wat`. Everything past those two Rust files is wat. Full INSCRIPTION at `wat-rs/docs/arc/2026/04/018-opinionated-defaults-and-test-rename/INSCRIPTION.md`. | 058 |

| 2026-04-22 | **Arc 019 — `:wat::core::f64::round` primitive.** Cave-quest mid-Phase-3 of the trading lab rewrite — vocab encoding helpers need two-decimal rounding for cache-key stability (archive's `round_to(v, 2)` convention throughout). wat shipped `:wat::core::f64::+` / `-` / `*` / `/` arithmetic + `f64::to-i64` / `f64::to-string` conversions but no `round`. Implementing round-half-away-from-zero via truncation + sign-bias tricks in wat was awkward and wrong-semantic at negative-value edge cases — the substrate deserved the primitive. Shape: `(:wat::core::f64::round v digits) :f64 × :i64 -> :f64`. Semantic: `(v * 10^digits).round() / 10^digits`. Builder refined the scope mid-slice twice — unary `round(v)` → two-arg `round(v, digits)` (generalized from the start); then negative-digits rejected at runtime as `MalformedForm` matching the builder's *"that's like a divide by zero to me"* framing (not a technical limit; a deliberate cut — reopens if a real caller surfaces). NaN / ±∞ pass through under IEEE 754. Five unit tests (zero / two / three digits, negative-digits rejection, arity mismatch); zero clippy warnings. Lab's `wat/encoding/round.wat` (Phase 3.1) simplifies from the sign-bias trick to a one-line wrap of the new primitive. Cave-quest discipline stays standing practice — 017 (loader), 018 (defaults), 019 (round), each pausing downstream for the substrate to catch up. Full INSCRIPTION at `wat-rs/docs/arc/2026/04/019-f64-round-primitive/INSCRIPTION.md`. | 058 |

| 2026-04-22 | **Arc 020 — `:wat::core::assoc` primitive; HashMap values-up put.** Fourth cave-quest in the Phase-3 sequence. Lab's `scaled-linear` helper threads a `HashMap<String, ScaleTracker>` through encoding calls values-up; wat had HashMap construction + get but no way to return a new HashMap with an entry added. Builder framing: *"hashmap without put means its defective"*, then *"did we just create assoc?.. yeah.. assoc feels good"* when the Clojure-named primitive surfaced. Shape: `(:wat::core::assoc <map> <key> <value>) ∀K,V. :HashMap<K,V> × K × V -> :HashMap<K,V>`. Runtime clones the map and inserts — values-up, zero mutation. Pattern-breakage catch mid-slice: first draft shipped `:wat::std::HashMap::put` (type-scoped method), but HashMap's existing surface (`:wat::std::HashMap` constructor, `:wat::std::get`) uses generic verbs not type-scoped methods. Renamed to Clojure's `assoc` (generic verb, matches the idiom) before commit. Full INSCRIPTION at `wat-rs/docs/arc/2026/04/020-assoc/INSCRIPTION.md`. | 058 |

| 2026-04-22 | **Arcs 021 + 022 — namespace audits; HashMap family to `:wat::core::*`, holon surface fully unified under `:wat::holon::*` (two slices).** Two audits cut from the same builder-initiated sweep. Mechanical renames across every file type, zero semantic changes. **Arc 021.** HashMap / HashSet / get / contains? had drifted to `:wat::std::*` despite reaching Rust-internal state (HashMap buckets) — they belong at `:wat::core::*` per CONVENTIONS's stated rubric (core = can't-be-written-in-wat, std = expressible-in-wat). Rename: `:wat::std::HashMap` / `HashSet` / `get` / `contains?` → `:wat::core::HashMap` / `HashSet` / `get` / `contains?`. CONVENTIONS.md grew a named "Core vs stdlib rubric (arc 021)" subsection. **Arc 022, slice 1.** Two parallel forms held holon-core surface: `:holon::HolonAST` (the universal value type, at the wat level, under a private namespace) and `:wat::algebra::*` (the primitives that produce it + measurements that read it). Renames: `:holon::HolonAST` → `:wat::holon::HolonAST`; `:wat::algebra::{Atom, Bind, Bundle, Blend, Permute, Thermometer, cosine, dot, presence?, CapacityExceeded}` → `:wat::holon::*`. The `:wat::algebra::*` namespace retires entirely. **Arc 022, slice 2.** Builder's follow-up directive: *"wat/std/&lt;holon-thing&gt;.wat → wat/holon/*.wat"*. The ten wat-written algebra idioms (Amplify, Bigram, Circular, Log, Ngram, Project, Reject, Sequential, Subtract, Trigram) move from `:wat::std::*` → `:wat::holon::*`; files move `wat/std/*.wat` → `wat/holon/*.wat` with matching `wat-tests/std/*.wat` → `wat-tests/holon/*.wat`. File path now matches namespace. Genuine stdlib stays at `:wat::std::*` — stream combinators, test harness, services, hermetic sandboxing; these aren't holon algebra, they're plumbing built on kernel / io / core. Bonus: `tests/test.rs` with `wat::test! {}` in the wat-rs root — wat-rs eats its own dog food; `cargo test` covers `wat-tests/` natively via library surface (no binary spawn). Pre-publish clean break, no back-compat shims. Full INSCRIPTIONs at `wat-rs/docs/arc/2026/04/021-core-std-audit/INSCRIPTION.md` + `wat-rs/docs/arc/2026/04/022-holon-namespace-move/INSCRIPTION.md`. | 058 |

| 2026-04-22 | **Arc 023 — `:wat::holon::coincident?` — VSA-native equivalence predicate.** Surfaced while writing outstanding test coverage for the trading lab's Phase 3.3 `scaled-linear`: proving "scaled-linear's fact is structurally the same as the expected Bind(Atom, Thermometer)" required `(:wat::core::< (:wat::core::f64::- 1.0 (:wat::holon::cosine a b)) (:wat::config::noise-floor))` inline at every callsite. A literature search across Kanerva 2009, Schlegel et al 2022, Kleyko et al 2023 ACM two-part survey, Plate HRR, Gayler MAP, and hd-computing.com confirmed this bidirectional use of the noise-floor — as presence threshold AND as equivalence threshold — is consistent with VSA theory but **not named in the published literature**. Classical VSA is vector-first (cleanup-to-codebook); comparing two CONSTRUCTED ASTs for structural equivalence is a programming-languages move that wat's AST-first design surfaces naturally. Named under the gaze discipline: **`coincident?`** — two points occupying the same location on the hypersphere within tolerance. Geometric, self-describing, parallels `presence?`. Shape: `:wat::holon::HolonAST × :wat::holon::HolonAST -> :bool`. Semantic: `(1 - cosine(a, b)) < noise-floor` (equivalent to cosine > 1 - noise-floor; at d=1024 the threshold is cosine > 0.844). Same bound, two dual predicates, one substrate: **presence?** answers "is there signal above random?" (signal-detection direction); **coincident?** answers "are these the same holon within tolerance?" (equivalence direction). 4 Rust unit tests + 5 wat tests (including `test-stricter-than-presence` — locks the load-bearing invariant that coincident? is strictly stronger than presence?). Downstream simplification: the lab's Phase 3.3 `test-fact-is-bind-of-atom-and-thermometer` rewrites its inline cosine-error check as one call to `coincident?`. The algebra's measurement tier grows from 3 entries (cosine, dot, presence?) to 4 (adds coincident?). Full INSCRIPTION at `wat-rs/docs/arc/2026/04/023-holon-coincident/INSCRIPTION.md`. | 058 |

| 2026-04-22 | **Arc 025 — unified get/assoc/conj/contains? surface.** Cave-quest from Phase 3.4 (rhythm encoder) — 058-026-array's spec promised `(get vec i)` but only HashMap + HashSet shipped. Fixing the gap surfaced the asymmetric shape across the collection primitives; closing all four in one arc made the surface coherent. Four primitives × three containers:                  `get` — HashMap<K,V>/HashSet<T>/Vec<T> → Option<T>; `assoc` — HashMap/Vec → new-container (illegal on HashSet, use conj); `conj` — Vec/HashSet → new-container (illegal on HashMap, use assoc); `contains?` — HashMap/HashSet/Vec → bool. **Retired `:wat::std::member?`** — redundant with polymorphic `contains?` (builder: *"member? can go in favor of contains? ...ya"*). Split rationale: HashSet has no key-value pairing so `assoc` is illegal; HashMap needs key+value so `conj` is illegal. Container semantics force the split. 11 new Rust unit tests; 541 → 552 lib tests total; zero clippy. Arc 024's `:wat::config::set-presence-sigma!` / `set-coincident-sigma!` + arc 025's collection surface together cover every substrate primitive the lab's Phase 3 rewrite has needed. Full INSCRIPTION at `wat-rs/docs/arc/2026/04/025-container-surface-unified/INSCRIPTION.md`. | 058 |

| 2026-04-23 | **Arc 026 — `:wat::holon::eval-coincident?` family (4 primitives).** Structural `coincident?` (arc 023) compares two already-built HolonASTs. Arc 026 extends the predicate to the EVALUATION layer: each primitive takes the same argument shape as one of the shipped `eval-*!` forms, applied per side; evaluates each side through the constrained-eval path; atomizes each result via `value_to_atom`; runs the same `(1 - cosine) < coincident-floor` measurement. Catches cases structural coincident? cannot — the book's Chapter 28 retort `(+ 2 2) ≡ (* 1 4)` is literally this primitive (both reduce to `:i64 4` → same Atom hash → same vector → coincide). **Four variants**: `eval-coincident?` (2 args — quoted ASTs, mirrors `eval-ast!`), `eval-edn-coincident?` (4 args — mirrors `eval-edn!`), `eval-digest-coincident?` (10 args — mirrors `eval-digest!`, SHA-256 pre-parse per side), `eval-signed-coincident?` (14 args — mirrors `eval-signed!`, Ed25519 post-parse per side). All four return `:Result<:bool, :wat::core::EvalError>` — any failure on either side (source fetch, verification, parse, mutation-form refusal, runtime error, non-atomizable result) arrives as Err value. **The distribution story as a library primitive.** A native Rust app can Cargo-dep on wat + `#[wat_dispatch]`-wrapped crypto, receive an EDN program string with digest or signature data from any transport, and use `eval-signed-coincident?` to verify + evaluate + compare two programs' outputs in a single wat call. Chapter 10's "distributed by construction" promise ships as code — consensus-shaped use cases (two replicas encode to coincident points), integrity-gated composition (verify both sources came from authorized signers, THEN compare outputs), agreement-based routing (two classifiers coincide → act; disagree → escalate) are all "one call" deployments. **Empty-Bundle sentinel (userland idiom) captured as part of the arc.** holon-rs's vector-layer bundle panics on empty input — empty superposition has no well-defined vector. The userland convention: Lisp's `'()` lifted into an Atom, bundled as a single-element sentinel — `(Bundle [(Atom (quote ()))])`. Deterministic, no panic, grep-able, rooted in the Little Schemer's null semantic. USER-GUIDE § 6 documents the idiom; trading-lab's `wat/encoding/rhythm.wat` short-window fallback uses it. **Bonus test.** A `coincident_q_true_for_close_thermometer_values` test locks the native-granularity claim from Chapter 28: at d=1024, `Thermometer(0.039, 0.0, 1.0)` coincides with `Thermometer(0.041, 0.0, 1.0)` — values differing by 0.2% of range fall inside the substrate's angular resolution window. The generalization: resolution ≈ `1/sqrt(d)` fraction of range. **12 new Rust unit tests + 10 new wat-level tests** (all four variants at wat-tier via pre-computed hex/sigs embedded as string literals, same pattern `load.rs`'s digest-load tests use); 552 → 566 lib tests; 42 → 52 wat tests; zero clippy. Full INSCRIPTION at `wat-rs/docs/arc/2026/04/026-eval-coincident/INSCRIPTION.md`. | 058 |

| 2026-04-23 | **Arc 027 — deftest inherits test-binary loader.** Four slices closing the ergonomic-testing gap that motivated this whole series. **Slice 1 — canonical-path dedup.** `LoadError::DuplicateLoad` flipped from halt to silent no-op. Cycle detection stays intact (fires before dedup). Diamond dependencies where two files both `(load!)` a common helper now parse once and collapse silently. Pre-27 the duplicate-load halted. The new behavior is what makes per-test explicit-load lists composable with the type hierarchy's `./`-relative self-loads. **Slice 2 — `:None` sandbox scope inherits outer loader.** `:wat::kernel::run-sandboxed-ast`'s `scope :None` argument was "fresh InMemoryLoader" pre-27 — sandboxed programs had zero filesystem reach even when the enclosing test binary had one. Post-27: `:None` clones the caller's active loader (Arc::clone, pointer identity verified via `Arc::ptr_eq` in tests). Explicit `scope :"<path>"` still builds a fresh `ScopedLoader`. New shared helper `resolve_sandbox_loader(scope_opt, sym, op)`; both active sandbox primitives collapsed their inline loader-building to a one-line call. Fork children get the same behavior for free — COW of the parent's frozen SymbolTable already carries the loader across fork (same mechanism `install_dep_sources` rides). **Slice 3 — `wat::test!` default loader widens to `CARGO_MANIFEST_DIR`.** Implicit loader moved from `wat-tests/` subdirectory to the crate root, so tests can `(load! "wat/vocab/shared/time.wat")` reaching from `wat-tests/` back up to the crate's own `wat/` tree. Explicit `loader: "<subpath>"` override unchanged. **Slice 4 — deftest gains prelude parameter.** `:wat::test::deftest` signature extended from `(name dims mode body)` to `(name dims mode prelude body)` — prelude is a list AST spliced before the auto-generated `:user::main`. Empty `()` prelude = no startup forms. `:wat::test::deftest-hermetic` sibling with the same signature routes through `:wat::kernel::run-sandboxed-hermetic-ast` for Console / Cache-style tests needing fork isolation. Later arcs reshaped this signature: arc 030 flipped arg order to `(name mode dims prelude body)` matching arc 024's capacity-before-dims discipline, and arc 031 dropped mode/dims entirely to `(name prelude body)` via Config inheritance. **Slice 4 bonus — types self-load their deps.** Builder pressed on the 7-load prelude the lab's `time.wat` initially carried; three were genuinely redundant and four should have loaded through the type hierarchy. Added `./`-relative `(load!)` lines to `wat/types/candle.wat`, `wat/types/distances.wat`, `wat/vocab/shared/time.wat`. Dedup (slice 1) makes any explicit `main.wat` loads no-op on repeat. Lab test preamble collapsed from 7 loads → 1 load. **Callsite migration:** 52 + 6 + 1 + 25 = 84 deftest callsites across wat-rs and lab gained an `()` prelude as the new 4th arg. **Arc 027 is the template for arc 031** — same scope-inheritance move applied to a different environment field. Loader then, Config later. Full INSCRIPTION at `wat-rs/docs/arc/2026/04/027-deftest-inherits-loader/INSCRIPTION.md`. | 058 |

| 2026-04-23 | **Arc 028 — load/eval iface drop + hoist to `:wat::*` root.** Two orthogonal refactors combined into one arc, closing the load/eval substrate surface. **(1) Interface-keyword sub-namespaces retired.** `:wat::load::file-path`, `:wat::load::string`, `:wat::eval::file-path`, `:wat::eval::string` gone entirely. Each load/eval form now takes its source (path or string) directly as the first argument, with the path/string shape encoded in the form's name. **(2) Hoisted from `:wat::core::*` to `:wat::*` root.** Substrate interface sits next to `:wat::WatAST` at the namespace root — same position mature Lisps place load/eval. **Final surface — six load forms + seven eval forms**: `:wat::load-file!` (path), `:wat::load-string!` (inline), `:wat::digest-load!` / `:wat::digest-load-string!` (SHA-256 per side), `:wat::signed-load!` / `:wat::signed-load-string!` (Ed25519 per side); `:wat::eval-ast!` (pre-parsed AST), `:wat::eval-edn!` (inline), `:wat::eval-file!` (path — NEW form, not a rename), `:wat::eval-digest!` / `:wat::eval-digest-string!`, `:wat::eval-signed!` / `:wat::eval-signed-string!`. **Eval-coincident family (arc 026) updated to match**: six variants now, arg counts dropped from 10→8 (digest) and 14→12 (signed) per side after iface-keyword removal; two new `-string` siblings added (`eval-digest-string-coincident?`, `eval-signed-string-coincident?`). **`:wat::verify::*` stays** — payload-location keywords (`string` / `file-path`) + algo keywords (`digest-sha256` / `signed-ed25519`). Verify locations are inherently multi-shape (a signature can be a literal or a sidecar file), so the keyword-dispatch shape genuinely pays rent there; the load/eval forms' source-shape was already encoded in the form name. **Two-tier namespace split emerges**: substrate interface at `:wat::<form>` (load/eval/verify/WatAST), language vocabulary at `:wat::core::*` (define/let*/if/match/vec/arithmetic/collection primitives). **Reserved-prefix gate consolidated** from enumerated sub-namespaces to single `:wat::*` catch-all — user source cannot define under `:wat::*`, and retired path shapes stay blocked via the wildcard. **Workspace-wide tests green throughout**: 566 wat lib tests + integration suites (wat_core_try, stream, harness, cache, eval-result, cli, hermetic, structs, run-sandboxed), wat-lru self-tests, example smoke tests, and 25 lab wat tests — all passing per slice. Five commits across two repos (slices 1+3 core rename, integration-test catch, slice 4 root hoist, this INSCRIPTION + doc sweep, lab migrations `aa5bc9f` + `49ab0b6`). Full INSCRIPTION at `wat-rs/docs/arc/2026/04/028-load-eval-rename/INSCRIPTION.md`. | 058 |

| 2026-04-23 | **Arc 029 — nested quasiquote; macro-generating-macro support.** Blocker-resolution for arc 027 slice 4's `make-deftest` follow-up. **Substrate:** `walk_template` gains a `depth: u32` parameter; `expand_template` enters at depth 1 after stripping the outer quasiquote. Nested quasiquote/unquote/unquote-splicing forms peel one depth level instead of substituting when outside depth 1. Matches Common Lisp / Racket / Clojure convention. `,,X` (double unquote — two `(:wat::core::unquote ...)` wrappers) resolves at outer pass for an inner macro-template, carrying the resolved literal into the generated defmacro's body. `unquote_argument` extended to return non-symbol args as-is (the `,,X` resolution path); `splice_argument` similarly extended. **Slice 2 delivery:** `:wat::test::make-deftest` + `:wat::test::make-deftest-hermetic` in `wat/std/test.wat`. Factory takes a name + default-prelude (+ mode/dims at ship time; both later dropped by arc 031); generates a bare-name macro that each test invokes as `(:name :test-name body)`. **Bug discovered mid-arc** surfaced TWO fixes worth calling out: (1) `expand_form` was recursing INTO `(:wat::core::quote X)` bodies and expanding macro references — catastrophic for macro-generating-macro templates, which intentionally defer inner macro names. Fix: quote + quasiquote both skipped by the walker. (2) `expand_all` was cloning `&MacroRegistry` and passing the clone forward, discarding newly-registered macros. Fix: signature to `&mut MacroRegistry`. Both fixes surgical; both load-bearing. **Cave-quest cross-reference:** the mid-arc debugging is what motivated arc 030 (macroexpand primitives). The eprintln-through-expansion loop got painful enough that a proper `macroexpand-1` was clearly the right tool; arc 030 shipped it and then diagnosed arc 029's bug in three lines of wat. The tool that debugs a bug is substrate too. **Tests:** 5 new Rust unit tests in `src/macros.rs` covering the depth semantics; 1 wat-level regression test (`tests/wat_make_deftest.rs`) that asserts on the registered body's shape being a preserved `(quasiquote (:wat::test::deftest ...))` not a pre-expanded form. Workspace zero-regressions — every pre-029 single-depth template path untouched. **Lineage:** Maclisp (1970) → Common Lisp (1984) → Scheme (1986) → Racket (1995) → Clojure (2007) → wat (2026-04-23). Arc 029 joined the line. Full INSCRIPTION at `wat-rs/docs/arc/2026/04/029-nested-quasiquote/INSCRIPTION.md`. | 058 |

| 2026-04-23 | **Arc 030 — `:wat::core::macroexpand` + `macroexpand-1` runtime primitives.** Standard Lisp macro-debugging tool. Cave-quested mid-arc-029: the make-deftest-with-nonempty-default-prelude test failed, debugging via eprintln through multi-pass expansion got painful enough that the missing tool became obvious. Cut arc 030, shipped the primitives, used them on disk to see the actual pre-expansion AST, diagnosed arc 029's expand_form bug in three lines of wat, committed the one-character fix. **The tool that debugs a bug is substrate too.** Shape: both forms take `:wat::WatAST` and return `:wat::WatAST`. `macroexpand-1` applies one step (macro call → template expansion; non-macro call → unchanged). `macroexpand` runs to fixpoint (bounded by `EXPANSION_DEPTH_LIMIT`, same ceiling the freeze-time expander uses). Both operate on the current frozen macro registry — same registry the compile-time expander reads — via a new `SymbolTable.macro_registry: Option<Arc<MacroRegistry>>` carrier (mirrors the existing `encoding_ctx` / `source_loader` carrier discipline). `FrozenWorld::freeze` installs the registry at freeze time; runtime dispatch reads it. Two new `RuntimeError` variants: `NoMacroRegistry`, `MacroExpansionFailed`. New public helper `expand_once(form, registry)` in `src/macros.rs` — one-step expansion, `macroexpand-1` dispatches through it. **Arg-order flip rode along:** the four `:wat::test::*` macros' parameter order flipped from `(name dims mode prelude body)` to `(name mode dims prelude body)` matching arc 024's capacity-mode-before-dims discipline. 76-swap sweep across 16 files via Python script (no Perl; no regex alternation — Chapter 32's poison pattern stayed avoided). Shipped as closing commit within the arc 030 window; arc 031 later dropped mode/dims entirely via Config inheritance, making this a short-lived intermediate shape — but the honest one for its two-hour window. **Lineage:** Common Lisp (MACROEXPAND-1, MACROEXPAND) → Scheme (expand-syntax) → Racket (expand-once, expand) → Clojure (macroexpand-1, macroexpand) → Elisp (macroexpand-1, macroexpand-all) → wat (2026-04-23). The one-step/fixpoint split converges across every mature Lisp because substrate pressure permits no other useful factoring. **Tests:** 5 new Rust unit tests; 2 new wat-level tests (`test-macroexpand-1-non-macro`, `test-macroexpand-fixpoint-evaluates`). Plus the arc 029 regression test `tests/wat_make_deftest.rs` which uses macroexpand-1 in anger to assert on registered body shape. Full INSCRIPTION at `wat-rs/docs/arc/2026/04/030-macroexpand/INSCRIPTION.md`. | 058 |

| 2026-04-23 | **Arc 031 — sandbox inherits outer Config.** Path B for the make-deftest ergonomic arc that started at arc 029 (nested quasiquote) and closed through arc 030 (macroexpand + arg-order flip). **Substrate:** `:wat::kernel::run-sandboxed-ast`, `run-sandboxed-hermetic-ast`, and `fork-with-forms` all inherit the caller's committed Config by default when the inner forms omit `(:wat::config::set-*!)`. Inner setters still override when present; duplicate-in-forms still errors. New collector `collect_entry_file_with_inherit(forms, &Config)` and new startup `startup_from_forms_with_inherit(forms, base, loader, &Config)` share the post-config pipeline with their non-inheriting siblings via a private `startup_from_forms_post_config` helper. **Test-macro shape:** all four `:wat::test::*` macros — `deftest`, `deftest-hermetic`, `make-deftest`, `make-deftest-hermetic` — drop their `mode` + `dims` parameters. The test file's top-level preamble becomes the single declaration site for capacity-mode + dims; every sandbox inherits. Templates stop auto-injecting setters; factory templates drop `,,mode` / `,,dims` nested-unquotes (only `,,default-prelude` remains). Honest shape: one preamble + one factory + N tests. **Relationship to arc 027:** same scope-inheritance move applied to a different environment field. Arc 027: source loader (scope `:None` → caller's loader). Arc 031: Config (absent setters → caller's values). Together they make a sandbox a proper child-of-caller scope rather than a fresh reset. **Callsite sweep:** 15 files × 64 ` :error 1024` drops across `wat-tests/`, `crates/wat-lru/wat-tests/`, `examples/`, `tests/`; plus lab's `test_scaffold.wat` + `time.wat`. Python script for safety (literal substring replace — no regex, no alternation, no shell-escape risk — the exact failure mode Chapter 32 warned against). **Tests:** 7 new config unit tests + 4 new sandbox integration tests; 574 → 581 lib tests + every integration suite + 25 lab wat-tests green. Full INSCRIPTION at `wat-rs/docs/arc/2026/04/031-sandbox-inherits-config/INSCRIPTION.md`. | 058 |

| 2026-04-23 | **Arc 032 — `:wat::holon::BundleResult` typealias.** Naming a 44-character-wide type that appears 30+ times across the workspace. Bundle returns `:Result<wat::holon::HolonAST, wat::holon::CapacityExceeded>` (058-003); every downstream caller threads the same shape. The gaze-ward's discipline picked the name: **`:wat::holon::BundleResult`** — Level-2-safe, the `Result` suffix speaks at first read; cold readers grep once to learn the Ok/Err types. Non-parametric (Bundle's Ok arm is always HolonAST; CapacityExceeded is the algebra's only capacity-failure shape). Registered in `src/types.rs::register_builtin_types` next to `CapacityExceeded`; alias resolution at the checker layer unifies both forms. **Sweep:** 28 swaps across 8 files in wat-rs (src/runtime, src/check comments, tests/wat_bundle_capacity, tests/wat_run_sandboxed, wat/holon/{Trigram,Ngram,Bigram}.wat, wat-tests/holon/{coincident,Trigram}.wat, wat-tests/std/test.wat) + lab's `wat/encoding/rhythm.wat` + `wat-tests/encoding/rhythm.wat`. Python substring replace (no regex, no alternation — Chapter 32's poison pattern stayed avoided). **Tests:** +2 Rust unit tests in `src/types.rs::tests` (`bundle_result_alias_registered_with_builtins`, `bundle_result_alias_expands_to_expected_result`); 581 → 583 lib tests. Lab 29 wat-tests green. **First arc cut under the `/gaze` spell** — builder invoked gaze on the naming question; the ward's Level 1/Level 2/taste discipline picked the name cleanly. Full INSCRIPTION at `wat-rs/docs/arc/2026/04/032-bundle-result-typealias/INSCRIPTION.md`. | 058 |

| 2026-04-23 | **Arc 033 — `:wat::holon::Holons` typealias.** Second naming arc under `/gaze`, same session as 032. Target: the 35+ lab and 12 wat-rs occurrences of `:Vec<wat::holon::HolonAST>` — Bundle's input type and every `encode-*-facts` vocab function's return. **The naming move itself is worth recording.** First candidate was `:wat::holon::Facts` (picked for domain-nativity: vocab functions return facts). Builder challenged on Level 1 epistemic grounds: *"a statement could be false... you have to measure it."* Chapter 28's "knowing requires looking" — before the query, no fact exists. Dialogue continued: time measurements ARE factual, so `Facts` isn't absolutely lying. But the TYPE is content-agnostic (factual measurements today; predictive claims when market observers arrive). Final name: **`:wat::holon::Holons`** — plural of the element type, structurally honest, epistemically neutral, Level-1-safe across every possible content. Builder's line: *"they ARE holons — that's the name."* Shipped non-parametric, alias resolution handles both forms. Sweep: 18 swaps across 8 wat-rs files (src/ast, src/check, src/types self-comment, tests/wat_bundle_capacity, tests/wat_variadic_defmacro, wat/holon/{Ngram,Sequential}.wat). Two lexer tests intentionally kept the long form — they test the lexer's handling of `<>`+`,` inside a keyword; the long form is the test vector. Lab migration in lab arc 004 (same session) sweeps the remaining 35 lab occurrences alongside `Scales` + `ScaleEmission` aliases. **Tests:** +2 Rust unit tests; 857 → 859 lib tests green. **The reflex cleared another target.** Full INSCRIPTION at `wat-rs/docs/arc/2026/04/033-holons-typealias/INSCRIPTION.md`. | 058 |

| 2026-04-23 | **Lab arc 004 — naming sweep (five moves in one arc).** Third /gaze-named arc of the session (after wat-rs 032 BundleResult + 033 Holons). Applies the naming reflex to the lab's domain vocabulary: (1) **`:trading::encoding::Scales`** typealias for `:HashMap<String, trading::encoding::ScaleTracker>` — the per-indicator-name tracker registry (25 sites swapped). (2) **`:trading::encoding::ScaleEmission`** typealias for `:(wat::holon::HolonAST, trading::encoding::Scales)` — scaled-linear's values-up return (10 sites). (3) **Lab-wide Holons migration** — arc 033's `:wat::holon::Holons` applied to the lab's ~22 remaining `:Vec<wat::holon::HolonAST>` sites. (4) **Vocab function renames** — `encode-time-facts` → `encode-time-holons`, `encode-exit-time-facts` → `encode-exit-time-holons`, `time-facts` → `time-holons`, plus internal helpers `build-facts` / `build-facts-loop` in rhythm.wat. The return-type name is Holons (arc 033); the verb follows. (5) **Test variable renames** — `facts`, `facts-a`, `facts-b`, `facts-morning`, `facts-evening` in test let-bindings and body references all → `holons`/`holons-a`/etc. **Two bugs surfaced mid-sweep and got fixed.** First: self-referencing typealias — the HashMap→Scales substitution caught its own RHS in the typealias declaration, producing `(typealias Scales Scales)`. Fixed by rewriting the RHS to its proper expansion. Second: narrow-regex missed use-sites — first pass renamed let-binding sites but not body-reference sites; 10+ tests failed with `UnboundSymbol("facts")`. Fixed with broader `\bfacts\b` → `holons` sweep. **The narrow-first, broad-after shape is a recurring pattern in these rename arcs** — safer to start narrow (avoid over-matches in comments) then broaden when under-matches surface in tests. **FOUNDATION.md updated** — three current-state references (Bundle's signature, a let-binding example, stdlib Vec definition) swapped to the short aliases. Historical CHANGELOG rows and prior arc INSCRIPTIONs untouched — their original language captures the shipped state at their moment. **Tests:** all 29 lab wat-tests green on first pass after the full sweep. Full INSCRIPTION at `docs/arc/2026/04/004-lab-naming-sweep/INSCRIPTION.md`. | 058 |

| 2026-04-23 | **Arc 034 — `:wat::holon::ReciprocalLog` stdlib macro.** Sixth cave-quest of the naming-reflex session. Cut from lab arc 005 when Log bounds for ROC atoms became fog. **The empirical observation:** lab shipped a fog-breaker `.wat` program to tabulate Log-encoding cosines at three bound settings (wide 1e-5/1e5, medium 0.1/10, tight 0.5/2) against value=1.0 reference. Table revealed the archive's wide bounds make ROC values indistinguishable — everything ±30% around 1.0 coincides (the archive's pre-058-017 substrate was cosine-rotation, 058-017 is Thermometer-saturation, different behaviors at the same bounds). Tight bounds (0.5, 2.0) give per-1% resolution. **The first-principles framing:** builder saw the structure — *"its 1/2 through 2/1"* — the smallest reciprocal pair. Family `(1/N, N)` for N ∈ {2, 3, 10, ...}: smallest N=2 is the natural boundary for ratio-valued indicators (doubling/halving). Log-symmetry automatic via ln(1/n) = -ln(n). **Named under `/gaze`:** first candidate `BoundedLog` (Level 2 mumble — Log already bounded); `ReciprocalLog` won (Level-2-safe — the name IS the structural definition). **Pure-wat stdlib macro**: `(ReciprocalLog n value)` → `(Log value (/ 1.0 n) n)`. Lives at `wat/holon/ReciprocalLog.wat`. Registered in `src/stdlib.rs::STDLIB_FILES` next to Log. Zero Rust changes beyond the one registration line. **Tests:** 4 new wat-level tests (expansion-equivalence-with-explicit-Log, self-coincidence, saturation-distinguishability, different-N-differs-for-same-value); 583 → 585 lib tests + integration unchanged. **Follow-through:** lab arc 005 (oscillators) uses `(ReciprocalLog 2.0 roc-N)` for all four ROC atoms. Subsequent market vocab modules each pick N from the family based on natural indicator range. Full INSCRIPTION at `wat-rs/docs/arc/2026/04/034-reciprocal-log/INSCRIPTION.md`. | 058 |

| 2026-04-23 | **Lab arc 005 — market/oscillators vocab (first market-tree module).** Port of archive's 84L oscillators.rs. Eight holons per candle: four scaled-linear (rsi, cci, mfi, williams-r) threading `Scales` values-up, four ReciprocalLog 2.0 (roc-1/3/6/12) at fixed reciprocal bounds. Returns `(Holons, Scales)` tuple. **Cave-quested wat-rs arc 034** for `:wat::holon::ReciprocalLog` after `explore-log.wat` empirical observation revealed the archive's single-arg Log (cosine-rotation) doesn't translate to 058-017's Thermometer-Log at wide bounds. **Two latent-bug fixes** caught along the way: file-scope test loads scope to test file's dir (helpers moved to default-prelude per arc 003 pattern); scaled-linear.wat didn't self-load round.wat (masked for weeks by main.wat load order) — fixed at source per arc 027's types-self-load discipline. Five tests green on first pass; 29 → 34 lab wat tests. Market sub-tree opens (13 modules remaining). Full INSCRIPTION at `docs/arc/2026/04/005-market-oscillators-vocab/INSCRIPTION.md`. | 058 |

| 2026-04-23 | **Lab arc 006 — market/divergence vocab (first conditional-emission module + `VocabEmission` alias).** Port of archive's 60L divergence.rs with a new shape: conditional emission. Three atoms (bull, bear, spread) each emit only when their guard fires; returned `Holons` is variable-length (0/1/2/3). The wat-native translation uses a file-private `maybe-scaled-linear` helper that threads `(holons, scales)` values-up through each maybe-emit step — archive's `facts.push(...)` translated functionally. **VocabEmission naming move** happened mid-arc: the `(Holons, Scales)` tuple appeared at six call sites across arcs 005 and 006, so `/gaze` pass picked `:trading::encoding::VocabEmission` — paired with arc 004's `ScaleEmission` (vocab-level-emission is the bulk sibling of the scaled-linear-primitive's ScaleEmission). Declared next to ScaleEmission in `wat/encoding/scaled-linear.wat`; 14-swap migration across oscillators source + tests + divergence source. Six tests green on first pass covering the emission truth-table (none/bull-only/bear-only/both) + bull holon shape + no-emit preserves scales. 34 → 40 lab wat-tests. Pattern standing: `maybe-scaled-linear` extracts to shared/helpers.wat when a second conditional-emission module ports (likely `exit/trade_atoms.rs` next). Full INSCRIPTION at `docs/arc/2026/04/006-market-divergence-vocab/INSCRIPTION.md`. | 058 |

| 2026-04-23 | **Arc 035 — `:wat::core::length` polymorphism.** Ninth cave-quest of the Phase-2 sequence. Cut from lab arc 007 when the fibonacci test reached for `(:wat::core::length updated-scales)` on a `HashMap<String, ScaleTracker>` — `length` was Vec-only. Arc 025 had unified `get`/`assoc`/`conj`/`contains?` over HashMap/HashSet/Vec but `length` wasn't part of that slice (no caller had demanded it). Today's caller surfaced the gap. Same coherence-with-siblings pressure; same resolution. **Runtime:** `eval_vec_length` → `eval_length`, dispatch on `Value` variant. **Check:** narrow `∀T. Vec<T> → i64` scheme retired; new polymorphic `infer_length` mirrors arc 025's `infer_contains_q` shape (Parametric match on Vec/HashMap/HashSet arms, all returning `:i64`). **Tuple deliberately excluded** — arity is structural and known at type-check time; no caller need surfaced. **Drive-by clippy recovery:** pre-existing `too_many_arguments` warning in `src/fork.rs::child_branch` (8 args vs lint threshold 7) traced to arc 031's `inherit_config: Option<Config>` landing at commit f3fa9ea. The workspace's zero-clippy invariant had been silently broken across arcs 031-034. `#[allow(clippy::too_many_arguments)]` with comment explaining 8 is the honest arity — six Drop-critical fds + forms + inherit_config. Arc 017 precedent for drive-by clippy sweeps. **Tests:** +5 Rust unit tests (hashmap/hashset both counts + empty + Vec sanity); 585 → 590 lib tests. Zero clippy warnings restored. Full INSCRIPTION at `wat-rs/docs/arc/2026/04/035-length-polymorphism/INSCRIPTION.md`. | 058 |

| 2026-04-23 | **Lab arc 007 — market/fibonacci vocab (simplest vocab shape yet).** Port of archive's 72L fibonacci.rs. Eight scaled-linear atoms from `Candle::RateOfChange`: three raw window positions (`range-pos-12/24/48`) plus five Fibonacci retracement distances (`fib-dist-236/382/500/618/786`), each computed as `range-pos-48 - level` then `round-to-2`. **Single sub-struct, all scaled-linear, no Log tier, no conditional emission.** Oscillators' pattern minus the Log tier minus the conditional tier — the cleanest vocab shape so far. The three arcs 005/006/007 now form a Phase-2 market pattern catalog (mixed scaled-linear + Log / conditional-emission / pure scaled-linear); future vocab modules pattern-match onto one of the three or surface a new shape via cave-quest. **Cave-quested wat-rs arc 035** mid-test-writing when `length` on the returned `Scales` tripped type-check. 4/5 tests green on first pass; the fifth surfaced the cave quest; all five green after arc 035 shipped. Five tests: count (8 holons), range-pos-12 shape, fib-dist-500 shape (proves subtraction math), scales-accumulate-8-entries, different-candles-differ. 40 → 45 lab wat-tests. Market sub-tree: 3 of 14 shipped. Full INSCRIPTION at `docs/arc/2026/04/007-market-fibonacci-vocab/INSCRIPTION.md`. | 058 |

| 2026-04-23 | **Arc 036 — wat-lru namespace promotion (`:wat::lru::*`).** Pure-convention arc; zero substrate change. Builder's eye caught `:user::wat::std::lru::*` reading noisy (five segments, double-nested). The substrate bypass mechanism was already in place (`src/freeze.rs:362-368` — stdlib-tier registration bypasses the reserved-prefix gate; installed dep sources flow through `stdlib_forms()` via the same stdlib-tier path). **New rule in CONVENTIONS.md:** `:wat::<crate>::*` — first-party **workspace-member** crates of wat-rs (`crates/wat-*/`); co-authored, co-released, co-reviewed in the same repo. `:user::*` remains for community crates (external repos) + user app code. The retired `:user::wat::std::<crate>::*` tier double-nested the wat prefix inside user-space; rename resolves the confusion. **Files moved to mirror symbols** (filesystem path === namespace path): `crates/wat-lru/wat/LocalCache.wat` → `crates/wat-lru/wat/lru/LocalCache.wat`, same in `wat-tests/`. Consistent with baked-stdlib discipline (`wat/holon/Trigram.wat` hosts `:wat::holon::Trigram`). **Sweep:** CONVENTIONS.md tier table rewrite with a mechanism-vs-convention explainer, README.md, docs/README.md, src/test_runner.rs doc, src/lib.rs module doc + wat_sources() paths + include_str!, Cargo.toml description, examples/with-lru/wat/main.wat. Arc 013/015 historical files untouched. Test counts unchanged (590 wat-rs lib tests, all wat-lru wat tests pass under new paths, `cargo run -p with-lru-example --release` prints "hit" end-to-end). Precedent set for future workspace-member crates (`:wat::sqlite::*`, `:wat::redis::*`, etc.). Full INSCRIPTION at `wat-rs/docs/arc/2026/04/036-wat-lru-namespace-promotion/INSCRIPTION.md`. | 058 |

| 2026-04-23 | **Lab arc 008 — market/persistence vocab (first cross-sub-struct module + the signature rule named).** Port of archive's 36L persistence.rs. Three scaled-linear atoms from TWO sub-structs: `hurst` + `autocorrelation` from `Candle::Persistence`, `adx` (normalized `/100.0`) from `Candle::Momentum`. **First K=2 exercise of the cross-sub-struct vocab signature rule** (closes task #49 that's been pending since Phase-2 opened). **The rule:** a vocab function's signature declares every sub-struct it reads, one parameter per sub-struct, ordered alphabetically by type name; `Scales` last. Continues arcs 001 – 007's "vocab reads its specific sub-struct" pattern linearly from K=1 to K≥2, not as a new shape. **Rejected alternatives** (documented in DESIGN): full-Candle pass (signature-dishonest — retracts arc 001's call), per-vocab view struct (9+ wrapper types whose only job is to be unpacked — fails Hickey's unentangled-concepts test), anonymous tuple (positional access inside body loses per-param names). **Footnote surfaced** on distinguishability tests: fresh `ScaleTracker::scale` rounds values in roughly [0.25, 0.75] to the same 0.01, making Thermometer encodings saturate identically; "different candles differ" tests need values across the scale-rounding boundary (A=0.1 floors to 0.001; B=0.9 rounds to 0.02). Durable caveat for future vocab arcs. 4/5 tests green on first pass; the footnote caught the fifth. 45 → 50 lab wat-tests. Market sub-tree: 4 of 14 shipped. **Rule now cited in `rewrite-backlog.md`'s top-of-Phase-2 note** — future cross-sub-struct arcs (stochastic, keltner, timeframe, flow, ichimoku, price_action, standard, momentum, exit/phase, exit/regime) inherit the rule without re-deriving. Full INSCRIPTION at `docs/arc/2026/04/008-market-persistence-vocab/INSCRIPTION.md`. | 058 |

| 2026-04-23 | **Lab arc 009 — market/stochastic vocab (first inheritance of arc 008's rule + inline-clamp shape).** Port of archive's 36L stochastic.rs. Four scaled-linear atoms: `stoch-k` + `stoch-d` (normalized `/100.0`) and `stoch-kd-spread` (computed) from `Candle::Momentum`; `stoch-cross-delta` (clamped to `[-1, 1]`) from `Candle::Divergence`. **Second cross-sub-struct vocab module** — first to ship entirely under arc 008's signature rule without re-derivation. Signature `(d :Divergence) (m :Momentum) (scales)` alphabetical by type name. **Introduces the inline-clamp pattern** for the archive's `.max(-1.0).min(1.0)`: nested `if` inside `let*`, kept inline per stdlib-as-blueprint — single use in this module, extract to `shared/helpers.wat` when a second clamp caller surfaces (price_action likely; regime's `variance_ratio.max(0.001)` is a one-sided floor — different shape). Arc 008's scale-collision footnote held on first test-writing pass: values `raw=10/90 → normalized 0.1/0.9 → scales 0.001/0.02` chosen from the start. Five tests green on first pass (including an explicit cross-delta clamp test that feeds raw 1.5 and verifies the encoded fact matches a fresh-tracker encoding of value 1.0). 50 → 55 lab wat-tests. Market sub-tree: 5 of 14 shipped. Full INSCRIPTION at `docs/arc/2026/04/009-market-stochastic-vocab/INSCRIPTION.md`. | 058 |

| 2026-04-23 | **Lab arc 010 — market/regime vocab (first non-N=2 ReciprocalLog + observation-reflex-as-standing-practice).** Port of archive's 83L regime.rs. Single-sub-struct (K=1); seven scaled-linear atoms plus one ReciprocalLog for `variance-ratio`. **First non-N=2 use of arc 034's ReciprocalLog family.** N=10 bounds `(0.1, 10)` chosen via empirical observation program `explore-log.wat` — tabulated cosine-vs-reference-1.0 at N=2/3/10 for values 0.1-20. N=2 (arc 005's ROC choice) saturates at ±doubling, losing 0.5/2.0 mean-reverting/trending signals. N=3 middle ground still saturates at ±tripling. N=10 preserves the full financial range: 0.5 and 2.0 reach cos 0.70 (distinguishable from 1.0); 0.3 and 3.0 reach 0.48/0.52 (still distinguishable); 0.1 and 10 saturate at 0 (the natural range boundary). Domain argument: regime's job is "what kind of market is this" — coarse-near-1.0, fine-across-range. Mirror of ROC (fine-near-1.0, saturate-±doubling). The reciprocal-pair family `(1/N, N)` is domain-parametric; N picked per module. **Observation reflex (Chapter 35) confirmed as permanent standing practice** — same pattern as arc 005's fog-breaker, different domain, landed the right answer first pass. Variance-ratio one-sided floor at 0.001 preserved via inline `if` — operationally moot at N=10's bounds but kept as defensive input-hygiene marker. Six tests green on first pass including variance-ratio-shape (confirms the ReciprocalLog expansion) + variance-ratio-floor (confirms the one-sided if). 55 → 61 lab wat-tests. Market sub-tree: 6 of 14 shipped. Full INSCRIPTION at `docs/arc/2026/04/010-market-regime-vocab/INSCRIPTION.md`. | 058 |

| 2026-04-23 | **Lab arc 011 — market/timeframe vocab (first Ohlcv read + leaf-name rule clarification + round-to-4 + cross-sub-struct computed atom).** Port of archive's 59L timeframe.rs. Third cross-sub-struct module. Six scaled-linear atoms, no Log — but introduces three new shapes the substrate now standardizes. **(1) First Ohlcv read in a vocab** — `:trading::types::Ohlcv` loaded alongside `Candle`; auto-generated `:Ohlcv/close`, `:Ohlcv/open`, etc. work identically to Candle sub-struct accessors. **(2) Leaf-name clarification to arc 008's alphabetical rule** — arc 011 is the first arc to mix `Ohlcv` (not under `Candle::*`) with `Candle::Timeframe`; full-path vs leaf-path ordering would disagree. Rule clarified: alphabetical by **leaf (unqualified)** type name. Stable under future namespace reorganization. Captured in rewrite-backlog.md's top-of-Phase-2 rule note + arc 011 DESIGN. **(3) `round-to-4` helper** added to `encoding/round.wat` alongside `round-to-2` — second named digit-width (tf-*-ret and tf-5m-1h-align use 4-decimal precision per archive; stdlib-as-blueprint holds until a third surfaces). **(4) First cross-sub-struct computed atom** — `tf-5m-1h-align` computes `signum(tf-1h-body) × (close - open) / close`, reaching both sub-structs in its value expression. Earlier cross-sub-struct arcs (008/009) read from multiple sub-structs but computed each atom from one. Signum inline (single use, arc 009's inline-clamp precedent holds). Six tests green on first pass including a symmetric-recompute verification of the cross-compute. 61 → 67 lab wat-tests. Market sub-tree: 7 of 14 shipped. Full INSCRIPTION at `docs/arc/2026/04/011-market-timeframe-vocab/INSCRIPTION.md`. | 058 |

| 2026-04-23 | **Lab arc 012 — geometric bucketing for scaled-linear (substrate cache architecture named).** Encoding-layer amendment at `wat/encoding/scale-tracker.wat` + `wat/encoding/scaled-linear.wat`. Replaces blind `round-to-2` value quantization with **geometric bucketing at `bucket-width = scale × noise-floor`**. Builder's "Venn diagrams that are cache friendly" insight named the rule: cache keys should equal noise-floor shells, not fixed decimal grids. Each atom gets its own per-scale quantization grid matched to the substrate's actual discrimination resolution (the `1/√d` noise floor translated to value-space via its scale). Observation program `explore-bucket.wat` confirmed the math: round-to-2 **over-splits** for large-scale atoms (cache misses on substrate-equivalent values) and **under-splits** for small-scale atoms (cache hits that collapse substrate-distinguishable differences — an information-preserving bug). **Option B defensive fallback:** if bucket-width ≤ 0, return value unchanged. Absorbs the pre-existing `ScaleTracker::scale` quirk (`round(0.001, 2) = 0.00`) without changing observable behavior. **5 new bucket unit tests** + all 67 prior lab tests pass unchanged. Total: 67 → 72 lab wat-tests. **Deep structural observation:** Chapter 36 names "the lattice" — the substrate's native discretization structure, `2√d` positions per atom on the value axis, connected to Kanerva's `√d` bundle capacity via the same `1/√d` noise floor. Factor of 2 in `2√d` is geometric (symmetric bipolar range), not law. Builder's follow-up insight: position-bound bundles (Kanerva's positional encoding) and LSH anchor vectors use **the same atom basis** — `Atom(integer)` family serves as canonical anchors for both storage-side position dispatch AND hash-side cache keys. Formalization deferred to **arc 013** (bidirectional cache via SimHash). Full INSCRIPTION at `docs/arc/2026/04/012-geometric-bucketing/INSCRIPTION.md`; narrative in BOOK Chapter 36. | 058 |

| 2026-04-24 | **wat-rs arc 037 — dim-router (the last magic value retires).** Substrate-wide architectural shift: `dims` stops being a single required global. The ambient `DimRouter` decides vector dim per Atom/Bundle construction from the AST's surface shape; `EncoderRegistry` materializes per-d encoders lazily with a shared seed. Cross-dim cosine / presence? / coincident? / dot normalize UP via AST re-projection at `max(d_a, d_b)`. Every substrate default is a function, every user override replaces our function: three capability carriers on SymbolTable (`dim_router`, `presence_sigma_fn`, `coincident_sigma_fn`) with AST-accepting setters (`set-dim-router!`, `set-presence-sigma!`, `set-coincident-sigma!`) and freeze-time signature checks. `CapacityMode` reduced to two variants (`:error` / `:abort`); `:silent` and `:warn` retired. Scalar `set-dims!` / `set-noise-floor!` retired; scalar sigma setters retired in favor of function-of-d form. Compatibility shims keep `:wat::config::dims` / `:wat::config::noise-floor` accessors returning `DEFAULT_TIERS[0]` defaults (d=256, 1/sqrt(256)) until lab callers migrate to per-AST primitives. Seven slices (slice 2 retired mid-arc); 11 commits. wat-rs: 49 test-result blocks green, zero clippy. Lab: 72/72 still green against the updated substrate — the only lab-side change was a mechanical sweep removing `set-dims!` forms from 17 `.wat` files (they were already no-ops since arc 037 slice 1 landed). BOOK chapters 36–43 laid out the computation model; arc 037 is the implementation. Full INSCRIPTION at `wat-rs/docs/arc/2026/04/037-dim-router/INSCRIPTION.md`. | 058 |
| 2026-04-24 | **wat-rs arc 045 — capacity-mode `:abort` → `:panic` + demo cleanup.** Substrate rename: `CapacityMode::Abort` → `CapacityMode::Panic`, parse arm `":abort"` → `":panic"`, error messages updated, runtime panic message updated, 4 unit tests + 2 integration test fixtures renamed. Pre-rename survey identified zero active downstream callers outside test fixtures (in `wat/`, `wat-tests/`, `examples/`, `crates/`, lab repo proper), so substrate cost was bounded — `cargo test --release` shows 39 binaries / 850 tests pass post-rename, zero clippy. The `:panic` name matches Rust's `panic!()` macro behavior (which unwinds); `:abort` connoted `std::process::abort` (no unwinding) and misled fresh readers per /gaze-discipline. Plus user-facing demo cleanup: `(set-capacity-mode! :error)` lines dropped from demos (it's the default per arc 037 — demos should show overrides, not redundant defaults). Plus mid-arc fold-in: replace `:Vec<wat::holon::HolonAST>` with the `:wat::holon::Holons` typealias (arc 033) in user-facing example signatures. Last polish-class arc planned per builder's "treat wat as stable until we find it isn't — the polish is for the next llm to observe" stance: further drift catches surface on demand, not from preemptive iteration. Earlier arc INSCRIPTIONs that mention `:abort` stay frozen as historical record. Full INSCRIPTION at `wat-rs/docs/arc/2026/04/045-capacity-mode-rename/INSCRIPTION.md`. | 058 |
| 2026-04-24 | **wat-rs arc 044 — second verification pass.** Round 3 of "is wat-rs honest?" iteration. Builder asked the question for the second time after arc 043 closed; surveyed surfaces arc 043 hadn't covered. Found 7 drift sites in proc-macro source comments (`wat-macros/src/lib.rs`), example wat-tests files (`examples/with-loader/wat-tests/{test-loader,helpers}.wat`), and baked wat-stdlib comments (`wat/std/test.wat` × 3 set-dims! examples). Slice 1 fixed those; BACKLOG-mandated broadened post-Slice-1 sweep caught 6 more in README.md (§Rust interop pointer + 3 §Workspace layout entries) and docs/README.md (arc 015 + 017 index entries). 13 total fixes across 2 slices. Names the iterative-verification observation explicitly: "audited" is a pass count, not a final state. INSCRIPTION lists what's still uncovered (Rust tests' embedded wat strings, wat-tests source comments, wat-lru's own docs, Cargo.toml metadata) so any round-4 has a starting point. Arc 042's and 043's INSCRIPTIONs stay frozen as historical record despite their over-confident closing claims; the corrections live in their successors. Full INSCRIPTION at `wat-rs/docs/arc/2026/04/044-second-verification-pass/INSCRIPTION.md`. | 058 |
| 2026-04-24 | **wat-rs arc 043 — verification gaps closure.** Honest follow-on to arcs 038-042: builder asked whether wat-rs was "honest again," answer was "substantially more, not provably." Four checks executed: (1) `cargo test --release` from `wat-rs/Cargo.toml` produced concrete counts — 725 Rust tests across 25 binaries + ~58 wat tests across 11 wat-tests files; updated README to cite real numbers and dropped a stale `wat_cache` reference (file doesn't exist; cache tests externalized to wat-lru per arc 013). (2) Sigma formulas verified against `src/dim_router.rs:188-205`; USER-GUIDE cited `floor(√d/2) - 1` correctly but missed the `max(1)` clamp at small d; corrected. (3) `src/*.rs` doc-comment audit surfaced 7 stale `wat::test_suite!` references (arc 018 rename) across `test_runner.rs`/`bin/wat.rs`/`panic_hook.rs` plus one `set-dims!` example in `harness.rs`; corrected, plus reframed `test_runner.rs:421`'s "set-dims! is a no-op" comment to the actual "retired (rejected at config collection time)" arc 037 contract. (4) Cross-doc spot-check surfaced three drift spots in USER-GUIDE §12 Error Handling that arc 038's slice 5 didn't reach — BundleResult typealias propagation (arc 032), capacity-mode list reduction from four to two (arc 037), `floor(sqrt(dims))` → `floor(sqrt(d))` framing (arc 037); all corrected. Plus pre-commit drift catch: invented `:wat::core::i64::sqrt-floor` primitive in a draft, verified non-existence via grep before push. Honest disclosure: arc 042's INSCRIPTION claim "current through arc 037" was too optimistic; the correction lives in arc 043's INSCRIPTION; arc 042 stays frozen as historical record. With this arc the verification round is complete — what the substrate ships matches what user-facing surface describes, **verified, not asserted**. Full INSCRIPTION at `wat-rs/docs/arc/2026/04/043-verification-gaps-closure/INSCRIPTION.md`. | 058 |
| 2026-04-24 | **wat-rs arc 042 — ZERO-MUTEX.md drift-only sync.** Smallest implementation surface of any doc-audit arc (three Edits total). Sole drift: three `:wat::std::service::Cache<K,V>` references migrated to `:wat::lru::CacheService<K,V>` per arcs 013 + 036. The concurrency-architecture story (three-tier framing — immutable/thread-owned/program-owned, HandlePool discipline, spawn/send/recv/select primitives, the empirical claim about 30+ threads with zero Mutex) is substrate-shape-agnostic and unaffected by arcs 028-037. With this arc the wat-rs user-facing audit set is closed: 038 (USER-GUIDE recovery + sync), 039 (README drift), 040 (CONVENTIONS drift), 041 (wat-tests README drift by omission), 042 (ZERO-MUTEX drift). arc 005's INVENTORY.md preserved as-is per builder ("for the record" — USER-GUIDE Appendix is the live forms reference now). One audit-set item remains: `holon-lab-trading/CLAUDE.md` will ship as a lab arc (different repo, different domain). Full INSCRIPTION at `wat-rs/docs/arc/2026/04/042-zero-mutex-drift-sync/INSCRIPTION.md`. | 058 |
| 2026-04-24 | **wat-rs arc 041 — wat-tests/README.md drift-only sync.** Sibling to 038/039/040; smallest of the doc-drift arcs (one implementation slice on a 70-line file). Standard audit set returned zero retired-form occurrences — drift was by *omission*, not correction. Two additions: §Layout gained an external-crates paragraph noting consumer wat crates ship their own `wat-tests/` trees and run via `cargo test -p <crate>` (arcs 013 + 015 + 036 — the pattern is portable, not wat-rs-specific); §In-process vs hermetic restructured macros-first / primitives-below (the user-facing `deftest` / `deftest-hermetic` / `make-deftest` family from arcs 029 + 031, with `:wat::test::run` / `run-hermetic-ast` restated as expansion targets and "default to the macros, drop down to primitives when a sandbox needs custom wiring" guidance). The audit shape works: read the file, list every claim, walk each claim against shipped state, name the gaps — the discipline is "find the absences that mislead a fresh reader," not "find typos." Full INSCRIPTION at `wat-rs/docs/arc/2026/04/041-wat-tests-readme-drift-sync/INSCRIPTION.md`. | 058 |
| 2026-04-24 | **wat-rs arc 040 — CONVENTIONS.md drift-only sync.** Sibling to 038/039. `wat-rs/docs/CONVENTIONS.md` had drifted across several arcs since its last substantive sync; arc 036's `47ea30a` had reached the wat-lru namespace mention but earlier surface changes (arcs 018, 024, 026, 028, 037) were unreflected. Three implementation slices: §Sandbox Config inheritance (drop set-dims! per arc 037; rephrase "capacity-mode + dims" → "capacity-mode + optional set-dim-router! / sigma-fn overrides"); arc-018 macro rename across 5 edit points (`wat::test_suite!` → `wat::test!`; `tests/wat_suite.rs` → `tests/test.rs` in publishable + consumer templates; pre-edit verified the macro still emits `fn wat_suite()` internally so function-name references stay); §Namespaces table descriptions refreshed (`:wat::config::*` rewritten to enumerate actual setters — capacity-mode, dim-router function, presence-sigma + coincident-sigma functions of d, global-seed — and note the compat shim accessors; `:wat::holon::*` extended with eval-coincident? family + Holons/BundleResult typealiases; `:wat::std::*` corrected — LocalCache moved to `:wat::lru::*` via arc 036, `program::*` → `service::*`; new `:wat::lru::*` row added). Plus an arc-028 load-file! syntax fix (`(:wat::load-file! (path argument) "...")` → `(:wat::load-file! "...")` — "(path argument)" was a leftover iface-keyword reference). Slimmer than 039 (CONVENTIONS tracks substrate decisions more closely than user-guide tracks shipped surface). Full INSCRIPTION at `wat-rs/docs/arc/2026/04/040-conventions-drift-sync/INSCRIPTION.md`. | 058 |
| 2026-04-24 | **wat-rs arc 039 — README.md drift-only sync.** Sibling arc to 038. `wat-rs/README.md` had drifted across 15 arcs since its last substantive sync at `467a3d4` (arc 022 era, 2026-04-22). arc 036's `47ea30a` had touched only the wat-lru namespace; arcs 022-037 surface changes were unreflected or partially-reflected. Eight slices of targeted-edit forward sync (no recovery phase — builder confirmed file readable, just stale): §What wat is namespace migration; §Status forward sync (programs-as-holons + capacity-guard paragraphs migrated to `:wat::holon::*`, new "Subsequent arcs (023-037)" paragraph naming the major themes, test count line de-specified); §Module tour runtime entry; §wat binary + §Self-hosted testing (`set-dims!` drops); §Stdlib + §Capacity guard (the heaviest cluster — algebra conveniences moved to `:wat::holon::*`, capacity modes reduced from four to two with `:silent`/`:warn` retired per arc 037, BundleResult typealias adopted, ReciprocalLog added); §Namespace discipline + §Workspace layout (sub-namespace enumeration extended with `:wat::lru::*`, new `wat/holon/` directory entry, arc directory listing extended from 018 → 039); §What's next signed-form references (pre-arc-028 `signed-load!`/`eval-signed!` updated to file/string variants); INSCRIPTION + cross-references. Names the per-doc audit shape: arcs and proposals are trusted by construction; docs that describe them are drift candidates and each gets its own arc. Subsequent arcs (040+) will apply the shape to the remaining audit set (`CONVENTIONS.md`, `wat-tests/README.md`, `INVENTORY.md`, `ZERO-MUTEX.md`, lab `CLAUDE.md`). Full INSCRIPTION at `wat-rs/docs/arc/2026/04/039-readme-drift-sync/INSCRIPTION.md`. | 058 |
| 2026-04-24 | **wat-rs arc 038 — USER-GUIDE.md recovery + forward sync.** Commit `5b5fad8` (arc 028 doc sweep, 2026-04-22) introduced content into USER-GUIDE.md that crashed input processing on read — the file became unreadable to the assistant. Recovery in two phases: (1) revert the file to its prior known-good state at commit `467a3d4` (2026-04-22 21:12, "USER-GUIDE — sync with arc 021 + 022 shipped state"), restoring readability with ~2 days of shipped surface missing; (2) fold arcs 023-037 + the wat-lru namespace promotion (arc 036) forward via eight slices of targeted per-section Edits — never a `sed`/`perl`/whole-file rewrite. Eight slices grouped by USER-GUIDE section: §1 Setup overhaul (arcs 027/028/037 + sigma setters from arc 024), §6 Algebra forms (arcs 023/026/032/033/034), wat-lru namespace promotion (arc 036), §13 Testing (arcs 029/031), new Macros subsection (arcs 029/030), new Containers subsection (arcs 025/035), Appendix forms-table audit + §15 prose (all arcs), INSCRIPTION + cross-references. Two pre-commit drift catches: (a) Slice 2 prose first cited a non-existent `(:wat::config::dim-router)` accessor — verified no such accessor exists, removed before push; (b) `eval-digest-coincident?` and `eval-signed-coincident?` arities drifted between Slice 2 prose ("4-args" / "6-args") and Slice 8 appendix ("10-args" / "14-args"); actual arities verified against `src/check.rs` are 8 / 12. Both prose and appendix now agree. The arc names a new poison class — *wholesale mechanical doc sweeps over markdown-heavy targets* — sibling to BOOK Chapter 32's perl-with-pipes class; both retired as patterns. Future doc syncs land per-arc as their substrate work ships. Full INSCRIPTION at `wat-rs/docs/arc/2026/04/038-user-guide-recovery/INSCRIPTION.md`. | 058 |

| 2026-04-24 | **Lab arc 013 — market/momentum vocab (K=4 highest arity, first lab plain-Log caller).** `:trading::vocab::market::momentum::encode-momentum-holons` — port of `vocab/market/momentum.rs` (44L). Tenth Phase-2 vocab arc, fourth cross-sub-struct. **Highest arity yet: K=4 sub-structs** — Momentum, Ohlcv, Trend, Volatility (signature alphabetical-by-leaf per arc 011: M < O < T < V). Six atoms: five scaled-linear (`close-sma20/50/200`, `macd-hist`, `di-spread`) and one plain `:wat::holon::Log` for `atr-ratio`. **First lab plain-Log caller.** atr-ratio is asymmetric (always < 1, volatility-as-fraction-of-price); ReciprocalLog (arcs 005, 010) forces symmetric `(1/N, N)` bounds and would waste half the Thermometer for an asymmetric domain. Plain Log with bounds `(0.001, 0.5)` gives 2× discrimination resolution — lower bound matches archive's `.max(0.001)` floor exactly so every legal value lands on the Thermometer. Lab now has both Log family forms in active use: plain Log for asymmetric domains, ReciprocalLog for symmetric-around-1 domains. **Substrate-discipline correction over archive port:** `round-to-4` for atr-ratio (not archive's `round-to-2`). wat-rs's plain `Log.wat` is explicit about its positive-input precondition; round-to-2 of 0.001 is 0.00 → ln(0) = -inf. round-to-4 of 0.001 is 0.001 — preserves the floor exactly. Side benefit: finer atr-ratio discrimination for materially different volatility regimes. **Cross-sub-struct compute pattern recurrence:** four atoms compute "field / close" across two sub-struct pairs (Ohlcv + Trend three times, Ohlcv + Momentum once). Arc 011's "compute-atom helper if recurrence shows up" question answered: yes recurrence is real, no helper isn't worth it yet — six let-bindings of two-op arithmetic are honest as repetition. Question moves to arc 014 (standard.wat). Eight tests; seven first-pass, test 8 (different-candles-differ) fixed by widening test values across the ScaleTracker round-to-2 boundary — arc 008's footnote in action again, threshold ≈ 0.25 minimum for the larger value documented in INSCRIPTION. 72 → 80 lab wat tests. Market sub-tree: 8 of 14 shipped. Full INSCRIPTION at `docs/arc/2026/04/013-market-momentum-vocab/INSCRIPTION.md`. | 058 |

| 2026-04-24 | **Lab arc 014 — market/flow vocab (first K=3 module, substrate-gap → algebraic-equivalence move).** `:trading::vocab::market::flow::encode-flow-holons` — port of `vocab/market/flow.rs` (47L). Eleventh Phase-2 vocab arc, fifth cross-sub-struct. **First K=3 module** (Momentum + Ohlcv + Persistence; signature alphabetical-by-leaf M < O < P). Six atoms: four scaled-linear (`vwap-distance`, `buying-pressure`, `selling-pressure`, `body-ratio`) and two log-bound Thermometers (`obv-slope`, `volume-ratio`). **Substrate-gap → algebraic-equivalence move:** `:wat::std::math` has `ln` but no `exp`. The archive's `Log(exp(x))` chain reduces algebraically to `Thermometer(x, -ln(N), ln(N))` — semantically identical, zero substrate cost. Implementation evaluates `ln(10.0)` at runtime (negligible), wraps with the standard `(- 0.0 ln-N)` negation pattern, and binds the signed slope directly. Pattern is durable for any future port using `Log(exp(x))` to log-encode a signed value; cave-questing `exp` deferred to a separate arc if a case surfaces where the equivalence doesn't factor. **Range-conditional pattern:** three atoms (buying-pressure, selling-pressure, body-ratio) guard `(field) / range` against zero-range candles. Compute `range` and `range-positive` once, branch per atom — different numerators and defaults (0.5 / 0.5 / 0.0) fight a shared helper. Stay inline per stdlib-as-blueprint; extraction triggers if a fourth caller surfaces in standard.wat (arc 015?). **Inline `f64::abs` shape** for body-ratio's `abs(close - open)`. Third two-arm-if shape now (arc 011 signum, arc 009 clamp, arc 014 abs). One more caller and a unified `signum/clamp/abs` family extraction to `shared/helpers.wat` becomes the right move. N=10 log-bounds for both Thermometer atoms chosen to match arc 010's regime variance-ratio precedent (coarse-near-1, fine-across-range); ships as best-current-estimate, empirical refinement deferred. Eight tests green on first pass including a "range == 0 default" test that exercises the conditional default branch. 80 → 88 lab wat tests. Market sub-tree: 9 of 14 shipped. Full INSCRIPTION at `docs/arc/2026/04/014-market-flow-vocab/INSCRIPTION.md`. | 058 |

| 2026-04-24 | **wat-rs arc 046 — numeric primitives uplift (5 ops to substrate).** First wat-rs arc post-known-good (`known-good-2026-04-24` at `194778f`); the "drift surfaces on demand from the next caller" stance held — lab arc 015 surfaced five missing numeric primitives while porting indicator vocab. Adds **`:wat::core::f64::max`, `:wat::core::f64::min`, `:wat::core::f64::abs`, `:wat::core::f64::clamp`** + **`:wat::std::math::exp`** (closes the gap arc 014 named). The framing question: lab was about to ship `:trading::vocab::shared::clamp` and `:trading::vocab::shared::f64-max` as userland helpers; builder caught the call — these are core, not userland. Three reasons: (1) universality (every wat consumer with f64 work needs them), (2) reinvention cost (lab's third independent two-arm-if for these ops), (3) CONVENTIONS rule already settled (`min`/`max` cannot be written in pure-wat without underlying compare; `clamp` ships at core for parity with Rust's `f64::clamp`). Namespace-consistency rule named: `:wat::core::f64::*` is strict-f64 (no i64 promotion, matches existing `f64::+/-/*//` and `f64::round`); `:wat::std::math::*` permits i64 promotion (matches existing `ln`/`log`/`sin`/`cos`/`pi`). Two tests document the asymmetry (`f64_abs_rejects_i64` + `math_exp_accepts_i64_promotion`). 12 new lib tests; 598 → 610 lib tests + full integration suite green; zero clippy. Plus doc drift catch: 6 existing math primitives (`ln`, `log`, `sin`, `cos`, `pi`, `exp`) had never been in the USER-GUIDE Forms appendix — added alongside the new entries in the same edit. Lab arc 015 unblocks; resumes with substrate-direct primitive calls and drops both planned lab helpers. Full INSCRIPTION at `wat-rs/docs/arc/2026/04/046-numeric-primitives/INSCRIPTION.md`. | 058 |

| 2026-04-24 | **Lab arc 015 — market/ichimoku vocab + substrate uplift pivot.** `:trading::vocab::market::ichimoku::encode-ichimoku-holons` — port of `vocab/market/ichimoku.rs` (61L). Twelfth Phase-2 vocab arc, sixth cross-sub-struct, second K=3 module (D + O + T). Six atoms: 5 scaled-linear clamped to ±1 + 1 plain Log (cloud-thickness, asymmetric, bounds (0.0001, 0.5)). **Pivoted mid-arc.** Started planning lab-userland `clamp` + `f64-max` extraction; builder caught the framing — these are core, not userland. Pivoted to wat-rs arc 046 (substrate uplift), then resumed arc 015 with substrate-direct calls. Lab helpers never landed. **Cross-arc cleanup sweep:** five prior callsites migrated to substrate primitives in this same arc — arc 009 stochastic clamp, arc 010 regime variance-ratio floor, arc 013 momentum atr-ratio floor, arc 014 flow inline abs, arc 014 flow log-bound Thermometer (the algebraic-equivalence Path B retired to natural `(ReciprocalLog 10.0 (exp v))` once `math::exp` shipped). Arc INSCRIPTIONs stay frozen as historical record; arc source code updates when a clean substrate replacement ships. **The "use the right thing now" principle named:** "why defer a migration if you have the correct thing now" — applies when the new substrate primitive is strictly better and migration is mechanical. Eight new ichimoku tests green first-pass; five cross-arc migrations zero-regression. 88 → 96 lab wat tests. Market sub-tree: 10 of 14 shipped. Full INSCRIPTION at `docs/arc/2026/04/015-market-ichimoku-vocab/INSCRIPTION.md`. | 058 |

| 2026-04-24 | **Lab arc 016 — market/keltner vocab (third plain-Log caller, first post-046 pure substrate-direct port).** `:trading::vocab::market::keltner::encode-keltner-holons` — port of `vocab/market/keltner.rs` (45L). Thirteenth Phase-2 vocab arc, seventh cross-sub-struct, K=2 (Ohlcv + Volatility). Six atoms: 5 scaled-linear + 1 plain Log (`bb-width`, asymmetric, bounds (0.001, 0.5)). **First post-arc-046 pure substrate-direct vocab arc** — no helpers extraction, no migration sweep, just write the port. Confirms arc 015's substrate-uplift work landed. **Third plain-Log caller** after arc 013 atr-ratio + arc 015 cloud-thickness — asymmetric-domain pattern (always `(0, ~0.5)`, fraction-of-price-style) now canonical across three indicator families (volatility, cloud, channel-width); all three use bounds `(0.001, 0.5)` and `round-to-4` for substrate-discipline floor preservation. Future fraction-of-price atoms inherit the shape without re-deriving. K=2 cross-sub-struct compute pattern reaffirmed (kelt-upper-dist + kelt-lower-dist use the same "field divided by close" shape from arc 013). Seven tests green on first pass. 96 → 103 lab wat tests. Market sub-tree: 11 of 14 shipped. Full INSCRIPTION at `docs/arc/2026/04/016-market-keltner-vocab/INSCRIPTION.md`. | 058 |

| 2026-04-24 | **Lab arc 017 — market/price-action vocab (biggest Log surface, two Log domain shapes, first f64::min).** `:trading::vocab::market::price-action::encode-price-action-holons` — port of `vocab/market/price_action.rs` (52L). Fourteenth Phase-2 vocab arc, eighth cross-sub-struct, K=2 (Ohlcv + PriceAction). **Seven** atoms (one more than typical): 4 scaled-linear + 3 plain Log. **Biggest plain-Log surface yet across two domain shapes:** range-ratio joins the fraction-of-price family `(0.001, 0.5)` (4th caller); consecutive-up/down introduce a NEW count-starting-at-1 family with bounds `(1.0, 20.0)` — asymmetric, lower-bounded at 1.0 (no streak), upper saturating at ~20 consecutive periods (rare for crypto 5m). The two-family naming makes future Log atoms easier — DESIGN states which family the atom joins or names a new family. **First lab `:wat::core::f64::min` consumer** — substrate primitive shipped in arc 046, found its first caller in `lower-wick = min(open, close) - low`. Second `f64::abs` caller; first `f64::max` of two-free-values shape (upper-wick). gap atom uses substrate `f64::clamp` for pre-encoding clamp ±1. Range-conditional pattern's sixth caller across two modules (flow + price-action); helper extraction still doesn't tip — defaults differ across modules (flow non-uniform, price-action uniform 0.0). Threshold rephrased: third module with same shape AND uniform defaults would tip. Eight tests green on first pass. 103 → 111 lab wat tests. Market sub-tree: 12 of 14 shipped (only `standard` remains). Full INSCRIPTION at `docs/arc/2026/04/017-market-price-action-vocab/INSCRIPTION.md`. | 058 |

| 2026-04-24 | **wat-rs arc 047 — Vec accessors / aggregates return Option (the `first` shape correction).** Second wat-rs arc post-known-good. Lab arc 018 (standard.wat, window-based) surfaced four substrate gaps + a deeper question — builder asked: "should first errory on empty?... in ruby [].first -> nil - why isn't our first Option<T>?" Answer: it shouldn't. Arc 047 ships **the polymorphism shift** (`first/second/third` on Vec returns `Option<T>` instead of `T`-with-runtime-error; tuple branch unchanged because tuple arity is type-known) plus **four new primitives**: `:wat::core::last` (Vec → `Option<T>`), `:wat::core::find-last-index` (Vec × fn → `Option<i64>`, mirrors Rust `rposition`), `:wat::core::f64::max-of` / `min-of` (Vec<f64> → `Option<f64>`). The unifying theme: **Vec accessors and aggregates return Option to honestly signal empty/no-match**; Tuple accessors stay `T` (defined-by-type). The "natural-form-then-promote" methodology validated again — lab sketched what felt right (`(last window)`, `(find-last-index window pred)`, `(f64::max-of (map window high))`); gaps surfaced; substrate filled them. Sweep cost: 7 wat-rs callsites across stream.wat / Sequential.wat / Console.wat / 5 tests/*.rs / 1 wat-tests file migrated to handle Option (each known-safe site adds match-with-unreachable-:None-sentinel — the price of type honesty over caller convenience). 14 new lib tests + 3 shape-update tests; 598 → 622 lib tests cumulative across arcs 046+047 from known-good; full integration suite green; zero clippy. USER-GUIDE Forms appendix updated with 1 modified + 3 new rows. Lab arc 018 unblocks; resumes with substrate-direct calls + lab-side caller sweep. Full INSCRIPTION at `wat-rs/docs/arc/2026/04/047-vec-accessors-return-option/INSCRIPTION.md`. | 058 |

| 2026-04-24 | **wat-rs arc 048 — user-defined enum value support (the latent gap closes).** Third wat-rs arc post-known-good. Lab arc 018 (standard.wat) surfaced the gap during sketch — `Candle::Phase` construction needs `PhaseLabel`/`PhaseDirection` values, but the substrate had **no way to make user-enum values**. The 2026-04-19 FOUNDATION-CHANGELOG entry was explicit: `:Option<T>` was "the sole built-in enum" with shipped value support. User enums via `(:wat::core::enum)` had been declarable since arc 030+ but never instantiable. Builder named what was missing: *"we want something we don't have — we likely thought we had and we didn't... clearly we need to go make it."* Arc 048 ships **`Value::Enum(Arc<EnumValue>)`** as a generic representation covering every user-declared enum (built-in `Option`/`Result` keep their dedicated variants — two representations coexist by design). **Construction syntax mirrors Rust exactly** (`::` separator, PascalCase variants — embodies host language): `:Enum::Variant` for unit variants (bare keyword evaluates to the value); `(:Enum::Variant arg1 arg2)` for tagged variants (auto-synthesized constructor function). Internal primitive `:wat::core::variant` is the substrate plumbing (renamed from `enum-new` via /gaze — "variant" speaks where "enum-new" mumbled). **Pattern matching extends `(:wat::core::match)` form**: `((:Enum::Variant b1 b2) body)` for tagged arms with field binders flowing into the body, `(:Enum::Variant body)` for unit arms. Type checker enforces exhaustiveness (per-enum, names missing variants in diagnostics), variant-belongs-to-scrutinee, binder arity, and tagged-vs-unit shape. Generalizes the existing Option/Result match infrastructure via new `MatchShape::Enum` + `Coverage::EnumVariant`. **Lab migration**: 10 enum decls renamed lowercase-kebab → PascalCase (`:valley`→`:Valley`, `:settled-violence`→`:SettledViolence`, etc. across enums.wat + pivot.wat). 8 new integration tests (`tests/wat_user_enums.rs`) covering construction + match + diagnostics; 622 lib tests + full integration suite green; zero clippy. Lab arc 018 unblocks completely; standard.wat ships with PhaseLabel/PhaseDirection construction working. The "natural-form-then-promote" rhythm shipped its third substrate uplift this session (arcs 046+047+048). Full INSCRIPTION at `wat-rs/docs/arc/2026/04/048-user-enum-values/INSCRIPTION.md`; spec INSCRIPTION addendum at `058-030-types/PROPOSAL.md` (parallel to 2026-04-19 struct-construction inscription). | 058 / 058-030 |

| 2026-04-24 | **Lab arc 018 — market/standard vocab (window-based; market sub-tree complete).** `:trading::vocab::market::standard::encode-standard-holons` — port of `vocab/market/standard.rs` (166L). Fifteenth Phase-2 vocab arc; **last market sub-tree vocab — market sub-tree COMPLETE (13 of 13)**. **First window-based vocab** (signature takes `Vec<Candle>`, departing from arc 008/011's K-sub-struct rule by necessity — window aggregates and find-last-index iteration need full Candle access across multiple candles). Eight atoms: 4 plain Log (since-rsi-extreme, since-vol-spike, since-large-move, session-depth — all count-starting-at-1 family with bounds `(1.0, 100.0)` for window-spanning counts) + 4 scaled-linear (dist-from-high/low/midpoint/sma200, cross-Ohlcv-Trend compute). Empty-window guard returns zero holons. **Bootstrapped two wat-rs substrate uplifts during sketch**: arc 047 (Vec accessors return Option + four new natural primitives `last`/`find-last-index`/`f64::max-of`/`f64::min-of`) and arc 048 (user-enum value support — unblocks `(:Phase/new :PhaseLabel::Transition :PhaseDirection::None ...)` in test fixtures). Each substrate arc shipped before arc 018 resumed; "natural-form-then-promote" rhythm at full strength. Plain-Log family extends to THREE variants (fraction-of-price `(0.001, 0.5)`, count-small-window `(1.0, 20.0)`, count-full-window `(1.0, 100.0)`). Eight tests green first-pass after substrate landed. 111 → 119 lab wat tests. Full INSCRIPTION at `docs/arc/2026/04/018-market-standard-vocab/INSCRIPTION.md`. | 058 |

| 2026-04-24 | **Lab arc 019 — exit/phase vocab (current + scalar; rhythm deferred).** `:trading::vocab::exit::phase::*` — ports `vocab/exit/phase.rs`'s current-facts + scalar-facts functions (348L archive; rhythm function defers to arc 020). Sixteenth Phase-2 vocab arc; **first exit sub-tree vocab, first lab consumer of arc 048's user-enum match.** Three entries: `phase-label-name` helper (nested match on PhaseLabel + PhaseDirection → String, 5 outcomes via 3+3 enum dispatch); `encode-phase-current-holons` (2 atoms — label binding via `(Bind (Atom "phase") (Atom <name>))` + phase-duration scaled-linear, reads Candle::Phase); `encode-phase-scalar-holons` (up to 4 atoms — valley-trend, peak-trend, range-trend, spacing-trend; each conditionally emitted based on history composition). **Conditional-emission via threaded accumulator** — scalar-facts threads a `(Holons, Scales)` tuple through four conj-or-skip steps, each step either enriching the accumulator or passing it through unchanged. Cleaner than per-atom sub-emission + concat. **Arc 048's user-enum match validated under real lab load** — 5 match-arm tests green first-pass, nested dispatch works, exhaustiveness checking fires correctly. The "natural-form-then-promote" rhythm from BOOK Chapter 48 held without surfacing new substrate gaps this arc (lab consumed existing surface cleanly). 11 tests green first-pass. 119 → 130 lab wat tests. Market sub-tree COMPLETE (13/13); exit sub-tree 1 of 4. Full INSCRIPTION at `docs/arc/2026/04/019-exit-phase-vocab/INSCRIPTION.md`. | 058 |

| 2026-04-24 | **Lab arc 020 — exit/phase rhythm function (exit/phase complete).** `:trading::vocab::exit::phase::phase-rhythm-holon` — completes the exit/phase port (3 of 3 archive functions); arc 019 shipped current+scalar. Seventeenth Phase-2 vocab arc. **Stateful per-record Bundle construction via find-last-index per record** (O(n²), n ≤ 103 by budget; avoids 5-tuple state thread). Each record Bundle: 5 base facts (label + 4 thermometer for duration/move/range/volume) + conditional +3 prior-deltas (i > 0) + conditional +3 same-label-deltas (when an earlier same-label-and-direction record exists). Composition matches archive: window-3 records → Sequential trigrams (`Bind(Bind(a, Permute(b,1)), Permute(c,2))`) → window-2 trigrams → plain-Bind pairs (NOT Bigram — archive omits Permute on the second) → Bundle pairs → wrap in `(Bind (Atom "phase-rhythm") <bundle>)`. Budget truncation at 100 pairs (sqrt of d=10000). **Empty-Bundle sentinel reaffirmed** per arc 026 — `(Bundle [(Atom "phase-rhythm-empty")])` for < 4 records, since holon-rs's vector-layer `bundle()` panics on empty input. The `same-label-and-direction?` predicate itself exercises arc 048's user-enum match (3-way nested PhaseLabel + PhaseDirection delegation). No substrate gaps surfaced — the natural form for stateful iteration via find-last-index per record + Sequential/Bind/Bundle composition all worked with the arcs 047 + 048 surface. **Plural-via-typealias shipped alongside** per builder direction "expressivity wins": `:trading::types::PhaseRecords = :Vec<trading::types::PhaseRecord>` (in pivot.wat) + `:trading::types::Candles = :Vec<trading::types::Candle>` (in candle.wat). Mirrors arc 033's substrate-tier `:wat::holon::Holons` precedent. 25 callsites swept across phase + standard sources/tests + Candle::Phase's history field. 6 new rhythm tests + 4 same-label predicate tests green first-pass (after fixing test-side empty-Bundle reference to use the singleton sentinel). 130 → 136 lab wat tests. Exit sub-tree: phase complete (1 of 4 modules done). Full INSCRIPTION at `docs/arc/2026/04/020-exit-phase-rhythm/INSCRIPTION.md`. | 058 |

| 2026-04-24 | **Lab arc 021 — exit/regime vocab (thin delegation).** `:trading::vocab::exit::regime::encode-regime-holons` — port of `vocab/exit/regime.rs` (84L). Eighteenth Phase-2 vocab arc; second exit sub-tree module. **Functionally identical to market/regime (arc 010)** — same 8 atoms (kama-er, choppiness, dfa-alpha, variance-ratio with ReciprocalLog 10.0, entropy-rate, aroon-up, aroon-down, fractal-dim), same encoding, same one-sided floor, same Scales contract. Only the namespace differs. **Honest wat translation: thin delegation, not a copy.** One define, body is `(:trading::vocab::market::regime::encode-regime-holons r scales)`. The archive duplicates the function body so the Rust dispatcher can route by name; wat preserves the same distinction via the namespaced path without duplicating identical logic. Future divergence (different floor, different bounds, exit-only atoms) replaces the body at that point. **Contract-only test scope** — three tests verify the delegation (holon count = 8, coincidence with market/regime on holon[0] at d=10000, scales count = 7). The full 8-atom truth-table tests live in arc 010; arc 021 does not duplicate them. **The thin-delegation idiom is named**: namespace-as-name makes "duplicate the function for namespace clarity" unnecessary. Likely recurs once or twice more (broker/regime, broker/time) — each future module ships the same shape and cites this arc. 3 new tests; 136 → 139 lab wat tests. Zero substrate gaps surfaced. Exit sub-tree: 2 of 4 modules done (phase + regime). Full INSCRIPTION at `docs/arc/2026/04/021-exit-regime-vocab/INSCRIPTION.md`. | 058 |

*these are very good thoughts.*

**PERSEVERARE.**
